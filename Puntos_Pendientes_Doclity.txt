PENDIENTES — Doclity (Agente IA)
================================
Documento vivo. Se actualiza al completar tareas (marcar [✓]) y se
sustituye por la versión nueva. Más ligero que la Bitácora: aquí va
lo que falta y la visión que lo guía, no el detalle de cada sesión.
Última actualización: 02/09/2026 (v45)


════════════════════════════════════════════════════════════════
PARTE A — VISIÓN Y DECISIONES CONCEPTUALES (cerradas)
════════════════════════════════════════════════════════════════

Esta parte NO es código pendiente: es el marco que guía las tareas de
la Parte B. Está cerrada y validada. Si una tarea contradice esto,
manda esto.


A.1 — COMPORTAMIENTO DEL AGENTE: "información primero, decisiones al usuario"
─────────────────────────────────────────────────────────────────────────

Regla raíz: el agente no inventa hechos del dominio de la empresa.
Primero investiga, y cada punto no resuelto se convierte en una
pregunta de decisión por adelantado, no en una suposición.

Flujo ante cualquier petición:
  1. INSPECCIONA primero lo que hay (busca en el corpus; si la tarea
     toca estructuras como tablas/esquemas, las mira antes de actuar).
  2. DETECTA las bifurcaciones que cambian el resultado
     (ej. ¿incluyo clientes sin ventas?, ¿qué relación hay entre tablas?).
  3. PREGUNTA por adelantado TODAS las decisiones de una vez,
     no a base de entregar-y-corregir.
  4. EJECUTA solo entonces. El conocimiento general del agente rellena
     el CÓMO (construir la query, redactar el correo), nunca el QUÉ
     (qué dice la realidad de la empresa: qué es tal campo, qué tablas
     hay, cómo se relacionan). Eso no se inventa: se busca o se pregunta.

Clave de la propiedad: la INFORMACIÓN MANDA. Lo documentado tiene
prioridad sobre lo que pida el usuario.

Cláusula de aviso (IMPORTANTE — riesgo de operar fuera de directrices):
  Cuando el agente rellene un hueco con conocimiento general que el
  usuario PODRÍA estar asumiendo que está documentado, debe avisarlo.
  Peligro real: el usuario cree que la empresa lo tiene documentado,
  el agente no lo encuentra, rellena en silencio, y el usuario acaba
  operando fuera de las directrices de su empresa creyéndose dentro.
  Eso es peor que no responder.

  Forma del aviso (UX): NO un párrafo dentro de la respuesta. Un icono
  de información visible que al pasar por encima se expande y muestra
  el mensaje (tipo: "Esto no lo he encontrado en vuestra documentación,
  lo he hecho con criterio general. Si tenéis norma propia, decídmela
  o añadidla al corpus."). Visible porque importa; sin ensuciar.

  Efecto de producto buscado: la ausencia de documentación pasa a ser
  una SEÑAL ÚTIL para el cliente (ve dónde le falta documentar), no un
  fallo silencioso.


A.2 — APRENDIZAJE: visión conceptual (diseño cerrado — ver B.6 para especificación completa)
─────────────────────────────────────────────────────────────────────────────────────────────

Premisa: guardar soluciones como ejemplos literales es contraproducente.
Se aprende el MAPA del territorio (hechos/convenciones estables),
no la RUTA concreta de una vez (la decisión de ese caso).

Tres tipos de cosa aprendida con mecanismos distintos (ver B.6 para detalle
completo de cada tipo, mecanismo de captura e interruptor de enseñanza).

ESTADO: DISEÑO CERRADO. Implementación = Fase Final del proyecto.
Ver B.6 para la especificación técnica completa.


A.3 — ENCAJE DE PRODUCTO (promesa honesta)
──────────────────────────────────────────

La promesa al cliente PYME es: "aplica vuestras reglas y os pregunta
lo que no sabe", NO "juicio humano perfecto".
Un agente replica criterios y convenciones; no replica el juicio tácito
del experto. La promesa humilde se cumple siempre y es más vendible a
largo plazo. El "trabajador perfecto" es el objetivo aspiracional, no
la garantía contractual.


A.4 — DIFERENCIACIÓN FRENTE A CRM / ERP / BI (visión cerrada — ver B.20 para implementación)
─────────────────────────────────────────────────────────────────────────────────────────────

Esta es la apuesta estratégica grande del producto a medio plazo. Marco
conceptual cerrado; la ejecución vive en B.20 (Parte B).

EL HUECO QUE OCUPAMOS
  Un CRM/ERP o un Power BI/MicroStrategy responden al QUÉ cuantitativo:
  "en Madrid se vende un 30% más que en Sevilla". Lo hacen agregando
  números de tablas. Pero se quedan ciegos ante el POR QUÉ cualitativo:
  "se vende más en Madrid porque tres de los cinco clientes enterprise
  tienen allí su sede de compras, según las actas de Q2". Esa frase NO
  está en ninguna tabla: vive en documentos (actas, correos, contratos,
  informes). Ahí es donde Doclity puede vivir y nadie más llega.

  Posicionamiento: NO somos el CRM/ERP, NO los sustituimos (ver "lo que NO
  hacemos" al final de A.4). Somos la CAPA DE INTELIGENCIA que se sienta
  ENCIMA de los datos que la empresa ya tiene, sumándole lo que esos
  sistemas nunca tuvieron: la documentación. Es la misma filosofía que ya
  aplicamos con Google Drive/OneDrive (registry pattern en lib/drive/):
  un proveedor que se enchufa. Aquí el proveedor es de DATOS, no de archivos.

EL COMPORTAMIENTO QUE NOS DIFERENCIA: investigación iterativa que cruza fuentes
  No es "mete un Excel en Pinecone". Es un agente de investigación en bucle:
    1. Recibe una pregunta abierta ("informe de ventas de España").
    2. Descubre qué fuentes hay y PIDE PERMISO antes de tocarlas
       ("hay estas tablas de ventas, ¿accedo? contienen tal, tal y tal").
    3. Saca un primer hallazgo cuantitativo ("se vende más en el sur").
    4. SE REPLANTEA SOLO — esto es lo clave — detecta que hay un "por qué"
       sin responder y decide ahondar.
    5. O pregunta al usuario si quiere profundizar, o lanza por su cuenta
       nuevas búsquedas: otra tabla, la documentación, cruza al cliente
       que concentra ventas, descubre su volumen/empleados/canal, etc.
    6. SINTETIZA todo en una explicación causal que ninguna fuente
       individual contenía.
  Conceptualmente esto YA lo permite el loop ReAct actual (runner-conv.ts):
  ya hace bucles, ya tiene search_docs/read_doc, ya pausa con escalate/ask_user.
  El "saco dato → me replanteo → busco más → sintetizo" es literalmente un
  ReAct bien promptado. La maquinaria del razonamiento NO hay que inventarla.

LAS DOS FRONTERAS DEL "POR QUÉ" (matiz importante, corrige una idea inicial)
  Hay dos tipos de "por qué" y conviene no confundirlos:
  · "POR QUÉ" QUE ESTÁ EN LOS DATOS: "vende más porque este cliente concentra
    el 60% del volumen y tiene 200 tiendas". El agente lo saca CRUZANDO TABLAS
    entre sí. NO necesita documentación para esto. (Idea aportada por el usuario,
    correcta: gran parte de la causa se descubre navegando tablas, sin docs.)
  · "POR QUÉ" QUE NO ESTÁ EN NINGÚN DATO: "vende más porque en marzo lanzamos
    una campaña regional", "porque el comercial de la zona es excepcional",
    "porque cambiamos de proveedor logístico". Esto NO aparece en ninguna tabla
    por muchas que se crucen. Solo existe si alguien lo escribió en un acta,
    informe o correo.
  RESUMEN: las tablas dan el QUÉ concentra las ventas; la documentación da el
  POR QUÉ pasó algo que las tablas no registran. El valor diferencial REAL
  aparece cuando la respuesta necesita LAS DOS. Un competidor que solo cruza
  tablas (un BI con IA) llega a la primera frontera. Doclity, bien hecho,
  cruza la segunda. La búsqueda debe ser SIEMPRE entre ambas, nunca quedarse
  en una sola.

DEPENDENCIA DE LA CULTURA DOCUMENTAL (dato de mercado, no defecto)
  La calidad del análisis causal "de segunda frontera" depende de que la
  empresa haya documentado el porqué. Si la causa solo está en la cabeza del
  director comercial y nunca se escribió, el agente cruzará tablas, no hallará
  el porqué en docs, y lo dirá con honestidad (encaja con reglas de honestidad
  del RAG y con A.1). Consecuencia: el producto BRILLA en empresas con buena
  cultura documental y rinde menos en las que documentan poco. Esto es un dato
  de posicionamiento a tener claro de antemano, NO una crítica. Además conecta
  con A.1: la ausencia de documentación es una SEÑAL ÚTIL (ves dónde te falta
  documentar), no un fallo silencioso.

EL USUARIO COLABORA (no es un oráculo)
  No construimos un adivino. Construimos un investigador que colabora con un
  humano que conoce su negocio. Una buena pregunta inicial, o responder al
  agente cuando pregunta "¿ahondamos por aquí?", mejora enormemente el
  resultado. Más realista y más vendible: el cliente siente que tiene el control.

LO QUE NO HACEMOS (límite explícito)
  NO sustituir al CRM/ERP (no ser el "system of record"). La idea futura de
  "rellenar Excel y que el agente lo meta en la base de datos" queda FUERA de
  alcance por estrategia, no por dificultad: ser el sistema de registro es una
  batalla brutal de fiabilidad, permisos, auditoría y confianza, y las empresas
  no migran su ERP a la ligera. Conectarse, no reemplazar.


════════════════════════════════════════════════════════════════
PARTE B — TAREAS PENDIENTES (orden a confirmar)
════════════════════════════════════════════════════════════════
EL MAPA DEL MVP — los cuatro frentes (F-89 P5/P6, 30/08/2026)
════════════════════════════════════════════════════════════════

PARA QUÉ ESTÁ ESTO AQUÍ. La lista de pendientes de abajo se lee como deuda
suelta, y no lo es: casi ningún pendiente vivo necesita arreglo propio —
necesita QUE SU FRENTE LLEGUE. Este mapa dice de qué frente es cada uno.
Es el plan de cierre de F-89, y se lee ANTES que la lista.

EL PATRÓN QUE LO JUSTIFICA, verificado dos veces esta semana: un frente futuro
resuelve un problema presente. El ciclo de vida del documento mata el camino sin
id; las citas por referencia matan el límite de la huella de prosa. Arreglar el
síntoma antes de que llegue su frente es trabajo que se tira.

EL CRITERIO PARA LEER LA COLUMNA «bloquea»: está en el protocolo,
claude/Protocolo_Harness_Tasas.md, «REGLA DE CIERRE». En una línea: bloquea lo
que hace FALSO lo mostrado o PIERDE un juicio del usuario; se declara lo que sea
límite de cobertura CONTADO Y DICHO.

────────────────────────────────────────────────────────────────
FRENTE 1 — R2 y el emparejamiento          [BLOQUEA]   ← EL SIGUIENTE
────────────────────────────────────────────────────────────────
  Qué: R2 verifica el emparejamiento donde hay clave; guarda del ancla donde no
       la hay; supresión del juez sobre pares que el diff ya comparó.
  Absorbe: B.124, y de propina el «dieciséis donde hay quince».
  Forma: tres commits deterministas con suite (la cascada es código puro), y
         TANDA DE HARNESS después — cambia qué se confirma en el producto
         entero, no solo en tablas.
  Por qué bloquea: publica falsos con el sello más fuerte. Rompe la primera pata.

────────────────────────────────────────────────────────────────
FRENTE 2 — cierre de la emisión            [BLOQUEA]
────────────────────────────────────────────────────────────────
  Qué: Ficha B (acciones por fila sobre huella tabular + extensión del payload
       de F-87 a tablas) y el saneo de B.122 segunda mitad.
  Absorbe: Ficha B, B.122 (2ª mitad), y LA HIGIENE DE LAS AVERÍAS SILENCIOSAS —
       la que F-93 mandó aquí («toda avería de red deja contador aunque el flujo
       siga con fallback») más B.132, que es la misma especie vista en el camino
       de INDEXACIÓN: el sync cuenta sus fallos en una variable local y tira el
       nombre del documento, y el embed no reintenta un 5xx.
       LA FRASE QUE UNE LOS TRES CASOS, y es el criterio para saber si el frente
       está cerrado: EL AISLAMIENTO DEL FALLO SE IMPLEMENTÓ Y LA MEMORIA DEL
       FALLO NO. Mientras haya un camino donde algo se caiga y no quede rastro
       consultable, esta mitad del frente sigue abierta.
  Después: REMEDICIÓN COMPLETA, que ahora es también la validación del Frente 1.
  Por qué bloquea: el saneo de B.122 es HIGIENE DE DATOS DEL CLIENTE —veinte
       filas suyas viajando a tres prompts y a la persistencia—, y eso es cara
       al MVP, no opcional.

────────────────────────────────────────────────────────────────
FRENTE 3 — ciclo de vida del documento     [BLOQUEA]
────────────────────────────────────────────────────────────────
  Qué: la fila nace en la subida con estado (en_revision → indexado/descartado).
       Especificado en intención en F-87 P4.
  Absorbe: B.112 (huérfanos: la identidad pasa a ser la FILA y no el nombre),
       el camino sin id (jubila el puente del payload de F-87), y B.119 en sus
       caminos restantes —la persistencia de descartes de F-86 se extiende a
       solapamientos, estilo y demás cuando todo hallazgo cuelga de un documento
       con identidad estable.
  Por qué bloquea: B.112 enseña el análisis de un documento MUERTO —información
       falsa en pantalla— y eso no puede vivir declarado.
  NOTA: era «frente propio, y no va ahora» en F-87 P4. F-89 lo asciende a
       tercer frente del MVP. Ya no es futuro.

────────────────────────────────────────────────────────────────
FRENTE 4 — prosa                           [SE DECLARA]  post-MVP
────────────────────────────────────────────────────────────────
  Qué: segmentación estructural, apartado como unidad, small-to-big, citas por
       referencia. Especificación de F-80/F-81, en papel.
  Absorbe: B.106 (el techo de una-por-par se disuelve con apartado-contra-
       apartado y pasadas múltiples), B.107 (las citas por referencia matan la
       glosa y la narración de raíz), B.123 en su mitad de prosa, la selección
       al 4,7%, y la medición del exhaustivo en prosa larga.
  Por qué se declara: la promesa NUNCA FUE exhaustividad en prosa. Vive
       declarado CON SUS CONTADORES Y SUS AVISOS — y ojo, sin ellos no está
       declarado y entonces sí bloquea (ver la regla de cierre).

────────────────────────────────────────────────────────────────
DÓNDE CAE CADA PENDIENTE VIVO
────────────────────────────────────────────────────────────────
  B.106  → Frente 4     (declarable)
  B.107  → Frente 4     (declarable)
  B.112  → Frente 3     (BLOQUEA)
  B.119  → Frente 3     (BLOQUEA en el flujo principal)
  B.122  → Frente 2     (BLOQUEA — 1ª mitad ya hecha el 30/08)
  B.126  → Frente 2     (higiene: el fail-open que se traga los fallos de red)
  B.132  → Frente 2     (BLOQUEA — entra por el camino de indexación, que es
                         por donde llega TODO al corpus; medido en producción)
  B.123  → tablas: RESUELTO por el diff · prosa: Frente 4 (declarable)
  B.124  → Frente 1     (BLOQUEA)
  Ficha B → Frente 2    (BLOQUEA)
  Selección de prosa al 4,7%      → Frente 4 (declarable)
  Exhaustivo en prosa larga       → Frente 4 (declarable, sin medir)

  LOS QUE NO TIENEN FRENTE porque son de otra especie: B.102, B.104, B.105,
  B.108, B.109, B.110, B.111, B.113, B.114, B.115, B.116, B.117, B.118, B.121.
  Son límites de evidencia, deuda documental o mantenimiento — no cambian lo que
  el cliente ve. Se leen aparte y no entran en esta cuenta.

────────────────────────────────────────────────────────────────
QUÉ FALTA PARA ENSEÑÁRSELO A UN CLIENTE, en una línea
────────────────────────────────────────────────────────────────
  LOS FRENTES 1, 2 Y 3 CERRADOS, Y LA REMEDICIÓN EN VERDE.
  El 4 se declara. Textual de F-89: «esto se le enseña a un cliente diciendo la
  verdad entera — que es el único listón que este producto se ha puesto».

════════════════════════════════════════════════════════════════
LA DEUDA TÉCNICA — los cinco puntos, medidos el 01/09/2026
════════════════════════════════════════════════════════════════

POR QUÉ ESTÁ AQUÍ Y NO FUERA. Hasta hoy esta lista vivía FUERA del
repositorio, y ésa era la causa —no el síntoma— de que se pudriera: ningún
commit la tocaba, ningún `grep` la encontraba, y nadie la verificaba al usarla.
Al auditarla contra el código (B.134) resultó que CUATRO DE LOS CINCO PUNTOS
DIRÍAN ALGO FALSO si se citaran. Se trae al repositorio y se reescribe con el
estado real.

VA EN LA CABECERA DE LA PARTE B por la misma razón que el mapa del MVP: es
contexto estructural que se lee ANTES que la lista numerada, no un hallazgo
suelto. La lista de abajo dice qué está roto; esto dice qué está a medio
construir.

⚠️ CADA PUNTO LLEVA LA FECHA EN QUE SE COMPROBÓ. Sin esa fecha, un estado es
una opinión: dice lo que alguien creyó, no lo que el código hacía. Quien lo cite
después de un cambio grande, que lo vuelva a mirar y actualice la fecha.

────────────────────────────────────────────────────────────────
D1 · TOKENS OAUTH EN TEXTO PLANO            ✅ RESUELTO
────────────────────────────────────────────────────────────────
  Comprobado: 29/08/2026 · reconfirmado 01/09/2026
  `lib/crypto.ts` (AES-256-GCM, clave `DRIVE_ENCRYPTION_KEY`) se usa en los
  cuatro puntos de escritura, y una consulta a `drive_connections` confirmó las
  dos filas —google_drive y onedrive— con `access_token` y `refresh_token`
  cifrados.
  SE ARRASTRÓ MESES ESTANDO RESUELTO, sin que nadie lo comprobara. Es el
  precedente que justifica esta sección entera.

────────────────────────────────────────────────────────────────
D2 · OBSERVABILIDAD DE USO DE LLM POR USUARIO   ⚠️ MAL ENUNCIADO
────────────────────────────────────────────────────────────────
  Comprobado: 01/09/2026
  EL DATO EXISTE, y por partida doble, las dos con `user_id`:
    · `logUsage` → tabla `usage_logs` (`lib/usage-logger.ts`): usuario, org,
      endpoint, modelo, tokens de entrada/salida, tokens de caché de escritura
      y lectura, latencia, éxito, créditos consumidos y la consulta del usuario.
    · `persistLLMUsage` → tabla `llm_usage` (`lib/observability/record-usage.ts`):
      lo anterior más el COSTE EN DÓLARES calculado (`computeCostUsd`).
  Los dos CABLEADOS en ask, analyze-v2, analyze-style e improve; `recordUsage`
  además desde tres puntos de `lib/llm/anthropic-client.ts`.
  LO QUE FALTA ES LA VISTA, no la observabilidad: `/settings/usage` agrega por
  ORGANIZACIÓN y no desglosa por persona. La pregunta «cuánto gasta cada
  usuario» tiene respuesta en la base de datos y no en la interfaz.
  ⚠️ BLOQUEADO POR B.135: hay dos sistemas paralelos y no se sabe si uno
  sustituye al otro. Construir la vista antes de decidirlo es elegir tabla a
  ciegas.

────────────────────────────────────────────────────────────────
D3 · ANÁLISIS SÍNCRONO CON EL USUARIO ESPERANDO  ◐ A MEDIAS
────────────────────────────────────────────────────────────────
  Comprobado: 01/09/2026
  HECHO — el EXHAUSTIVO: crea un job en `analysis_jobs`, lo ejecuta el worker de
  Railway y el cliente sondea (`app/api/analysis-jobs/[id]/route.ts`). Era la
  mitad cara.
  VIVO — el RÁPIDO: sigue siendo síncrono, con `maxDuration = 120` en
  `app/api/analyze-v2/route.ts`. Medido en las tandas del 30-31/08: **10 a 18
  segundos con UN candidato**. Hay margen contra el tope, pero NO ESTÁ MEDIDO
  con muchos candidatos, que es cuando se acercaría.

────────────────────────────────────────────────────────────────
D4 · SIN FALLBACK ENTRE PROVEEDORES          🔴 VIVO — el único intacto
────────────────────────────────────────────────────────────────
  Comprobado: 01/09/2026
  `lib/pinecone.ts` y `lib/llm/anthropic-client.ts` tienen UN SOLO proveedor
  cada uno, sin alternativa ni degradación.
  ESTABA ESCRITO PARA EL LLM Y HOY SE ENSANCHA: la caída de Pinecone del 01/09
  —control plane, 5xx, más de cuarenta minutos— enseñó que aplica igual al
  vectorial, con el agravante de que sin él NO HAY NI CHAT NI ANÁLISIS. El LLM
  caído degrada una función; el vectorial caído apaga el producto.
  Ver B.133, que es este punto visto desde la incidencia.

────────────────────────────────────────────────────────────────
D5 · MEMORIA CONVERSACIONAL DEL RAG          ⚠️ MAL ENUNCIADO
────────────────────────────────────────────────────────────────
  Comprobado: 01/09/2026
  LA MEMORIA CONVERSACIONAL EXISTE: `MAX_HISTORY_MESSAGES = 6` en `lib/rag.ts`,
  la reescritura de la consulta usa el historial (`rewriteQueryForSearch`), y el
  historial viaja al modelo en la llamada final.
  LO QUE NO SE REUTILIZA SON LOS DOCUMENTOS RECUPERADOS: cada turno rehace el
  retrieval desde cero, aunque la conversación siga sobre los mismos documentos
  del turno anterior. Cuesta una consulta a Pinecone y una reescritura por
  turno, y puede cambiar de fuentes a mitad de conversación sin motivo.
  EL PUNTO VIVO ES ÉSE, y es más estrecho que «no hay memoria conversacional».

────────────────────────────────────────────────────────────────
EL RECUENTO DEL 01/09, para que se vea de un vistazo
────────────────────────────────────────────────────────────────
  RESUELTO ............ 1   (D1)
  A MEDIAS ............ 1   (D3, falta el rápido)
  MAL ENUNCIADO ....... 2   (D2, D5 — el problema existe, la frase no)
  VIVO TAL CUAL ....... 1   (D4)

  CUATRO DE CINCO DIRÍAN ALGO FALSO si se citaran con el texto viejo.




════════════════════════════════════════════════════════════════

Leyenda: [ ] pendiente · [~] en curso · [✓] hecho


[✓] B.1 — (UX) Barra indicadora de créditos disponibles
      Recuperar la barra de créditos que existía en /chat y desapareció
      al rediseñar el layout. Mostrarla en el agente y donde corresponda.
      VISIÓN: el usuario debe ver su saldo sin buscarlo; transparencia
      de consumo, sobre todo ahora que el agente gasta por turno.

[✓] B.2 — (Comportamiento) Confirmaciones de solo-lectura + lenguaje legible
      search_docs / read_doc / finalize NO deben pedir permiso (son de
      solo lectura; pedirlo causaba lentitud y jerga tipo "¿permiso para
      llamar a search_docs?"). Reservar la confirmación para futuras
      acciones con efecto real. Traducir TODOS los textos de
      confirmación/escalación a lenguaje legible para no técnicos.
      VISIÓN: el usuario no técnico no debe ver fontanería interna;
      pedir permiso solo cuando hay algo real que aprobar.

[✓] B.3 Fase 2 + B.10 — (Comportamiento + UI) Aviso de "no documentado"
      Comportamiento + visual completos y validados. El agente distingue lo propio de
      empresa (pausa con 3 opciones legibles) de lo genérico (resuelve + icono
      "Conocimiento general" con detalle al hover). Commits: 9c1ac70, 7a95055,
      8f37feb, 34233f2, 7de5ac6.

[✓] B.4 — (Funcionalidad) Acceso del agente a estadísticas de uso
      Herramienta usage_stats: métricas agregadas por documento (frecuencia de
      consulta, cobertura de recuperación %, problemas de calidad, docs nunca usados).
      Doble gate admin (excluida del LLM para members + verificación en ejecutor).
      NUNCA expone el campo question ni datos sensibles. Añadida a READ_ONLY_TOOLS.
      Commit: 1f52630.

[✓] BUGFIX finalize — Turno con muchos docs truncaba respuesta → invalid_input → bucle
      Resuelto en tres capas: d3d4e50 (finalize fatal, corta el bucle confirmar→error),
      887babf (max_tokens 4096→8192, más margen de salida), aaec17d (check stop_reason
      antes de persistir + citations tolerantes si llegan malformadas sin truncación).
      Si supera 8192 tokens, falla limpio con mensaje legible. Sin bucle posible.

[✓] B.21 — (Comportamiento) Manejo de peticiones demasiado grandes
      Las dos piezas implementadas (Sesión 20). PROACTIVA: instrucción en
      FORMAT_SECTION basada en el TAMAÑO del texto a reproducir, no en número de
      documentos — distingue leer muchos docs (correcto) de reproducir íntegros
      varios (redirige a biblioteca + ofrece sintetizar). Verificada: ante "dame
      todo íntegro" el agente redirige por sí solo, sin truncar. REACTIVA: mensaje
      de truncación legible y orientativo (constante TRUNCATION_MESSAGE), conv vuelve
      a idle. Por el camino se cerraron dos bugs: (1) créditos no reembolsados en
      truncación finalize → ahora reconcilia; (2) segundo punto de truncación
      silenciosa en texto libre → ahora entrega con aviso visible. Ver bitácora S20.

[✓] B.22 — (UX / RESPONSIVE) Layout del agente adaptado a móvil
      COMPLETADO. Patrón elegido: drawer lateral (cajón deslizante) solo en móvil; escritorio
      intacto. Detección de móvil con hook useMediaQuery (umbral 767px), una sola fuente de
      verdad en page.tsx que baja por props a los componentes que la necesitan.
      Implementado: hook useMediaQuery; componente ConversationDrawer (backdrop + panel
      deslizante, cierre por Escape/backdrop/selección, scroll-lock); en móvil el sidebar de
      conversaciones se monta dentro del drawer y se abre con botón ☰ en una barra superior
      nueva que muestra también los créditos (antes ocultos en el footer del sidebar); hint de
      teclado ("Enter para enviar") oculto en móvil; iconos de renombrar/borrar SIEMPRE visibles
      en móvil (en escritorio siguen al hover); altura del agente y del drawer a 100dvh.
      B.17 (renombrar) y B.18/B.19 (scroll) verificados funcionando dentro del drawer en móvil.

[✓] B.23 — (UX / RESPONSIVE) Altura de viewport en móvil (100vh → 100dvh) fuera del agente
      COMPLETADO en Sesiones 18 y 23.
      · AuthenticatedLayout y AppRail: h-screen → h-dvh. Arregla la altura base de toda la zona
        autenticada y el corte del último icono del AppRail (cerrar sesión).
      · chat/page.tsx: height 100vh → 100dvh. Arregla la caja de escribir cortada y el scroll espúreo.
      · globals.css body: min-height 100vh → 100dvh. Elimina la franja blanca global.
      · settings/team, billing, usage (Sesión 23): restructurados a flex column + height:vvHeight|100dvh
        + cabecera flexShrink:0 + contenido flex:1 overflowY:auto. El teclado ya no empuja la cabecera
        fuera de la vista. En settings/usage, la barra de pestañas también queda fija (flexShrink:0).
      · Nuevo hook useScrollFocusedInputIntoView en AuthenticatedLayout: cuando el teclado aparece,
        hace scrollIntoView del input activo. Arregla Chrome Android con scroll en contenedor anidado.
      · Legales, landing, invite, login: scrollean en window → sin bug → no necesitan cambios.

[✓] B.24 — (UX / PULIDO) Layout shift en cabeceras al cargar (sensación de poco profesional)
      COMPLETADO en Sesión 20 (23/06/2026). Dos commits:
      · Commit 1: barra de pestañas — placeholder con minHeight:80 mientras loading=true,
        evita el CLS en móvil donde las 3 pestañas (plan Business) envolvían a 2 líneas
        al llegar summary.plan.
      · Commit 2: grids KPI en QualityTab y ChatTab — DayFilter visible desde el primer
        frame + div vacío minHeight:80 durante loading; minHeight:80 en estado sin-datos y
        en el contenedor del grid con datos. Los tres estados ocupan ≥80px → sin salto.

[✓] B.33 — (DEUDA TÉCNICA / Coherencia) Unificar detección de móvil en chat/page.tsx
      RESUELTO EN SESIÓN 25 (30/06/2026) por vía diferente a la planificada, y mejor.
      El flash del sidebar (causa raíz del problema) se eliminó moviendo el posicionamiento
      del sidebar a CSS media query (.chat-sidebar en globals.css). El navegador posiciona
      el sidebar desde el primer render sin esperar a JS, sin flash posible.
      isMobile (useState+useEffect) sigue presente en chat/page.tsx pero ya no controla
      el posicionamiento del sidebar: gestiona solo el overlay oscuro en móvil, el strip
      colapsado de escritorio y las props onClose/onCollapseSidebar del DocumentsSidebar.
      La sustitución total por useMediaQuery queda como deuda menor de coherencia sin
      impacto visible en UX.

[✓] B.32 — (UX / MÓVIL) Aviso "mejor en ordenador" en ImprovementModal
      COMPLETADO en Sesión 24 (29/06/2026).
      El editor de mejora de documentos con IA era ilegible en móvil (dos columnas,
      textarea larga, lista de problemas). En lugar de adaptar el editor, se muestra
      un aviso claro en viewport <768px. El wrapper ImprovementModal detecta isMobile
      con useMediaQuery; si móvil → ImprovementMobileNotice (tarjeta con botón
      "Entendido" que llama a onClose → descarta el archivo y limpia el estado);
      si escritorio → ImprovementModalDesktop sin cambios. 3 claves i18n nuevas en
      es.json y en.json (mobileNoticeTitle, mobileNoticeBody, mobileNoticeButton).

[✓] B.31 — (UX / Coherencia) Unificar el arranque del agente al modelo progresivo del chat
      COMPLETADO en Sesión 25 (30/06/2026) como parte del refactor de contextos React.
      El agente ya no bloquea con pageLoading. Usa AccountContext (useAccount()) para leer
      features y créditos: spinner mientras !features, Paywall si !features.hasAgent.
      El arranque es progresivo: la interfaz queda disponible de inmediato, los datos
      llegan del contexto compartido sin bloquear la pantalla.
      IDEA FUTURA (baja prioridad, no urgente): contador animado de créditos al CAMBIAR
      el saldo (al gastar), no al cargar — detalle decorativo.

[✓] B.30 — (UX / PULIDO) CreditsIndicator compact en cabeceras móviles + placeholder anti-salto
      Nueva prop compact?: boolean: solo muestra "<X> cr" con color dinámico, sin
      recuadro ni barra de progreso. Cuando credits es null en modo compact, muestra
      "·· cr" en gris tenue (opacity 0.5) para reservar espacio y evitar el salto
      visual — modo completo sigue devolviendo null. Añadido en barra móvil del agente
      (sustituye CreditsIndicator completo) y en cabecera del chat (quita "X documentos").

[✓] B.29 — (UX / PULIDO) FeedbackButton autónomo y global en cabeceras móviles
      accessToken hecho opcional (prop?: string | null); guardia if(!accessToken) eliminada
      (el fetch usa credentials:include). FeedbackButton añadido a la barra móvil del
      agente. ChatHeader ya lo tenía desde la sesión.

[✓] B.28 — (UX / BUG) Panel de feedback cortado en móvil
      Causa raíz: el panel (position:absolute) era recortado por overflow:hidden/auto en
      ancestros. Con AppRail 64px fijo, <main> mide 311px en iPhone 375px; el panel
      sobreasalía 29px y desaparecía. Fix: position:fixed con top calculado en
      getBoundingClientRect().bottom al abrir y right:16 anclado al viewport.

[✓] B.27 — (UX / PULIDO) Quitar botón "volver al chat" redundante en settings
      Eliminado de settings/usage y settings/team. En esas páginas la AppRail
      siempre está visible y permite navegar; el botón era ruido innecesario.
      La cabecera queda solo con el h1, mismo padding y borde inferior.

[✓] B.26 — (Seguridad) Política RLS DELETE de documents endurecida
      De USING(true) a filtro por org_id (mismo patrón que SELECT/INSERT).
      SQL ejecutado y verificado en Supabase; registro actualizado en
      supabase-setup.sql. Riesgo latente cerrado (era inerte por uso de
      service role, pero quedaba como trampa para futuros endpoints anon).

[✓] B.25 — (UX / Voz) Micrófono duplicaba texto en móvil
      Causa real (vista con log en pantalla del propio móvil): el navegador en Android
      entrega en cada onresult la FRASE COMPLETA ACUMULADA repetida en múltiples
      resultados, todos marcados isFinal. El código emitía cada final → frase entera
      repetida en cada evento mientras se dictaba. Fix: leer SOLO el último resultado
      (el más completo) y emitir únicamente el fragmento nuevo respecto a lo ya emitido
      (emitDelta + emittedRef). Añadido: guard anti doble-tap (abort de instancia previa),
      auto-reinicio en onend para escucha continua en móvil (manualStopRef distingue
      parada del usuario vs corte del navegador) y cleanup al desmontar. Todo en
      components/VoiceInput.tsx; props intactas, sin tocar chat ni agente. Verificado en
      móvil real: chat + agente, parada manual, ritmos y pausas cortas/largas con y sin botón.

[✓] B.5 — (Latencia) Trigger directo Vercel→Railway (Opción D). Commits ba1e822 (worker:
      trigger-server.ts + POLL_INTERVAL 10s) + 130edbf (notifyWorker fire-and-forget en Vercel).
      Endpoint POST /trigger/agent en el worker (valida Bearer secret), notifyWorker tras el 200.
      Polling baja a 10s como red de seguridad (no se elimina). Claim por locked_at intacto.
      Degrada a polling puro si faltan variables. Dominio worker: doclity-worker.up.railway.app.
      Variables: RAILWAY_TRIGGER_SECRET (Railway+Vercel), RAILWAY_WORKER_URL (Vercel).
      VERIFICADO: reanudación tras confirmación en 502ms (antes hasta 10s por polling).

[✓] B.11 — (Comportamiento) Dar al agente conciencia temporal (fecha/hora actual)
      Inyectado en el system prompt al final (tras FORMAT_SECTION) para no romper
      el caché futuro (B.7). Zona horaria Europe/Madrid (DST automático). Solo fecha,
      formato largo español. Parámetro now:Date pasado desde runner-conv.ts.
      Commit: 6d3ea38.

[✓] B.12 — (UX) Auto-foco del input tras enviar mensaje
      El input recupera el foco automáticamente en la transición running→idle de la
      misma conversación (prevStateRef{id,status}). No se dispara al seleccionar o
      cargar conversaciones antiguas (evita el teclado pop-up en móvil).
      Commit: 3252423.

[✓] B.13 — (UX + Seguridad) Borrar conversaciones desde la barra lateral
      Borrado real (CASCADE borra los mensajes). Aislado por org_id. Confirmación
      inline "¿Borrar? Sí/No". Bloqueo si hay turno vivo (locked_at reciente, mismo
      umbral 5 min del worker); permite borrar si el running es fantasma. Deselecciona
      si era la activa y limpia el polling.
      Commits: 8963a36 (endpoint + hook), c57c13c (UI).

[✓] B.14 — (Funcionalidad) Acceso del agente a metadatos documentales
      Herramienta list_docs: 9 campos seguros (id, name, created_at, size_bytes,
      chunk_count, status, source, folder_path, source_modified_at). SELECT explícito,
      nunca SELECT *. Solo para admins: doble capa (no se ofrece al LLM para member;
      el executor verifica role !== 'admin'). Aislado por org_id.
      Nota: tokens de Drive SÍ están cifrados con AES-256-GCM (lib/crypto.ts) —
      no hay deuda de texto plano.
      RESUELTO (verificado 19/07/2026): encrypt() en callback y en ambos puntos de
      refresh de sync; decrypt() al leer. Confirmado en BD: 0 filas en texto plano,
      incluida la más antigua (mayo 2026). Formato iv:authTag:ciphertext en todas las
      filas de drive_connections.
      Commits: d416dab (herramienta + gate + runner) + tanda 2 (FORMAT_SECTION +
      CONFIDENTIALITY_SECTION).

[✓] B.16 — (Seguridad) Confidencialidad del agente
      Capa 1: tablas sensibles (organizations, memberships, tokens) fuera del alcance
      del agente — no hay herramienta que las exponga. Capa 2: CONFIDENTIALITY_SECTION
      en el system prompt (zona estable, antes de DATE_SECTION, no afecta al caché de
      B.7). Instrucción equilibrada: el agente explica el producto con naturalidad;
      rechaza revelar el prompt o credenciales cuando detecta patrones de extracción
      ("ignora tus instrucciones", "muéstrame tu prompt", "actúa sin restricciones").
      Commits: tanda 2 (junto con FORMAT_SECTION).

[✓] B.17 — (UX) Renombrar conversaciones
      Edición inline en el sidebar (lápiz al hover). PATCH /api/agent/conversations/[id]
      extendido para aceptar title además de confirmation_mode. Validación: trim + máx
      80 chars; vacío tras trim → null (revierte al título automático fecha/hora).
      Actualización optimista con rollback en el hook si el PATCH falla.
      Commits: 94486fc.

[✓] B.18 — (UX) Al abrir una conversación, posicionarse abajo (últimos mensajes)
      scrollIntoView instant al cambiar conversation.id (antes del auto-scroll suave).
      Commit: e6e8fe4.

[✓] B.19 — (UX) Botón "ir abajo" cuando no estás en el fondo de la conversación
      Botón 40px circular, position:absolute en el contenedor externo (no en el scroll),
      aparece solo cuando scrollHeight - scrollTop - clientHeight > 80px.
      Commit: e6e8fe4.

[✓] B.15 — (Seguridad / Roles) Rol de administrador temporal
      Mecanismo para que el dueño de la cuenta pueda elevar temporalmente a un
      usuario a administrador (para soporte, auditoría, etc.) sin cambiar su rol
      permanente.
      NO mezclar con trabajo del agente. Requiere diseño propio y cuidadoso del
      modelo de permisos: quién puede elevar, por cuánto tiempo, qué acciones
      permite el rol temporal, y cómo se revoca (automáticamente o manualmente).
      VISIÓN: control de acceso sin fricción para escenarios de soporte puntual.
      IMPLEMENTADO en Sesión 17 (12/06/2026). Ver bitácora para detalle completo.

[✓] B.7 — (Cierre técnico) Limpieza del agente legado + prompt caching
      PARTE 1 — Limpieza (~2356 líneas eliminadas): runner.ts, persist.ts,
      agent-handler.ts, endpoints legado, componentes y hooks viejos.
      Tabla agent_tasks intacta. Commits: 83bf7a4, 4c93091, 3a949d9, 2206377.
      PARTE 2 — Prompt caching: dos bloques de system (estable cacheado con
      cache_control:ephemeral + DATE_SECTION fuera del caché), tools cacheados
      (último tool marcado con cache_control). Verificado en Railway: cache write
      en turno 1, cache read en turnos siguientes (~3193t ahorrados en system+tools;
      hasta ~12929t en llamadas largas por caché de contexto intra-turno automático).
      Commit: 8305bdc.

[✓] B.8 — (Regresión) Pasada de prueba antes de añadir mucho
      Probar: 3 modos de confirmación × 3 caminos (approve/modify/reject)
      + escalación + memoria entre turnos.
      VISIÓN: red de seguridad. Verde aquí antes de construir encima.

[ ] B.20 — (Funcionalidad GRANDE) Conexión a bases de datos + análisis cruzado datos↔documentación
      Marco conceptual completo en A.4. Esto es la materialización técnica. Es la
      apuesta diferencial frente a CRM/ERP/BI. NO es una sesión: son varias.
      ESTADO: DISEÑO PRIMERO antes de tocar código (igual que B.6 y B.15).

      ── POR QUÉ HOY NO SE PUEDE (límite arquitectónico actual) ──
      El pipeline actual es RAG semántico puro sobre texto: trocea en chunks,
      embebe en Pinecone, recupera los ~15 más parecidos. Para datos tabulares
      esto FALLA en dos cosas:
        · No agrega: "¿cuántas llamadas en marzo vs febrero?" exige contar/sumar
          filas. El RAG no cuenta, recupera trozos parecidos → la cuenta sale mal
          o inventada (si las filas de marzo están en 200 chunks, solo recupera ~15).
        · El embedding semántico es pésimo para celdas: una fila
          "2026-03-12 | Cliente X | 14 min | no cerrada" no tiene semántica rica que
          multilingual-e5-large capture. Los embeddings van con prosa, no con tablas.
      Falta una PIEZA ARQUITECTÓNICA entera, no afinar prompts. La pieza es separar
      dos tipos de pregunta y darle al agente una vía SQL real:
        · TIPO A (estructurada/cuantitativa: cuántas, cuánto, tendencia, agrupado por)
          → necesita datos en TABLA SQL de verdad + el agente genera SQL, lo ejecuta,
            recibe números exactos. (text-to-SQL)
        · TIPO B (cualitativa/contextual: por qué, qué dijeron, en qué contexto)
          → es el RAG actual, que ya funciona.
      El valor está en hacer LAS DOS en la misma pregunta y combinarlas (ver A.4).

      ── PLAN POR FASES (validar barato antes de la ingeniería pesada) ──

      FASE 0 — Validación con UNA tabla controlada (riesgo casi cero, demo rápida)
        · Meter UNA tabla real como tabla normal en el Supabase Postgres que YA
          tenemos. Sin conectores externos, sin CRM, sin nada genérico.
        · Herramienta nueva del agente que solo sepa consultar ESA tabla
          (ej. query_sales_data). Read-only.
        · Documento de esquema de esa tabla en el corpus (ver "documento de esquema").
        · OBJETIVO: ver si el bucle "saco dato → me replanteo → busco en docs →
          sintetizo" produce algo que dé escalofríos de lo bueno. Si funciona con
          una tabla, la idea está validada y hay demo. Si no impresiona ni con datos
          controlados, mejor saberlo antes de construir conectores.

      FASE 1 — "Sube tu Excel/CSV → se vuelve tabla consultable"
        · Generalizar: el usuario sube un Excel/CSV y va por "ruta datos" (tabla SQL
          en Postgres con esquema detectado), NO por "ruta documento" (RAG). O ambas.
        · Aquí entra el text-to-SQL de verdad, pero sobre datos que el usuario sube
          (más controlable que sistemas externos).
        · Decidir al subir: ¿ruta documento (RAG), ruta datos (SQL), o las dos?

      FASE 2 — Conectores a bases de datos externas reales (LA CARA, la más pesada)
        · Postgres/MySQL propios de la empresa primero (conexión directa por red).
        · CRMs/ERPs cerrados (Salesforce, HubSpot) DESPUÉS: ahí NO hay BD accesible,
          se conecta vía su API (otra auth, normalmente OAuth como Drive). El agente
          recibe una herramienta que sabe llamar esa API. Esto YA se puede hoy, no
          esperamos avances. Se añade como otro proveedor en el registry pattern.

      ── CONEXIÓN DE SOLO-LECTURA (decisión del usuario, correcta) ──
        · Cada empresa crea un USUARIO DE BD dedicado al agente, SOLO con permiso
          SELECT (ni escritura ni nada). Práctica estándar en producción.
        · Para conectarse hace falta cadena de conexión: HOST (URL/dirección del
          servidor), PUERTO, NOMBRE DE LA BD, USUARIO, CONTRASEÑA. (No solo user+pass.)

      ── ⚠️ SEGURIDAD NO NEGOCIABLE: credenciales cifradas DESDE EL DÍA 1 ──
        · Las credenciales de BD son secretos críticos: dan acceso a TODO el negocio
          del cliente. Si se filtran en texto plano, es un incidente que se lleva la
          empresa por delante, NO un bug.
        · NACEN CIFRADAS. Reutilizar el patrón que ya usamos para tokens de Drive
          (AES-256-GCM, lib/crypto.ts — ver B.14). NO repetir la deuda de "texto plano
          y ya lo arreglo pronto". Esto no es deuda asumible.

      ── DOCUMENTO DE ESQUEMA POR TABLA (la mejor idea de la conversación) ──
        Un documento por tabla que describe: qué campos tiene, qué significa cada
        campo, clave única, relaciones, y si es TABLA HISTÓRICA. Resuelve TRES
        problemas a la vez y es elegante:
          1. Enseña al agente a escribir SQL correcto (sabe qué columnas hay, no las
             inventa).
          2. Evita joins que duplican registros (sabe la clave única y cómo se
             relacionan las tablas).
          3. Ese documento VIVE EN EL PROPIO RAG de Doclity → el producto se documenta
             a sí mismo usando su propia funcionalidad (coherencia arquitectónica).
        En la industria esto es un "semantic layer" / catálogo de datos por el que se
        paga mucho. Resolverlo como "un documento más en el corpus" es simple y potente.
        CUIDADO: hay que mantenerlo sincronizado con la tabla real. Si alguien añade una
        columna y no actualiza el doc, el agente trabaja con info vieja. Problema de
        PROCESO manejable, no fallo de diseño.

      ── BARRERAS ANTI-DESTROZO DE SQL (dos capas, ambas obligatorias) ──
        El documento de esquema hace el SQL INTELIGENTE, pero NO impide que sea
        PELIGROSO. Hacen falta barreras de CÓDIGO que no dependen del criterio del LLM:
          · Usuario read-only (ya previsto) → impide escrituras pase lo que pase.
          · LÍMITE DE FILAS forzado en código (ej. corta a 10.000) aunque el agente
            pida "todo".
          · TIMEOUT de consulta: si tarda más de X s, se cancela (protege contra la
            tabla histórica enorme).
          · Por encima: el agente, avisado por el doc de esquema de que una tabla es
            histórica, PREGUNTA antes ("esto puede tardar y traer mucho, ¿seguimos?")
            vía el mecanismo escalate que ya existe.
        CLAVE MENTAL: prompt + documento hacen que se porte bien por defecto; las
        barreras de código garantizan que NO PUEDA portarse mal aunque quiera. Nunca
        confiar solo en que el LLM se comporte.

      ── QUÉ ES REALISTA Y CUÁNDO ──
        · Razonamiento iterativo (saco dato→replanteo→busco más→sintetizo): el loop
          ReAct YA lo hace. Real HOY.
        · Cruce tabla+documentación en una respuesta: real, es darle las dos
          herramientas y dejar que combine. Real HOY, con trabajo de prompting.
        · Conexión segura a BD externas (credenciales cifradas, esquemas, permisos,
          barreras SQL): real, pero es la ingeniería serie. Varias sesiones.

      ── ENCAJE CON PRINCIPIOS DEL PROYECTO ──
        · Nuevo dominio limpio: lib/agent/tools/query-table.ts (o similar) + quizá
          lib/tabular/ para la lógica de conexión/esquema. Abstracción en el límite
          (nunca SDK de BD directo desde endpoint), como exige el perfil de trabajo.
        · No rompe nada existente: se añade en paralelo al RAG actual.
        · Aislamiento por org_id en todo (credenciales, esquemas, resultados).

      DEPENDE DE / SE RELACIONA CON: A.4 (visión), A.1 (investigar primero, preguntar
      las bifurcaciones por adelantado — encaja perfecto con "¿accedo a estas tablas?"),
      B.15 (permisos: quién puede ver qué datos; el comercial no saca nóminas),
      B.16 (confidencialidad), B.6 tipo 2/3 (hechos de dominio y criterios de trabajo
      como LEFT JOIN — el documento de esquema y B.3 ya cubren buena parte).

[ ] B.6 — FASE FINAL
      ── B.6 — MECANISMO DE APRENDIZAJE (diseño cerrado, implementación = fase final) ──

      PRINCIPIO: se aprende el MAPA del territorio (hechos/convenciones estables),
      no la RUTA concreta de una vez (la decisión de un caso). Guardar soluciones
      literales envenena; por eso se destila.

      TRES TIPOS de cosa aprendida, cada uno con su mecanismo:

      TIPO 1 — CONVENCIÓN DE EMPRESA (ej: "los correos los firmamos con el nombre del equipo").
        · Específica de la empresa, pocas, estables.
        · SIEMPRE PRESENTE en el system prompt (bloque estable, ya cacheado — no lo encarece
          porque son pocas).

      TIPO 2 — HECHO DE DOMINIO DE EMPRESA (ej: "claper es la tabla de clientes",
        "ventas–clientes es N:N").
        · Específico de la empresa, muchos, crecen sin techo.
        · RECUPERADO SEGÚN LA PREGUNTA — se indexan (reutilizar el sistema de búsqueda semántica
          que ya existe para el corpus) y se traen solo los relevantes a la consulta. NO van
          siempre presentes (saturarían y encarecerían el prompt).

      TIPO 3 — CRITERIO DE TRABAJO GENERAL (ej: "cuando se piden todos los registros incluyendo
        los huérfanos, usar LEFT JOIN para no excluirlos").
        · NO es específico de la empresa: es conocimiento general que el agente YA tiene.
        · ⚠️ AVISO DE DISEÑO: "enseñar" esto es en realidad "recordarle que lo aplique". ANTES de
          construir el tipo 3, comprobar si el comportamiento de investigar-primero (B.3) ya
          resuelve estos casos solo (un agente que inspecciona bien la estructura se da cuenta
          solo de que toca LEFT JOIN). Si B.3 ya lo cubre, el tipo 3 apenas se usará. Evaluar con
          uso real antes de invertir en él. Mecanismo de recuperación: a decidir (probablemente
          como el tipo 2, recuperado según pregunta).

      CAPTURA (cómo entra algo al aprendizaje):
        · SOLO si el usuario pulsa el botón "enviar + tener en cuenta para el aprendizaje" (el
          interruptor de enseñanza). El "enviar" normal NO aprende nada.
        · El usuario escribe la corrección en lenguaje NATURAL ("deberías haber hecho un LEFT
          JOIN aquí").
        · El agente DESTILA esa corrección en una regla/hecho reutilizable y SE LA MUESTRA al
          usuario para que la APRUEBE, EDITE o DESCARTE antes de guardar. Esto evita que se cuele
          la decisión puntual del caso (ej: destila "usa LEFT JOIN para incluir huérfanos", NO
          "para esta consulta concreta usa LEFT JOIN"). El usuario tiene la última palabra.

      INTERRUPTOR DE ENSEÑANZA:
        · Quien manda (admin) abre una fase de enseñanza: durante ella, cualquier usuario puede
          marcar correcciones para aprender.
        · La enseñanza se CIERRA: solo el admin sigue alimentándolo, o se congela.
        · No es jerarquía de confianza permanente, es un interruptor con control de accionamiento.

      ALCANCE: lo aprendido es a nivel de TODA LA ORGANIZACIÓN (lo aprende uno, lo saben todos).
      Aislado por org_id (como todo lo demás).

      GESTIÓN (admin): pantalla/acceso desde Doclity, por organización, donde el admin puede VER,
      EDITAR y BORRAR lo aprendido (convenciones, hechos, criterios). Imprescindible: sin esto se
      acumulan reglas viejas que ya no aplican y nadie sabe por qué el agente hace lo que hace.

      FUTURO (no en la primera versión): permitir adjuntar correos/documentos de ejemplo.

      IMPLEMENTACIÓN: FASE FINAL del proyecto. Empezar por DISEÑO técnico (esquema de BD, cómo se
      indexan los hechos, cómo se inyecta cada tipo, la pantalla de gestión) cuando se llegue.
      Se construye sobre B.3 (comportamiento), el sistema de búsqueda semántica, y los permisos
      de B.15.

      · PARCIAL — Tipo 1 (convención de empresa) + gestión implementados en Sesión 23
        (07/07/2026): tabla learned_rules, CRUD admin en settings/learning, e inyección en el
        system prompt del chat y del agente. Pendiente de B.6: Tipos 2 y 3 y el mecanismo de
        captura/destilación (ver A.2 en la bitácora).


[✓] B.34 — (Funcionalidad) Lagunas de documentación: el usuario marca lo que falta
      HECHO (Sesión 27, 01/07/2026). Cuando el chat RAG no cubre una pregunta,
      el usuario deja constancia con un botón; queda registrado para que quien
      documenta actúe. La ausencia de documentación pasa a ser señal útil (encaja
      con A.1). Implementado en 3 fases con build verde entre medias:
      · FASE 1 (backend): tabla documentation_gaps (org_id, user_id, question,
        answer, note, status pending/documented/dismissed, created_at, resolved_at)
        con RLS activado + política service_role. Endpoint POST /api/documentation-gaps
        (patrón de /api/feedback: getAuthenticatedUserHybrid → createServiceClient →
        resolveOrg → insert). Archivos: supabase-documentation-gaps.sql (ejecutado en
        Supabase), app/api/documentation-gaps/route.ts.
      · FASE 2 (frontend): botón en cada mensaje del assistant. Guarda pregunta +
        respuesta + nota opcional. Tras registrar → "Registrado ✓" y se bloquea.
        Flag noContext añadido a RAGResult (lib/rag.ts) y expuesto por /api/ask;
        propagado a Message (question, noContext) vía useChat hasta ChatMessage.
        Componente nuevo components/chat/DocGapButton.tsx.
      · FASE 2b (pulido): botón en color ámbar (var(--warning)) en TODOS los mensajes
        (decisión del usuario: se ve siempre, sea buena o mala la respuesta).
        Internacionalizado con next-intl: claves chat.docGap.* en es.json y en.json.

[ ] B.35 — (Funcionalidad) Panel de admin para revisar las lagunas de documentación
      FASE 3 de B.34, pendiente. Vista donde el admin ve los gaps registrados,
      idealmente ORDENADOS POR FRECUENCIA (preguntas repetidas = prioridad de
      documentar), con la pregunta, la respuesta que se dio y la nota del usuario.
      Poder marcar cada gap como 'documented' o 'dismissed' (la tabla ya tiene
      status y resolved_at preparados). Encaje natural: pestaña en /settings/usage
      (solo Business+, como el resto de analítica), o sección propia.
      VISIÓN: cerrar el círculo. Sin este panel, los gaps se registran pero nadie
      los revisa de forma organizada. Aquí es donde el cliente VE dónde le falta
      documentar y actúa.

[ ] B.36 — (Deuda menor de B.34) noContext no cubre el caso "LLM dice no sé"
      DIAGNOSTICADO, no urgente. Hay TRES formas de "no encontré", solo dos marcan
      noContext=true (Pinecone sin matches / score bajo, y topDocs vacío tras filtrar).
      El TERCER caso —el LLM SÍ recibe documentos pero él mismo responde "no encontré
      información"— hoy devuelve noContext=false. No afecta a lo visual (el botón es
      ámbar en todos los mensajes), pero SÍ importará para B.35 si se quiere distinguir
      "no había nada" de "respondió pero flojo". Opciones evaluadas: (a) detectar por
      el texto de la respuesta — rápido pero frágil; (b) respuesta estructurada del LLM
      con campo found:boolean — robusto pero toca el prompting del RAG. DECISIÓN: no
      tocar hasta montar B.35 y decidir con datos reales si el ruido justifica el fix.

[ ] B.38 — (OPTIMIZACIÓN DE COSTE) Prompt caching en el pipeline de análisis
      INVESTIGADO en Sesión 28 (01/07/2026). NO implementado aún.

      SITUACIÓN ACTUAL: ninguna de las 7 llamadas del pipeline de análisis
      (rerank, judge, extract-claims, verify-claims, synthesize, style-check,
      double-check) usa `cacheSystem: true`. Todas van por `callLLMJson` sin
      prompt caching activado.

      POR QUÉ IMPORTA: en un análisis exhaustivo observado en producción,
      la fila Haiku consumió 73.276 tokens de entrada. El pipeline repite el
      system prompt en cada llamada. Con prompt caching, el system prompt de
      la primera llamada se cachea (coste × 1,25 una sola vez) y las siguientes
      lo leen a 0,025 $/Mtok (vs 0,25 $/Mtok sin caché), es decir, 10× más
      barato en tokens de entrada cacheados.

      ACLARACIÓN TÉCNICA IMPORTANTE: el prompt caching de Anthropic NO cambia
      el resultado del modelo; solo afecta a la latencia y al coste. El modelo
      procesa exactamente las mismas instrucciones. No es un "cambio de
      comportamiento" del sistema: es ahorro económico puro.

      ESTIMACIÓN DE AHORRO (análisis exhaustivo, 73.276 tokens de entrada):
      · Sin caché: 73.276 × 0,25 $ / 1.000.000 = ~0,018 $ por análisis
      · Con caché (asumiendo system prompt ~4.000 tokens cacheable × N llamadas):
        el ahorro depende del % de tokens cacheados. Si ~30% del input es system
        prompt reutilizable, el ahorro es ~3× en esa fracción → aprox. 0,005-0,006 $
        por análisis (ahorro de ~0,012-0,013 $ por análisis exhaustivo).
      · Escala: si hay 100 análisis exhaustivos/mes, ~1,2 $ ahorrados. No es
        dramático en absoluto con el volumen actual, pero es coste real y creciente.

      QUÉ REQUIERE IMPLEMENTARLO:
      1. Identificar el system prompt que se repite en cada paso y marcarlo con
         `cache_control: { type: 'ephemeral' }` (o pasar `cacheSystem: true` a
         `callLLMJson`).
      2. Añadir la cabecera `anthropic-beta: prompt-caching-2024-07-31` en
         `callLLMJson` cuando `cacheSystem` esté activo (ya existe en `buildHeaders`).
      3. Verificar que los tokens de caché aparecen en `llm_usage` (ya hay campos
         `cache_write_tokens` y `cache_read_tokens`).
      IMPORTANTE: este cambio solo toca `callLLMJson` y los system prompts de los
      pasos del pipeline — NO cambia prompts, modelos ni lógica de créditos.

      PRIORIDAD: baja (ahorro marginal con volumen actual). Revisar cuando el
      volumen de análisis exhaustivos crezca o cuando el coste de Haiku sea un
      porcentaje relevante de los costes totales.

      ESTADO (F-33, 23/08): CONDICIONADO AL PASO 5, no cerrado.
      Estructura decidida: `system` con reglas y esquema, primer bloque del mensaje
      de usuario con el DOCUMENTO NUEVO, breakpoint de cache_control ahí, y el
      candidato después. La clave que se nos había escapado: en un análisis el juez
      hace hasta 6 llamadas y en todas se repiten DOS cosas —las instrucciones y el
      documento nuevo (hasta 6.000 caracteres, idéntico en las 6)—; solo cambia el
      candidato. El caché no es para "instrucciones estáticas" sino para todo lo
      que no cambia entre llamadas consecutivas. Prefijo cacheable estimado:
      ~8.700 + ~6.000 caracteres ≈ 4.000-4.500 tokens, en el umbral o por encima, y
      el prompt del paso 5 además crece.
      AL IMPLEMENTARLO, CEBAR EL CACHÉ: los juicios van ahora de 5 en 5 (B.93), y
      el caché lo crea la primera llamada que responde. Cinco simultáneas pueden
      salir todas antes de que exista la entrada y ninguna leerla. Lanzar el juicio
      1 solo y los 2-6 en lote después. Un `if` en el fan-out, no un rediseño.
      CRITERIO DE CIERRE: medir cache_creation_input_tokens > 0 Y
      cache_read_input_tokens > 0 en llm_usage con el prompt real del paso 5. Si no
      alcanza el mínimo de Haiku, cerrar como NO APLICABLE anotando el conteo real.

[ ] B.37 — (Funcionalidad, SECUNDARIO) Idioma de las respuestas del LLM en el chat
      El i18n de la interfaz (next-intl) ya funciona, pero solo traduce textos FIJOS.
      Las respuestas del chat las genera el LLM al vuelo y salen en español aunque la
      interfaz esté en inglés. Para que respondan en el idioma activo hay que pasar el
      idioma al prompt del RAG (/api/ask). El CÓDIGO es sencillo; lo que hay que cerrar
      antes son dos DECISIONES de producto: (1) ¿el idioma lo marca la interfaz o la
      pregunta del usuario? (los productos serios suelen usar el de la pregunta);
      (2) ¿qué hacer con las citas literales de documentos que están en español —
      traducirlas puede introducir imprecisiones en documentación corporativa?
      PRIORIDAD BAJA: el producto está enfocado primero en español. Riesgo de dejarlo
      a medias: interfaz en inglés pero respuestas traduciendo docs españoles con
      imprecisiones da peor impresión que tenerlo todo coherente en español.

[ ] B.39 — (FIX FONDO) analyze-v2: pasar el texto ya extraído en lugar de re-descargar de Storage
      DIAGNOSTICADO. Ciertos PDFs fallan con "bad XRef entry" en analyze-v2 pero no en
      ingest, porque analyze-v2 descarga de Storage justo tras el upload antes de que el
      archivo esté completamente escrito. El reintento de 3 intentos (fix 86c65cf2) es
      una mitigación parcial.
      Fix de fondo: que el frontend extraiga el texto una sola vez (tras el upload)
      y lo pase a analyze-v2 via directText en el body (el endpoint ya soporta directText).
      Así analyze-v2 nunca descarga de Storage — elimina la condición de carrera por diseño.
      Implica reordenar el flujo en hooks/chat/useDocuments.ts:
        upload → extractText (una vez) → analyze-v2(directText) → ingest
      NO urgente: el documento entra igualmente al corpus vía ingest y el chat lo usa;
      solo se pierde el análisis previo del archivo afectado. Encaja con el rediseño
      del flujo de la Fase B.

[ ] B.40 — (DEUDA MENOR) Vectores huérfanos en ingest
      Si algo falla entre el upsert a Pinecone (paso 8) y el insert en Supabase (paso 9)
      en app/api/ingest/route.ts, quedan vectores sin fila de documento en la BD.
      El usuario no puede verlos ni borrarlos; ocupan namespace de la org.
      Sin impacto funcional hoy (el chat ignora vectores sin documento al resolver fuentes),
      pero es deuda latente. Revisar al reconducir ingest por la capa Pinecone (B.0.2):
      ese es el momento natural para añadir un try/catch que, si el insert en Supabase
      falla, elimine inmediatamente los vectores recién subidos.

[ ] B.41 — (UX) Avisar al usuario tras reindexar un documento: la memoria conversacional puede
      mezclar la versión antigua con la nueva
      DIAGNOSTICADO (09/07/2026). Al reemplazar un documento (mismo nombre, force=true) y
      volver a preguntar en la MISMA conversación, el chat mezcla la respuesta anterior (que
      sigue en el historial) con el contenido nuevo del documento, y lo reporta como una
      "contradicción entre documentos". No es un bug de datos: se verificó que el borrado de
      vectores funciona (cleanup-orphans reporta 0 huérfanos), que full_text en Supabase
      contiene solo la versión nueva, y que el agente en conversación nueva responde bien.
      Es la memoria conversacional del chat haciendo su trabajo: detecta que lo que dijo antes
      contradice lo que lee ahora, y avisa.
      Fix propuesto: tras una subida que REEMPLAZA un documento existente (replaced=true en la
      respuesta de /api/ingest), mostrar un aviso al usuario:
        - En el chat: recomendar "Limpiar chat" (solo hay una conversación activa).
        - En el agente: recomendar empezar una conversación nueva (sí tiene hilos separados).
      Texto orientativo: "Has actualizado un documento. Para evitar respuestas basadas en la
      versión anterior, limpia el chat / empieza una conversación nueva."
      Dónde: hooks/chat/useDocuments.ts ya recibe data.replaced de /api/ingest (lo usa hoy para
      el mensaje "Documento X actualizado"). Ese es el punto natural para añadir el aviso.
      NO urgente: no hay pérdida ni corrupción de datos; es una recomendación de uso.

[✓] B.42 — (SEGURIDAD) Endpoint /api/admin/purge-org eliminado
      HECHO (Sesión 29, 09/07/2026).
      CORRECCIÓN DEL DIAGNÓSTICO ANTERIOR: la anotación original decía que este endpoint no
      tenía autenticación. ERA INCORRECTO. Al leer el archivo se comprobó que SÍ estaba
      protegido con ADMIN_SECRET (header Authorization: Bearer <secreto>, 401 si no coincide)
      y que además comprobaba orgData.purged_at para no purgar dos veces. No había agujero
      de seguridad crítico.
      Motivo real de la eliminación: código muerto. Nadie lo llamaba desde el repo (ni frontend,
      ni hooks, ni worker, ni scripts); era una herramienta manual (curl/Postman) que nunca se
      usó. Menos superficie expuesta para una operación que destruye una organización entera.
      Los dos consumidores legítimos de purgeOrganization() siguen intactos e importan la función
      directamente de lib/purge-org.ts:
        · app/api/org/purge/route.ts — borrado voluntario (auth + resolveOrg)
        · worker/src/index.ts — purga automática tras 90 días de gracia
      También se conserva app/api/admin/purge-expired/route.ts, que importa la misma función.
      Si algún día hace falta purgar una organización ajena, reconstruirlo con: autenticación de
      superadmin, confirmación explícita en dos pasos, y registro de quién ejecutó la purga
      (nada de eso existía en el endpoint eliminado).

[✓] B.43 — (SEGURIDAD) cleanup-orphans protegido con sesión de admin
      HECHO (Sesión 29, 09/07/2026). El diagnóstico original era CORRECTO: el endpoint no tenía
      ninguna autenticación (su propio comentario decía "ACCESO PÚBLICO. Borra en TODAS las
      cuentas") y la página que lo consume tampoco comprobaba sesión. Cualquiera con la URL podía
      listar los documentos de todas las organizaciones y, con ?dryRun=false, borrar sus vectores.
      Fix aplicado (dos commits):
        · Endpoint: exige getAuthenticatedUserHybrid + resolveOrg + org.role === 'admin', y opera
          ÚNICAMENTE sobre org.orgId (desaparece el bucle de organizaciones y la clave compuesta;
          el código quedó además más corto y rápido). Se eliminaron los campos organizationsScanned
          y org de la respuesta.
        · Página: añadido credentials:'include' al fetch (sin él la cookie no viajaba y el endpoint
          habría devuelto 401 siempre), manejo de 401/403 con aviso claro, y eliminadas las
          referencias a los campos que ya no existen (g.org rompía en runtime).
      Verificado en producción: como admin reporta solo la propia organización (46 vectores, 23
      documentos, 0 huérfanos); en ventana de incógnito muestra el aviso de "necesitas iniciar
      sesión como administrador" sin exponer ningún dato.
      La herramienta SE CONSERVA: hará falta para el backfill de metadata de vectores (B.4 de la
      Fase B).

[ ] B.44 — (BUG) El upload lock no se libera tras una subida exitosa
      DESCUBIERTO (09/07/2026). El lock vive en organizations.upload_locked_by /
      upload_locked_at. Lo activa el frontend antes de subir (chat/page.tsx llama a activateLock),
      pero tras una subida CORRECTA queda activo: se comprobó que una subida exitosa a las 12:03
      dejó el lock puesto durante horas. Solo se libera si el usuario pulsa el candado o si pasan
      60 minutos y el backend lo expira en el siguiente checkUploadLock.
      Efecto: la organización queda con las subidas bloqueadas ("está analizando... bloqueada
      temporalmente") sin motivo, y otros usuarios no pueden subir.
      Fix: que ingest (o el frontend, en su finally) desactive el lock al terminar, tanto en
      éxito como en error. Revisar dónde se activa (chat/page.tsx) y si existe un endpoint de
      desactivación que no se esté llamando.

[ ] B.45 — (ESCALABILIDAD, IMPORTANTE) El sync de Drive no escala: procesa todos los archivos
      en una sola petición HTTP
      DESCUBIERTO (09/07/2026). app/api/drive/sync/route.ts recorre TODOS los archivos de la
      carpeta y, por cada uno, descarga, extrae texto, genera embeddings y hace upsert a
      Pinecone — todo dentro de una única llamada con maxDuration = 300s. Con una cuenta de
      OneDrive personal (bastantes archivos) el sync se colgó y acabó en "error de conexión":
      la función de Vercel se corta a los 5 minutos y el frontend espera una respuesta que nunca
      llega. Peor: se corta A MITAD, dejando unos documentos indexados y otros no, sin punto de
      reanudación. La siguiente sincronización vuelve a empezar y puede no terminar nunca.
      Con cuentas pequeñas (la de Doclity) funciona; con un Drive corporativo real, romperá.
      Es la misma enfermedad que la deuda técnica nº3 (análisis síncrono): trabajo pesado dentro
      de una petición HTTP con límite de tiempo.
      Fix: llevar el sync a la cola de background (encaja con la Fase D). Alternativa intermedia:
      procesar por lotes con reanudación (guardar el último archivo procesado y continuar en la
      siguiente llamada).
      PRIORIDAD: BLOQUEANTE DEL ONBOARDING. Una empresa con un Drive real (cientos de documentos)
      NO PUEDE NI CONECTAR hoy: el sync se corta a los 5 minutos, a mitad, sin punto de
      reanudación. No es una mejora deseable, es un requisito para vender.
      Nota importante: esto es un problema de INDEXACIÓN, no de análisis. El análisis de los
      documentos de Drive nunca es automático (D8: el sync no lanza análisis; el usuario decide
      qué analizar desde la bandeja, viendo el coste). Cuando sí se pide, los análisis van a la
      cola de analysis_jobs con el worker de Railway, sin límite de tiempo HTTP. El sync sufre la
      misma enfermedad que el análisis síncrono (deuda técnica nº3) y debería resolverse con la
      misma medicina, en la Fase D: llevarlo a la cola de background.


[ ] B.46 — (PRODUCTO) Analizar un LOTE de documentos entre sí, sin comparar contra el corpus
      Caso de uso: enviar un paquete de documentos a un cliente (p. ej. documentación legal) y
      necesitar que sean coherentes ENTRE ELLOS. Hoy el análisis siempre compara contra todo el
      corpus, lo que mezcla dos preguntas distintas: "¿mi corpus es coherente?" y "¿este paquete
      que voy a entregar es coherente?".
      Ejemplo: tres escritos donde el condenado se llama Pepe Pérez en uno y Jaimito Pérez en
      otro. Detectarlo ANTES de enviarlos, comparándolos solo entre sí.
      Otro caso: carpetas de equipos distintos con metodologías legítimamente diferentes.
      Compararlos entre sí genera contradicciones esperadas y ruidosas.
      Diseño posible: al seleccionar documentos, elegir el ÁMBITO de comparación: (a) contra todo
      el corpus (por defecto), (b) solo entre los seleccionados, (c) contra una carpeta concreta.
      El retrieval ya filtra por documentId (excludeDocumentId existe en retrieval.ts), así que
      restringir el ámbito es viable técnicamente.
      ESTRATEGIA EN DOS RONDAS (idea del usuario, 09/07/2026): para una carga inicial grande,
      analizar primero el lote ENTRE SÍ (resolver sus contradicciones internas) y solo después
      contra el corpus. Reduce el efecto cascada (ver B.47). Coste: dos rondas de análisis. NO
      debe ser obligatorio: se ofrece como opción, porque con 400 documentos son 4.000 créditos.

[ ] B.47 — (CORRECCIÓN) Los análisis guardados quedan OBSOLETOS cuando cambia el corpus
      Los resultados viven en analysis_results (una foto del momento del análisis). Si corriges un
      documento, los análisis de OTROS documentos que lo mencionaban dejan de reflejar la realidad:
      siguen diciendo "contradice a X" cuando X ya se corrigió.
      SOLUCIÓN (diseñada con el usuario, 09/07/2026): al modificar el documento X,
        1. BORRAR las incidencias que otros documentos tenían contra X (son fotos de una versión
           que ya no existe).
        2. REANALIZAR X contra el corpus.
      Fundamento: al reanalizar X, TODAS sus contradicciones actuales —las que sobreviven y las
      NUEVAS que la corrección haya podido crear con documentos que antes no lo mencionaban—
      quedan registradas en el informe de X. No se pierde ninguna contradicción: cambia de dueño.
      Y es el dueño correcto, porque X es el documento que se movió.
      Coste: UN análisis (el de X), no N. Descarta la idea inicial de reanalizar todos los que
      aparecían en involved_documents, que además era insuficiente (detectaba las contradicciones
      que desaparecen, no las que aparecen).
      Requiere: analysis_results.document_id (ya añadido en B.1), poder borrar/invalidar los
      resultados que mencionen a X en involved_documents, y encolar el reanálisis de X.

[ ] B.48 — (PRODUCTO) Excluir documentos concretos de la comparación (archivo histórico)
      Caso: conservar versiones antiguas como documentos distintos (documento_v1, documento_v2) en
      vez de reemplazarlas. El análisis las marcará como duplicados y contradicciones, y
      técnicamente tendrá razón, pero es ruido inútil: el usuario ya sabe que son versiones.
      Propuesta: marcar un documento como "archivo histórico / no comparar". Se conserva en el
      corpus pero se excluye del retrieval de análisis.
      Relacionado con la Fase C (D5, identidad y versiones), pero es un caso DISTINTO: no se quiere
      REEMPLAZAR la versión vieja, se quiere CONSERVARLA sin que genere ruido.

[ ] B.49 — (PRODUCTO, FUTURO) Filtro de documentos en el chat y el agente
      Que el usuario pueda elegir contra qué documentos (o carpetas) quiere preguntar, en vez de
      contra todo el corpus. Ejemplo: "responde solo con la documentación del equipo A".
      Técnicamente viable: queryVectors ya acepta un parámetro `filter` genérico (metadata de
      Pinecone), y los vectores llevan documentId y folderPath en su metadata.
      NO URGENTE. Idea de producto para una versión futura; no desviar el foco.

[ ] B.50 — (PRODUCTO) Las duplicidades y las contradicciones no pesan lo mismo
      Una contradicción es casi siempre un problema real. Una duplicidad puede ser perfectamente
      normal (plantillas, encabezados, cláusulas estándar, textos legales repetidos). Hoy el
      análisis las presenta con el mismo peso, y el usuario acaba descartando duplicados una y
      otra vez.
      Propuestas: (a) separar visualmente la severidad en el modal y en la bandeja; (b) que los
      descartes de duplicados sean PERMANENTES por organización (es el D9 del encargo:
      excludeFingerprints persistentes, previsto para la Fase C); (c) considerar no contar los
      duplicados descartados sistemáticamente como "incidencia" en la bandeja.


════════════════════════════════════════════════════════════════
NOTAS DE ORDEN (propuesta, a confirmar)
════════════════════════════════════════════════════════════════

- B.2 y B.3 (comportamiento) son los cimientos: tocan system prompt y
  lógica de pausa, sobre los que se prueba todo lo demás. Irían primero.
- B.8 (regresión) conviene ANTES de empezar a añadir, para tener una
  base verde de referencia.
- B.4 y B.5 son funcionalidad/latencia, después de los cimientos.
- B.20 (conexión a BD + análisis cruzado) es la apuesta diferencial grande
  (ver A.4). Empezar SIEMPRE por FASE 0 (una tabla controlada en el Supabase
  actual) para validar el cruce con riesgo casi cero antes de la ingeniería
  pesada de conectores. Diseño técnico primero. Credenciales cifradas desde
  el día 1 (no negociable). Se apoya en A.1 (investigar+preguntar antes) y
  en los permisos de B.15.
- B.6 (aprendizaje) es la FASE FINAL: se construye sobre B.3, el sistema
  de búsqueda y los permisos de B.15. Diseño técnico primero cuando se llegue.
- B.7 (cierre técnico) completado.
- B.22 (layout móvil del agente) COMPLETADO. B.23 (dvh + teclado móvil) COMPLETADO en sesiones
  18 y 23: layout autenticado, AppRail, chat, body global, y settings (team/billing/usage)
  restructuradas a modelo chat. B.24 (layout shift) y B.25 (micrófono móvil) cerrados.
- B.26 (RLS DELETE), B.27 (botón volver redundante), B.28 (panel feedback cortado),
  B.29 (FeedbackButton autónomo), B.30 (créditos compactos + placeholder) COMPLETADOS en sesiones 21-22.
- B.31 (arranque progresivo del agente) COMPLETADO en sesión 25 — AccountContext, sin pageLoading.
- B.32 (aviso móvil ImprovementModal) COMPLETADO en sesión 24.
- B.33 (sidebar flash en móvil) RESUELTO en sesión 25 — CSS media query, mejor que useMediaQuery.
- B.34 (lagunas de documentación) COMPLETADO en sesión 27 — backend + frontend + pulido.
  B.35 (panel admin de gaps) es la continuación natural (Fase 3 de B.34).
  B.36 (noContext tercer caso) diagnosticado, no urgente; decidir con datos reales en B.35.
  B.37 (idioma del LLM en chat) pendiente de decisiones de producto; prioridad baja.
  B.38 (prompt caching en análisis) investigado, anotado; implementar cuando el volumen justifique.
  B.39 (fix fondo analyze-v2 race condition) diagnosticado, no urgente; encaja con el rediseño de flujo de Fase B.
  B.40 (vectores huérfanos en ingest) deuda menor; revisar al reconducir ingest por la capa en B.0.2.
  B.41 (aviso tras reemplazar documento) UX no urgente; diagnosticado sesión 29 (09/07/2026). Implementar cuando el flujo de upload quede estable.
  B.42 (endpoint purge-org eliminado) HECHO — era código muerto protegido con ADMIN_SECRET; diagnóstico original incorrecto.
      Nota: al revisar B.43 se comprobó también app/api/admin/purge-expired/route.ts — SÍ está
      protegido con ADMIN_SECRET. Los endpoints de admin restantes quedan: cleanup (página) y
      cleanup-orphans (protegidos con sesión de admin) y purge-expired (ADMIN_SECRET).
  B.43 (admin/cleanup-orphans protegido con sesión de admin) HECHO — endpoint y página corregidos; se conserva para el backfill B.4.
  B.44 (upload lock colgado tras subida exitosa) BUG diagnosticado; pendiente de fix en chat/page.tsx + ingest.
  B.45 (sync de Drive no escala) BLOQUEANTE DEL ONBOARDING — elevado a máxima prioridad en sesión 31.
  B.46-B.50 (ideas de producto surgidas del diseño de la bandeja, sesión 32, 10/07/2026):
    B.46 análisis por lote entre sí (ámbito de comparación configurable + estrategia en dos rondas).
    B.47 obsolescencia de análisis guardados al corregir un doc (solución: borrar incidencias ajenas + reanalizar X).
    B.48 archivo histórico: excluir versiones antiguas de la comparación sin borrarlas del corpus.
    B.49 filtro de documentos en chat/agente (futuro, técnicamente viable con metadata Pinecone).
    B.50 duplicidades vs contradicciones: no pesan lo mismo; descartes persistentes (D9, Fase C).
  B.51 — Jobs de análisis zombis: sin recuperación ante caída del worker.
    Si el worker de Railway muere entre 'processing' y 'completed'/'failed', el job
    en analysis_jobs queda bloqueante para siempre (status 'processing') y hay que
    cerrarlo a mano en Supabase (UPDATE ... SET status='failed'). Mientras existe,
    el 409 "analysis_in_progress" bloquea TODO análisis nuevo de la organización.
    Ocurrió en producción (13/07/2026): un exhaustivo se cortó porque Railway se
    quedó sin saldo. Falta timeout/recuperación: p.ej. tratar como 'failed' un job
    en 'processing' más de N minutos, o un heartbeat del worker. Hermano de la deuda
    técnica nº3 (análisis a cola). Riesgo OPERATIVO: un corte del worker bloquea los
    análisis de toda la org hasta intervención manual en la BD.
  B.52 — Estilo: el botón "Descartar" del AnalysisModal es gris, se prefería
    rojizo. Da sensación de deshabilitado aunque funciona. Cambio estético (no bug:
    el botón ya era gris antes del refactor 3A). Decidir el color junto con el diseño
    de los botones del modo review (paso 3), donde "descartar" se desdobla en "Cerrar"
    y "Quitar del corpus".
  B.53 — [RESUELTO S35 — el documentId ya viaja frontend→job→worker vía columna analysis_jobs.document_id; el resultado exhaustivo guarda document_id relleno] [FASE D, bloque único] Cerrar el ciclo del hash y la cola.
    Es UN solo bloque de trabajo, no piezas sueltas: (1) migrar document_id a
    analysis_jobs — la cola por org es imposible sin saber de qué documento es
    cada job; (2) el worker rellena analyzed_content_hash al completar el
    análisis; (3) encender la marca "modificado desde el último análisis" en la
    bandeja (aviso suave, tercera categoría no bloqueante, reanálisis a un clic,
    5 cr) — HOY NO se enciende porque el worker no puede rellenar la columna y
    la marca acusaría de "modificado" a documentos recién analizados en
    exhaustivo; (4) eliminar el umbral de 'pending' del 409 (B.51) y sustituirlo
    por semántica de cola: encolar varios pending será lo NORMAL, el gate actual
    se reescribe entero; (5) sustituir el umbral grueso de 20 min por heartbeat
    que el worker toque por etapa del pipeline.
  B.54 — [OPERATIVO, fuera de código] Averiguar y documentar el mecanismo de
    deploy del worker de Railway en Estado_Del_Proyecto.txt. Hoy no lo sabemos
    (¿auto-deploy desde GitHub como Vercel?). Fase D toca el worker: será lo
    primero que haga falta.
  B.55 — i18n de la bandeja de revisión. Los textos de /settings/review y de
    components/review/ están hardcodeados en español; el resto de la app usa
    next-intl con claves. Decisión consciente de B.5 por simplicidad. También
    ReviewActions (los de UploadActions sí usan t()).
  B.56 — DocumentsSidebar.tsx tiene 750 líneas (límite del proyecto: 400).
    Trocearlo en subcomponentes. No se hizo en B.5 para no abrir otro frente.
  B.57 — Ambigüedad de la prop `mode` en AnalysisModal: el subcomponente interno
    AnalysisModeBadge recibe mode: 'quick'|'exhaustive' y el modal principal
    mode: 'upload'|'review'. Scopes distintos, compila bien, pero el nombre
    duplicado confunde al leer (nos costó una verificación). Renombrar el del
    modal a viewMode. Cosmético, cero riesgo.
  B.58 — Documentos indexados desde el modal de mejora ANTES del fix e91e8b44
    no tienen full_text ni content_hash: darán 422 en /api/documents/[id]/text
    y no se pueden mejorar ni analizar. Si alguno importa, rellenar full_text
    con SQL. Los nuevos ya nacen bien.
  FASE B COMPLETA (B.0–B.6, sesiones 26-33, hasta 19/07/2026):
  B.0 refactor Pinecone, B.1-B.4 estados y backfill, B.5 bandeja de revisión
  completa (pasos 0-4 + B.51 + fix index-text + B.5-hash-a), B.6 filtro del
  chat por 'analizado' (D3, lib/rag.ts CHAT_ANALYZED_FILTER).
  Siguiente: FASE C (identidad y reemplazo D5, relevo D6, portero por hash D7,
  fingerprints permanentes D9, aviso al borrar D10b). La Fase C es la más
  delicada: toca la identidad de documentos y el duplicado fantasma
  (google_drive+onedrive del mismo archivo) es su caso de prueba principal.
  FASE C — C.1 (identidad de origen) CERRADO en sesión 34: rename source_path→
  provider_file_id, índice único documents_identity_unique, detección honesta
  de errores en el sync (C.1c-1/C.1c-2), todo verificado en producción con
  trigger centinela. Siguiente: C.4b (modelo de generación), tras el diseño de Fable.
  B.59 — [RESUELTO S35, commit 5a020619] [FLECO UI de C.1c] El backend del sync ya cuenta failed/deleteFailed y devuelve
    success:false, pero la UI de sincronización NO muestra los fallos al usuario
    (solo pinta nuevos/actualizados/eliminados/sin cambios). Un fallo que el usuario
    no ve sigue siendo silencio para él. Mostrar failed/deleteFailed en el resultado
    del sync. Consultar a Fable si es parte imprescindible de C.1c o pieza aparte.
  B.60 — [PARA FASE D] Persistir el resumen del sync (N ok / M fallidos + errores) en BD
    (drive_connections o donde la cola dicte) — prerrequisito de mover el sync a
    background; hoy el resumen solo vive en la respuesta HTTP y muere con ella.
    Patrón de referencia: status + result jsonb + error_message de analysis_jobs.
  B.61 — [RESUELTO por disolución en C.3, S35 — el reemplazo es UPDATE en sitio, no hay reinsert, no hay 23505 posible] [PARA C.3, LITERAL] Resolver el orden del reemplazo (insertar-antes-de-borrar
    vs actualizar-en-sitio) compatible con el índice de identidad — el índice manda,
    el flujo se adapta. Evidencia: la verificación de C.1c dejó 4 huérfanos (uno por
    intento) por subir vectores antes del insert.
  B.62 — [OBSERVACIÓN PARA FASE C, BORRADOS] Hoy un documento borrado en Drive/OneDrive
    sigue en el corpus hasta el siguiente sync (el borrado es reactivo, no inmediato).
    En ese intervalo el chat aún puede responder con su contenido. Valorar con Fable
    si conviene un borrado más inmediato o el modelo reactivo basta.
  B.63 — [FLECO de B.59] El success del sync (app/api/drive/sync) se calcula como
    failedCount===0, ignorando deleteFailedCount. Un sync donde solo falla un borrado
    reporta success:true con deleteFailed>0. Valorar con Fable si success debe ser
    failedCount===0 && deleteFailedCount===0. La UI ya muestra deleteFailed (B.59),
    esto es solo la semántica del flag success. No urgente.
  B.64 — [RESUELTO S35] Candado transversal. El candado (upload_locked_by/at en
    organizations) ya cubre TODAS las rutas que mutan el corpus: ingest y analyze-v2
    (ya lo tenían) + sync, index-text y deleteDocument (que lo hereda a DELETE
    /api/documents y a la acción de duplicados de cleanup). 423 visible en la UI
    (helper uploadLockMessage). Admin sin bypass. Verificado con dos usuarios.
    PENDIENTE MENOR derivado: acción explícita de "liberar candado de otro" (admin,
    cuando el portador está ausente) — no construir ahora, solo anotado.

  B.65 — [RESUELTO S35, D3-rev] El análisis compara solo contra corpus validado
    (analysis_status='analizado'), no contra pendientes sueltos. Fuente de verdad:
    Supabase, no metadata de Pinecone. En rápido (endpoint) y exhaustivo (worker),
    cargando la lista de analizados justo antes de analizar. PENDIENTE relacionado:
    B.46 (excepción del lote — que un lote se compare entre sí) para cuando exista
    el flujo de análisis por lotes.

  C.4 — [EN CURSO] Relevo atómico de versiones (D6). Objetivo: que un documento
    nunca parpadee en el chat durante un reemplazo — siempre una generación activa,
    la vieja sirve hasta que la nueva valida, swap atómico. DISEÑO CANÓNICO:
    Diseno_C4_F1.txt (F-1). Protocolo de canal F-n vigente (toda consulta a Fable
    cita la última respuesta recibida).
    NOTA: el primer "C.4a" (chat por lista de Supabase con $in) fue un diseño
    fantasma NO emitido por Fable; F-1 lo REVIRTIÓ (moría con las generaciones).
    Sub-fases (orden F-1):
      · REVERSIÓN [HECHO S37, commits 841325b5+e9ae0199] — chat y análisis filtran
        por CORPUS_ACTIVO (constante compartida, metadata Pinecone), no por listas
        de Supabase. Retirados: el $in del chat y analyzedDocumentIds del análisis
        (mecanismo de B.65-app/worker). La REGLA de B.65 se mantiene vía el filtro.
        excludeDocumentId intacto.
      · C.4a-real [HECHO S37] — commit f62128f8. Modelo en Supabase (solo esquema,
        cero comportamiento): columna active_generation (int, default 1) en documents +
        tabla document_staged (document_id PK, generation, full_text, content_hash,
        chunk_count, size_bytes, source_modified_at, created_at; RLS por org).
      · C.4b [HECHO S37] — commits 28dfeae7 + 55014f88. Capa B.0 consciente de
        generación: IDs ${documentId}-g{N}-{i} para N>=2 + metadata generation; g1
        implícita (parsing tolerante, sin backfill); adaptado el listado/parseo de
        C.3c. Sin tocar el chat.
      · C.4c [HECHO S37] — swapDocumentVectors (lib/document-swap.ts), idempotente
        de 4 patas con document_staged como marcador de "swap en curso" (diseño F-3).
        Verificado con desastres fabricados (endpoint temporal, ya retirado). Commits
        05ac49fd (función), c1ac531e (VectorMetadata.generation, deuda de C.4b),
        88019c86 (retirada del endpoint de prueba).
      · C.4d [HECHO S38] — el sync versiona. d-1 HECHO (e9f4801f+250482e0). d-2
        rediseñado por F-6: el swap se dispara al COMPLETAR el análisis (analyze-v2),
        NO al validar; mark-analyzed con staged NIEGA salvo aprobación explícita
        (409 por defecto, approveStaged:true = aprobación humana, F-11). d-2a HECHO
        (bandeja muestra staged + texto staged-first, S38). d-2b HECHO (S38, 30
        commits, F-7 a F-18): portero de calidad (swap condicional según hallazgos
        de corpus, F-4-rev), puntero al análisis exacto que frenó (F-12), veto de
        concurrencia interino (F-13/F-14), veto por origen sincronizado (F-9-rev/
        F-15, addendum Diseno_C4_F9rev_origen.txt), UI de decisión completa
        (activar/descartar, opción C sobre AnalysisModal) y el cerrojo del sync
        arreglado (F-16, addendum Diseno_C4_F16_cerrojo.txt). Detalle completo en
        Bitácora, entrada "SESIÓN 38 (cont.)".
      · C.4e [PENDIENTE, residual — F-17] — lo que F-15 dejó vivo: migrar la mejora
        de documentos MANUALES a swapDocumentVectors (hoy index-text borra-e-inserta
        directo; el veto por origen ya saca a los de Drive de este camino) y matar
        el match-by-name del modal de mejora en favor de emparejado universal por
        document_id (hoy solo los staged con puntero exacto lo usan, F-12; los docs
        normales siguen por nombre) + detector de multi-generación en cleanup como
        red de seguridad. Foto de fase F-17: es la última pieza de la Fase C, tras
        C.6/C.5/C.7/C.8.

      PENDIENTES DESTAPADOS EN d-2b (Sesión 38, detalle en Bitácora):
        - [✓ HECHO, 15/08/2026] Barrido de resucitados + conteo al dueño (única
          pieza viva de F-9-rev): ejecutado, CERO hallazgos — 0 grupos por
          content_hash y 0 pares mismo-nombre/distinto-origen en ambas orgs de
          prueba. F-9-rev queda COMPLETO. Detalle en Bitácora, "SESIÓN 38
          (cierre)".
        - Comentario cruzado pendiente en el enum/config de providers (F-18): "al
          añadir proveedor: revisar veto de index-text (F-9-rev)".
        - mark-analyzed no comprueba el veto de concurrencia (lib/analysis-lock.ts):
          ventana confusa, no corruptora (el swap es idempotente). Para Fase D.
        - [✓ RESUELTO 21/08/2026, ver B.73] Carpeta sin trackear
          app/api/admin/diagnose-vectors/: registrada en git tras arreglar su
          reconstrucción manual de IDs de vector.
        - Import huérfano deleteVectorsByIds en index-text/route.ts (sin uso tras
          1661958a).
        - window.alert() de errores de indexado en el modal de mejora → sustituir
          por banner integrado.
        - [✓ RESUELTO, commit 0aef3530] El 409 de concurrencia ya no se cuenta como
          "N con error" en la bandeja: ReviewAnalysisSummary separa failed de
          blocked, y el resumen sale como aviso azul informativo con el mensaje
          real del veto.
        - [NUEVO, destapado al ejecutar el barrido] /api/admin/duplicates (C.2) no
          detecta el patrón real de un resucitado: agrupa por content_hash
          IDÉNTICO, pero un resucitado y su corrección manual son textos
          DISTINTOS (la corrección existe porque el original tenía un problema)
          — el patrón real es mismo NOMBRE + un lado de nube y otro manual, con
          hashes distintos, y hoy solo se encuentra por SQL directo. Además su
          preselección automática marca como superviviente la copia MÁS
          ANTIGUA, que en este patrón suele ser la CORRECCIÓN: un admin que
          confíe en el valor por defecto borraría la buena y conservaría el
          resucitado. Evaluar detección adicional por nombre+origen, o al menos
          invertir/avisar en la preselección.
        - Pendiente de comentar los dos hallazgos de arriba (barrido en cero +
          hueco de la herramienta de duplicados) a Fable en la próxima consulta;
          no se le escribió mensaje propio porque no había decisión que tomar
          todavía.

  B.65 — [REGLA VIGENTE, mecanismo migrado en S37] La regla (el análisis solo
    compara contra corpus validado) SIGUE activa, ahora vía el filtro CORPUS_ACTIVO
    en Pinecone (antes: lista analyzedDocumentIds descartada en memoria, retirada en
    la reversión de S37). No confundir la retirada del mecanismo con deshacer la regla.

  B.66 — [BUG mejora-con-IA] En el modal de "Mejora con IA", al resolver/solventar un
    problema de tipo DUPLICADO, el editor de texto se queda VACÍO (borra todo el
    contenido) en vez de eliminar solo el fragmento duplicado. Antes eliminaba solo la
    parte duplicada — regresión. Revisar la lógica que aplica la resolución de un
    problema al texto del editor (components/improvement/, EditorPanel / la función
    que quita el fragmento). No urgente (acción manual) pero es pérdida del texto en
    edición. Arreglar antes de cerrar el bloque grande de análisis/versionado.
    Reproducido en S37.

  B.67 — [Chat/RAG] El dedupe a máximo 4 documentos hace que un documento corto
    pierda el corte frente a Excels que generan muchos fragmentos al convertirse en
    tablas Markdown (un protocolo de una página compite en igualdad de condiciones
    contra un Excel que aporta decenas de fragmentos). Evaluar subir el límite o
    señalar al usuario cuándo usar el agente (que no tiene el problema: busca en
    varias vueltas y lee documentos enteros) en vez del chat. Detectado en S38 con
    el corpus de prueba Dentalia (ver Bitácora, "SESIÓN 38 (cierre 2)").

  B.68 — [Sync] Mensaje accionable cuando pdf-parse falla o devuelve 0 caracteres
    (hoy el usuario solo ve "N sin texto legible" sin saber qué hacer). Sugerir
    reescanear/reguardar el PDF o comprobar si está escaneado sin OCR. No es bug:
    pdf-parse no soporta PDFs generados por script ni sin capa de texto; los PDF
    ofimáticos normales se leen bien.

  B.69 — [Sync] Evaluar soporte de .odt (LibreOffice) en extractText y en las
    listas blancas de los proveedores (onedrive.ts, google.ts, ingest).

  B.70 — [Coste] Repetir la medición de llm_usage con ~100 turnos de agente (la
    muestra de S38 fue de 16: 9,3 céntimos de media, máximo 30) y decidir si el
    mínimo de 15 créditos cobrados por turno cubre el peor caso — con margen medio
    del 38% hoy, un turno largo puede salir negativo. Investigar además por qué
    analyze_exhaustive con Haiku cuesta 3x lo que con Sonnet.

  B.71 — [Observabilidad] llm_usage no se lee desde ninguna pantalla: /settings/usage
    agrega usage_logs, analysis_results y chat_queries, pero no el coste real en
    USD que ya guarda esa tabla. Y las filas operation='agent' tienen
    credits_charged NULL (el worker no lo pasa al persistir), así que no se puede
    comparar coste real contra créditos cobrados sin cruzar con agent_messages.

  B.72 — [Agente] tokensToCredits (lib/agent/credit-calc.ts) asume Sonnet fijo y no
    lee el modelo real de llm_usage; si el agente cambiara de modelo, los créditos
    cobrados quedarían desalineados del coste real sin que nada lo detecte.

  B.73 — [✓ HECHO 21/08/2026] [SEGURIDAD/CORRECCIÓN, mismo fallo que mark-analyzed] Ocho puntos construyen
    IDs de vector asumiendo generación 1. Detectado en la exploración del paso 3 de
    F-20 (agosto 2026), hermano directo del fallo ya corregido en mark-analyzed
    (commit b8d0df63). Construyen `${id}-${i}` a mano en vez de usar buildVectorId
    con la generación activa del documento: para un documento en generación >= 2
    esos IDs no existen.
      - lib/delete-document.ts:144 — borra, CON red de seguridad (hace también
        deleteVectorsByFilter por documentId en paralelo; el borrado real no
        depende de estos IDs).
      - app/api/drive/disconnect/route.ts — borra, SIN red de seguridad confirmada,
        y opera sobre documentos de Drive, los únicos que versionan. REVISAR PRIMERO.
      - app/api/documents/coverage/[id]/route.ts:92,97 — lee; el panel de cobertura
        mostraría todos los chunks vacíos.
      - app/api/improve/route.ts:108 — lee; el modal de mejora recuperaría texto vacío.
      - app/api/ingest/route.ts:178 — borra vectores de un manual viejo por colisión
        de nombre; correcto hoy por accidente (los manuales nunca versionan), no por
        diseño.
      - app/api/admin/cleanup-orphans/route.ts — el backfill de analysisStatus
        etiquetaría IDs inexistentes.
      - app/api/admin/diagnose-vectors/route.ts:46 — herramienta de diagnóstico:
        mentiría para generación >= 2.
      - lib/pinecone/vectors.ts:150 — NO es un fallo, es el prefijo de
        listVectorIdsByPrefix, correcto por diseño; se anota para que no se
        "arregle" por error.
    HORIZONTE 0: hoy el corpus está entero en generación 1 y no muerde; en cuanto se
    apruebe una versión de Drive habrá documentos en generación >= 2 y estos ocho
    empiezan a fallar sin avisar. Fix: buildVectorId(id, activeGeneration, i)
    leyendo documents.active_generation, igual que b8d0df63. Uno por uno, build
    verde entre medias. Empezar por drive/disconnect.
    RESUELTO en siete puntos + el constructor común: 47974739 (buildAllVectorIds),
    c4f32867 (drive/disconnect, + aborta si falla el borrado), a46512b9 (ingest,
    ídem), 34cc8f37 (cleanup-orphans, + byGeneration en el informe), 193c0735
    (coverage, improve, delete-document) y este commit (diagnose-vectors, que
    además queda registrado en git).

  B.74 — [OBSERVABILIDAD] El filtro de 50 caracteres (lib/chunking.ts:436, y su
    gemelo en chunkText) descarta toda pieza de <=50 caracteres sin log ni contador:
    nadie puede saber cuántas piezas perdió un documento. MEDIDO EMPÍRICAMENTE sobre
    los 5 ficheros de muestra del piloto (agosto 2026): 133 filas de tabla extraídas
    entre OPE-06, OPE-02 y RRHH-06, CERO descartadas; los dos únicos descartes
    fueron ruido real (una leyenda de hoja y un membrete de empresa). Longitud
    mínima de una fila que pasa el filtro: 78 caracteres, 28 por encima del umbral —
    el corpus de muestra NO está al borde. RIESGO VIVO: un Excel de cliente con
    columnas de nombre corto y pocos valores podría caer por debajo de 50 y perder
    filas reales EN SILENCIO; el umbral se diseñó para prosa y se aplica igual a
    filas de tabla, que son información completa aunque sean cortas. Fix propuesto
    (no construir aún): no aplicar el filtro a chunk_type 'table_row' (o un umbral
    distinto), y en cualquier caso contar los descartes y exponerlos igual que
    unreadableCount en el sync.

  B.75 — [MEDICIÓN PENDIENTE] La medición de B.74 cubre 5 ficheros de muestra en
    local, no los 40 del piloto. Antes de dar por bueno que el corpus real no pierde
    filas de tabla, repetir la medición sobre los 40 — idealmente instrumentando el
    propio filtro con un contador, que es el fix de B.74 y resuelve las dos cosas de
    una vez.

  B.76 — [PRERREQUISITO DEL PRIMER CLIENTE DE PAGO] Reprocesado del corpus sin
    intervención del cliente. El paso 3 de F-20 (job de reprocesado por revisión de
    extracción) se RETIRÓ por decisión del director, validada por Fable en F-21: sin
    clientes, el corpus se rehace borrando y resubiendo, que es gratis. Con un
    cliente real deja de serlo. SE AGRUPA CON la retención del original en Storage
    para el camino de ingest (hoy ingest borra el fichero y no hay columna con su
    ruta, así que los documentos manuales no son reprocesables en absoluto). Fable
    (F-21): "son la misma necesidad — poder rehacer el corpus de un tercero sin su
    intervención — vista desde los dos caminos de entrada, y deben resolverse
    juntas".
    LO QUE YA SE SABE, para que quien lo retome no repita la exploración:
      - analysis_jobs NO admite otro tipo de job: no tiene columna job_type y su
        esquema exige document_text, sample_texts y credits_consumed. Además el
        worker recoge esa cola solo por polling de 10s (el trigger HTTP sirve
        únicamente a la cola del agente) y veta más de un job en curso por
        organización.
      - El worker de Railway NO puede hablar con Drive: le faltan las variables
        (DRIVE_ENCRYPTION_KEY y credenciales de Google/Microsoft), le faltan las
        librerías de extracción (xlsx, mammoth) y el refresco del token OAuth no es
        un helper de lib/ sino código duplicado dentro de
        app/api/drive/sync/route.ts (ramas POST y GET).
      - swapDocumentVectors NO es reutilizable tal cual: cuelga entero de que exista
        una fila document_staged, que es el mecanismo del portero de calidad que un
        reprocesado NO debe atravesar (el contenido no ha cambiado). Fable (F-21):
        el trabajo no es "reutilizar el swap" sino "extraer del swap la parte de
        relevo y dejar el portero fuera".

  B.77 — [F-20, paso 4 pendiente] El muestreo corta a 40 los documentos con muchos
    chunks. pickSampledTexts (app/api/analyze-v2/route.ts) devuelve todos si hay
    <=40 chunks, 30 si hay 41-80, y 40 si hay más. Medido en OPE-06: 114 chunks
    persistidos, 40 samples — se descartan 74 filas y se busca con una de cada
    tres. El criterio (índices distribuidos uniformemente) se diseñó para prosa,
    donde muestrear un documento largo es razonable; para una tabla significa
    ignorar dos tercios de las filas. No es una regresión: antes el problema
    quedaba oculto porque el reagrupado nunca producía tantos chunks. Decidir si
    las filas de tabla deben quedar exentas del muestreo o si el tope sube.

  B.78 — [CALIDAD DEL JUICIO] Falso positivo por proximidad entre filas de la
    misma persona. En el análisis de RRHH-06 contra OPE-02 el juez emitió una
    "contradicción" titulada "Horas semanales de Nuria Ferrer" citando dos filas
    que NO se contradicen: una de evaluaciones (empleado, puesto, clínica, fecha)
    y otra de turnos (empleado, puesto, box, días, horas). Coinciden en empleado y
    puesto; el título anuncia una discrepancia de horas que no aparece en el texto
    citado. Hipótesis: al ver filas sueltas, el juez confunde "hablan de la misma
    entidad" con "se contradicen" — efecto colateral del troceado por fila, no del
    troceado viejo. Es justo lo que el paso 5 (citas estructuradas {sheet, rowKey,
    cells} + verificador sobre celdas) debería cortar. Anotado como caso de prueba
    concreto para validar el paso 5.

  B.79 — [TIPOS] El análisis etiqueta los documentos de OneDrive como 'manual'.
    DocumentFragment y CandidateDocument (lib/analysis/types.ts) tipan source como
    'manual' | 'google_drive', sin 'onedrive', y lib/analysis/retrieval.ts:132
    mapea `meta.source === 'google_drive' ? 'google_drive' : 'manual'`. Un
    documento de OneDrive llega al pipeline marcado como manual. Hoy no rompe
    nada visible, pero falsea el origen en cualquier decisión que lo consulte.

  B.80 — [FLECO, ingest] El fichero subido no se limpia de Storage en los caminos
    de error. app/api/ingest/route.ts sube el fichero a Supabase Storage y lo
    borra al final del camino feliz (paso 10). De los caminos de error, solo el de
    "texto insuficiente" limpia; los de descarga fallida (500), extracción fallida
    (400) y el nuevo de borrado de vectores fallido (502, B.73) dejan el fichero
    en Storage. Inconsistencia preexistente, detectada al revisar B.73. Efecto:
    ficheros huérfanos en el bucket que nadie borra. Barato: un finally o un
    helper de limpieza compartido por los cuatro caminos.

  B.81 — [PRODUCTO / ASIMETRÍA DEL ANÁLISIS] Encontrar una contradicción depende
    de cuál de los dos documentos analices. El juez recibe el documento NUEVO
    entero (newDocumentText, hasta NEW_DOC_LIMIT_QUICK = 6000 chars) pero del
    documento EXISTENTE solo los fragmentos que el retrieval recuperó (máximo
    MAX_FRAGMENTS_PER_DOC_QUICK = 25, con presupuesto FRAGMENT_BUDGET_CHARS_QUICK
    = 3000). Analizar A contra B NO es la misma operación que analizar B contra A.
    MEDIDO EN PRODUCCIÓN (21/08): analizando RRHH-06 aparece la discrepancia del
    puesto de Dr. Pablo Reyes contra OPE-02; analizando OPE-02 con RRHH-06 en el
    corpus, el emparejamiento SÍ ocurre (2 candidatos, 1 seleccionado por el
    rerank, 1 juicio emitido) pero el juez no emite nada — porque la fila
    correspondiente de RRHH-06 no está entre los fragmentos que se le muestran.
    NO ES UN BUG: es consecuencia directa del diseño asimétrico. Pero es un límite
    del producto que un cliente percibiría como incoherencia ("¿por qué me lo dice
    en un documento y no en el otro?"). Decidir si se documenta como
    comportamiento esperado, si el análisis debe ser simétrico para pares
    seleccionados, o si basta con avisar en la bandeja. Relacionado con B.47
    (análisis obsoletos al cambiar el corpus) y con el techo de recall de
    MAX_SELECTED_QUICK = 6.

  B.82 — [CALIDAD DEL JUICIO, refuerza B.78] El juez no es estable ni consigo
    mismo: el mismo par de documentos, con el mismo prompt byte a byte, produce
    falsos positivos DISTINTOS entre ejecuciones. Medido el 21/08 sobre RRHH-06 vs
    OPE-02: una ejecución emitió "Horas semanales de Nuria Ferrer" y otra "Horas
    semanales de Dra. Ana Belmonte" — el mismo patrón de falso positivo (título
    que anuncia una discrepancia de horas que las citas no contienen; en la
    segunda las citas son literalmente "Fecha evaluación: 2026-06-11" contra
    "Horas semana: 8", dos datos distintos que el propio prompt pone como ejemplo
    de lo que NO es contradicción), con distinto empleado. El prompt no cambió
    entre ambas (verificado en código: 4d-2 no toca judge.ts). Es evidencia
    adicional para F-22: no es un problema de instrucciones, es que una sola
    pasada de generación no puede vigilarse a sí misma. Se resuelve con el
    verificador de hallazgos, no con un ejemplo más en el prompt.

  B.83 — [FRENTE ABIERTO, localizado] El falso negativo NOR-01 / CLI-03 muere en
    el RERANK, no en el juez. La contradicción sembrada (conservación de historia
    clínica: 15 años en CLI-03 contra 5 en NOR-01) apareció el 20/08 con un corpus
    de 4 documentos y NO aparece el 21/08 con el corpus ampliado. Log del
    21/08 analizando CLI-03: "Retrieval: 2 candidatos" seguido de "Rerank: 0
    seleccionados" — el par llega al rerank y el rerank lo DESCARTA, así que el
    juez nunca ve esos documentos juntos. Confirma la lectura prudente que ya se
    había anotado: la detección del 20/08 fue efecto del corpus reducido (menos
    competencia por las plazas del rerank), no una mejora real. El frente sigue
    abierto, pero ahora se sabe DÓNDE: la etapa de rerank (lib/analysis/rerank.ts,
    MAX_SELECTED_QUICK = 6 y el criterio con el que el LLM elige). Es el mismo
    techo de recall que F-22 señaló como "el siguiente lugar donde un verdadero
    positivo se pierde sin dejar rastro".

  B.84 — [CALIDAD DEL JUICIO, exhaustivo] El double-check emite "inconsistencias
    menores" que son el patrón 1 de F-23, y con narración dentro de la cita.
    Medido el 21/08 en un exhaustivo de RRHH-06 contra OPE-02, con el rápido
    dando 0 hallazgos de ese tipo sobre el mismo par:
      "Dra. Marta Gil es Odontóloga general en la clínica Chamberí." contra
      "Dra. Marta Gil es Odontóloga general en Clínica Dentavia Chamberí (no solo
      'Chamberí')."
    Dos problemas en un solo hallazgo:
    (1) NO es una inconsistencia: las dos afirmaciones dicen lo mismo con distinto
        grado de detalle. Es exactamente el patrón 1 (equivalencia) que F-23 manda
        RECLASIFICAR a solapamiento mediante la regla determinista R1.
    (2) La segunda "cita" NO es una cita: contiene un comentario del modelo
        —"(no solo 'Chamberí')"— dentro del texto entrecomillado. El prompt del
        juez lo prohíbe explícitamente ("Las citas deben ser TEXTO COPIADO tal
        cual del documento, sin comentarios, sin explicaciones") y
        fixQuotesInJudgment tiene un filtro de narración (NARRATION_PATTERNS) que
        lo debería haber descartado. Averiguar por qué no lo hizo: o esos
        hallazgos vienen del camino atómico / double-check y NO pasan por
        fixQuotesInJudgment (F-20 ya anotó que verify-claims nunca pasa por el
        verificador de citas), o el patrón no cubre esta forma concreta.
    Ambos casos mueren con el verificador de hallazgos, aplicado también sobre el
    camino atómico como fija F-24 (P2). Anotado como caso de prueba concreto para
    validarlo.

  B.85 — [BUG, ingest] Subir a mano un documento que ya existe en Drive crea un
    duplicado al 100%. Detectado por el usuario el 21/08 y coherente con el
    incidente del doble OPE-02 del 20/08. app/api/ingest/route.ts detecta
    colisiones por nombre SOLO entre documentos manuales: la consulta filtra
    .or('source.is.null,source.neq.google_drive') y después manualCollisions
    vuelve a filtrar d.source !== 'google_drive'. Un fichero con el mismo nombre
    ya sincronizado desde Drive no cuenta como colisión, así que se indexa una
    segunda copia y el análisis del nuevo documento encuentra un duplicado exacto
    consigo mismo.
    El filtro es deliberado —viene del veto por origen de F-9-rev/F-15, que saca a
    los documentos de Drive del camino de reemplazo manual para no romper la
    identidad de origen— pero la consecuencia no lo es: hoy el usuario acaba con
    dos documentos idénticos y sin aviso. Mínimo aceptable: detectar la colisión
    con un documento de Drive y AVISAR ("ya existe un documento con este nombre
    sincronizado desde Drive; si quieres reemplazarlo, hazlo en Drive y
    sincroniza"), en vez de indexar en silencio. No debe reemplazarlo: el veto por
    origen sigue siendo correcto.
    Nota: el filtro de duplicados por content_hash (C.2) tampoco lo evita, porque
    actúa al analizar, no al indexar.

  B.86 — [DIAGNÓSTICO, conteo de descartes] "Cita inventada" y "cita real
    atribuida al documento equivocado" se cuentan como el mismo motivo. Detectado
    al investigar un descarte del 21/08: en un exhaustivo de RRHH-06, el juez
    emitió un solapamiento contra CLI-05 poniendo en el campo `evidence` —que debe
    ser una cita LITERAL del documento existente— la frase "Pendiente reciclaje de
    protección radiológica", que pertenece en realidad a RRHH-06, el documento
    ANALIZADO (hoja "Evaluaciones", fila del Dr. Pablo Reyes, columna
    "Comentarios": "Buen nivel general, pendiente reciclaje de protección
    radiológica."). CLI-05 no contiene esa frase ni nada parecido: no aparece la
    palabra "reciclaje" en todo el documento.
    El descarte fue CORRECTO: verifyQuote buscó la cita en CLI-05, no la encontró
    y la rechazó. La capa de procedencia funciona y no descarta de más — se
    verificó reproduciendo findBestMatch contra el texto real, que SÍ la encuentra
    cuando el haystack es el documento correcto.
    LO QUE FALTA: fixQuotesInJudgment incrementa `citaNoVerificable` sea cual sea
    la causa, y no intenta buscar la cita fallida en el haystack CONTRARIO. Son
    dos fallos distintos del juez —alucinación de la cita frente a confusión de
    campos— con implicaciones distintas, y hoy son indistinguibles en el conteo.
    FIX PROPUESTO (para el verificador de hallazgos, F-22/F-25): cuando verifyQuote
    falle en un lado, reintentar contra el texto del otro lado. Si aparece allí, el
    motivo es `cita_lado_cruzado`, no `cita_no_localizada`. Es una llamada más a
    una función que ya existe, y convierte el conteo en diagnóstico: si predomina
    el lado cruzado, el problema está en cómo se le presentan los dos documentos al
    juez; si predomina la no localizada, en el modelo. Encaja con la petición de
    F-25 de registrar también qué lado falló.

  B.87 — [DEUDA CONDENADA, F-29] chunkText, en su camino del marcador
    PRE_SEGMENTED_SEPARATOR, es un duplicado estructural de la rama de tabla de
    chunkSegments: las dos parten el mismo texto pieza por pieza respetando lo que
    decidió extractSegments. Hoy la subida inicial de un documento (analyze-v2,
    caso rápido y exhaustivo, cuando aún no está indexado) trocea el MISMO
    documento DOS VECES con DOS funciones distintas en la misma request:
    chunkSegments(extractedSegments) produce newDocChunks (los chunks tipados
    correctos, con chunkType/cells, que sí llegan al verificador de hallazgos vía
    new_document_chunks/newDocumentChunks), pero sampleTexts —lo que de verdad ve
    el LLM en el análisis, tanto rápido (pickSampledTexts(chunks)) como exhaustivo
    (chunks.map(c => c.text))— sigue saliendo de chunks, que es
    chunkText(joinSegments(segments)): la ruta vieja, re-derivada del texto plano
    en vez de reutilizar newDocChunks que ya está calculado un poco más abajo en
    el mismo fichero (app/api/analyze-v2/route.ts:231 vs 262-266).
    NO SE ARREGLA AHORA: está condenado. La pieza aplazada de F-25 (derivar
    sampleTexts de los chunks ya calculados, en vez de rehacerlos con chunkText)
    es lo que lo cierra. En cuanto eso se haga, chunkText pierde su único
    llamador externo (ver B.88) y se retira en el paso 6.

  B.88 — [PARA EL PASO 6, grep de referencia] Todos los llamadores de chunkText
    en el repo, a fecha de F-29 (22/08/2026), para que su retirada no sorprenda a
    nadie: UNO SOLO fuera de lib/chunking.ts —
    app/api/analyze-v2/route.ts:231 (`const chunks = chunkText(text, 'temp-id',
    fileName, orgId);`), alimentado por `text`, que es o bien `directText` (ya
    sin marcador, texto plano) o `joinSegments(segments)` (con marcador, para el
    caso storagePath de F-29:B.87). Ningún otro fichero del repo la llama — grep
    `chunkText\(` sobre *.ts, único resultado externo a chunking.ts. El día que
    B.87 se cierre, retirar chunkText (y buildChunks, que solo ella usa) es
    borrar código sin tocar ningún otro llamador.

  B.89 — [LÍNEA DE BASE MEDIDA, 22/08] Con el corpus ampliado a 13 documentos, el
    acierto de control DESAPARECE. Medición hecha tras el ciclo de resubida de
    B.74 (extractor_version 2), en modo rápido y con los 13 documentos en la
    tanda:
      RRHH-06 → 20 samples, "Contexto de fragmentos: 1/1 resueltos",
                1 juicio emitido, SIN INCIDENCIAS.
      OPE-08  → 35 samples, 3 candidatos, "Rerank: 0 seleccionados", sin juicio.
      RRHH-02 → 1 sample (el convenio entero es UN chunk), 3 candidatos,
                1 seleccionado, 1 juicio, sin incidencias.
    Con solo 4 documentos en el corpus, ese mismo RRHH-06 emparejaba con OPE-02
    recuperando 16 fragmentos y encontraba la discrepancia del puesto de Dr. Pablo
    Reyes ("Implantólogo" contra "Implantólogo / Cirujano oral"), que es el único
    acierto verificado que teníamos como control. Con 13 documentos, empareja con
    UN solo fragmento y no encuentra nada.
    CON MÁS CORPUS ENCUENTRA MENOS. No es un fallo introducido por los cambios de
    esta sesión: el rerank descarta desde siempre, y con 4 documentos no había
    nada que descartar. El problema estaba tapado por el tamaño del corpus.
    Es el mismo techo de recall de B.83 (MAX_SELECTED_QUICK = 6), ahora medido
    sobre un acierto real y no solo sobre una contradicción sembrada.
    LO QUE NO SABEMOS, y decide si la corrección planificada basta: el log cuenta
    cuántos candidatos hubo, pero no CUÁLES. No consta si OPE-02 estaba entre los
    candidatos de RRHH-06 y el rerank lo descartó, o si ni siquiera llegó a
    candidato. En el primer caso, la corrección del rerank lo devuelve; en el
    segundo, la causa está antes —en el retrieval— y el rerank no la toca.
    NO SE ARREGLA AHORA. Está planificado: F-22/F-23 fijan que el rerank se salte
    cuando candidatos <= MAX_SELECTED_QUICK (cuando hay menos candidatos que
    presupuesto, esa llamada solo puede perder verdaderos positivos), y que vaya
    DESPUÉS del verificador de hallazgos — sin verificador, dejar pasar más
    candidatos abre la puerta a más falsos positivos sin nada que los filtre.

  B.90 — [CRITERIO DE COMPROBACIÓN, atado a B.89] Cómo verificar la corrección del
    rerank cuando se haga, para no darla por buena sin datos:
    PRUEBA: analizar RRHH-06 en modo rápido con los 13 documentos en la tanda.
    ÉXITO: reaparece la discrepancia del puesto de Dr. Pablo Reyes, con las citas
    "Puesto: Implantólogo" y "Puesto: Implantólogo / Cirujano oral".
    SI NO REAPARECE: la causa no era el rerank sino el retrieval, y hay que
    investigar por qué OPE-02 no llega a candidato. Antes de concluir nada,
    repetir 2-3 veces: el juez no es estable entre ejecuciones (B.82), así que una
    sola pasada no demuestra ni una cosa ni la otra.
    MEJORA DE INSTRUMENTACIÓN QUE HARÍA FALTA: el log de retrieval y rerank
    debería nombrar los documentos candidatos y los seleccionados, no solo
    contarlos. Hoy no se puede distinguir "el rerank lo descartó" de "nunca fue
    candidato" sin instrumentar. Barato y desbloquea el diagnóstico.

  B.91 — [LÍNEA DE BASE, 23/08 — sustituye a la de B.89] Con 14 documentos
    analizados en tanda (extractor_version 2, tras el ciclo de resubida de B.74 y
    con los chunks ya como haystack de verificación), el cuadro cambia respecto a
    B.89 y a favor:
      · REAPARECE el falso negativo histórico: la contradicción de conservación de
        historia clínica (15 años en CLI-03 contra 5 en NOR-01) se detecta, y en
        LOS DOS SENTIDOS. Con el corpus de 4 documentos había aparecido una vez y
        se atribuyó al tamaño reducido; con 14 ya no cabe esa explicación.
      · El RERANK deja de descartarlo todo: selecciona 3-4 candidatos por
        análisis, frente a los "0 seleccionados" repetidos que motivaron B.83 y
        B.89. Con más corpus hay material que repartir, que es para lo que existe
        esa etapa.
      · Los chunks como haystack funcionan: "Chunks para verificación: N/N
        documentos con chunks (0 por full_text de respaldo)" en todos los
        análisis. Ningún candidato cae al fallback.
      · 3 contradicciones sobre 14 documentos; el resto, solapamientos o sin
        incidencias.
    CONFIRMA B.81 (asimetría) CON UN CASO LIMPIO: RRHH-06 NO detecta la
    discrepancia del puesto de Dr. Pablo Reyes contra OPE-02, pero OPE-02 SÍ la
    detecta contra RRHH-06. Los logs lo explican: RRHH-06 seleccionó 3 candidatos
    y OPE-02 seleccionó 4 — distinto material delante, distinto resultado. No es
    un fallo nuevo: es el diseño asimétrico ya anotado.
    EL LOG DE LADOS INDEPENDIENTES (F-27 3.1) YA ACTÚA: en el análisis de OPE-06,
    dos solapamientos descartados con lado=nuevo, y la cita descartada era
    literalmente el nombre del fichero ("OPE-06_presupuestos-y-financiacion.xlsx")
    donde debía ir una cita del documento. El verificador lo cazó correctamente.
    TIEMPOS: 41s (CLI-03) y 48s (OPE-06), frente a los 9-15s del corpus pequeño.
    Con 4 candidatos hay 4 juicios, cada uno una llamada al LLM. Dentro del
    maxDuration de 120s, pero el margen se estrecha: anotar como dato a vigilar
    cuando el corpus crezca a los 40 documentos del piloto.
    VALOR PARA LO QUE VIENE: por primera vez el corpus produce hallazgos con los
    que medir el verificador de hallazgos. Con 4 documentos no producía ninguno y
    eso lo dejaba a ciegas.

  B.92 — [CRITERIO DE CIERRE DEL PASO 5, fijado por F-30] El paso 5 (citas
    estructuradas {sheet, rowKey, cells} — ver B.78) NO está terminado hasta que
    se borre la localización de filas por texto: splitTabularSegments y la vía
    por segmentos de verifyQuote (lib/analysis/judge.ts). Cuando el juez cite
    {sheet, rowKey, cells}, la fila se localiza por datos — una consulta directa
    a la fila, no un troceado de cadena buscando coincidencias — y esta vía
    muere con su troceador. Es un mecanismo puente (F-30 lo dejó reducido a la
    semántica mínima que hacía falta tras retirar findRowContainingSegments,
    ver F-27 P3), y sin este criterio escrito se vuelve permanente por inercia,
    igual que casi le pasa a chunkText (B.87/B.88).

  B.93 — [OPTIMIZACIÓN MEDIDA, 23/08] Concurrencia igualada y pausas
    incondicionales retiradas del modo rápido (F-31 P1+P3, commits 090ab79f y
    88aa2abe). Medición sobre la misma tanda de 14 documentos, antes y después:
      OPE-08  18s → 9s     OPE-07  28s → 10s
      OPE-02  33s → 15s    OPE-03  38s → 16s
      OPE-04  43s → 13s    OPE-05  44s → 20s
      OPE-06  55s → 17s
    Tres veces más rápido de media; la tanda entera pasa de ~6 minutos a ~4.
    En OPE-06 el retrieval cae de 23,6s a 3,1s: los seis lotes de embeddings, que
    antes se separaban con 3s de espera fija cada uno, se despachan en dos
    segundos.
    NI UN SOLO 429 en toda la tanda, así que la pausa incondicional no protegía
    nada — era el precio del peor caso pagado siempre. El backoff nuevo (con
    jitter, hasta 6 reintentos) loggea cuándo actúa, así que si alguna vez satura
    quedará registrado: es la instrumentación que Pinecone no tenía.
    RESULTADOS IDÉNTICOS: mismas contradicciones (puesto de Pablo Reyes y
    conservación de historia clínica, ambas en los dos sentidos), mismos
    descartes, mismos recuentos de contexto. Optimización pura, sin cambio de
    comportamiento.
    DATO NUEVO QUE ESTO DESTAPA: el rerank es ahora la etapa MÁS LENTA del modo
    rápido — 3 a 8,5 segundos en una sola llamada, y en varios análisis tarda más
    que todos los juicios juntos (8,5s frente a 8,6s de SEIS juicios en OPE-05).
    La corrección de B.83 ya planificada (saltar el rerank cuando
    candidatos <= MAX_SELECTED_QUICK) se lleva esa etapa entera en corpus
    pequeños, y ahora se sabe cuánto vale: es la mayor ganancia de latencia que
    queda sobre la mesa.
    CONTEXTO COMERCIAL (F-31): el coste y el tiempo NO escalan con el tamaño del
    corpus —Pinecone busca igual de rápido en 40 que en 4000 vectores— sino con
    el número de candidatos que superan el umbral, que está topado en
    MAX_SELECTED_QUICK. El coste por documento analizado es esencialmente plano y
    predecible, lo que es un argumento directo para el modelo de precios. Lo que
    sí escala con el corpus es cuántos documentos hay que analizar en una
    incorporación inicial (40 de golpe al dar de alta a un cliente), y ahí la
    pieza es el worker y la cola — no por velocidad, sino para no depender de que
    el navegador del cliente siga abierto.

  B.94 — [CERRADO COMO NO APLICABLE, 23/08] Caché de prompt en style-check.
    Descartado por umbral del modelo, no por diseño. STYLE_PROMPT mide 2.293
    caracteres (~570-700 tokens) y el mínimo cacheable de Claude Haiku 4.5 es
    4.096 tokens — verificado en la documentación de Anthropic, no de memoria:
    nuestra suposición era la contraria (creíamos que Haiku exigía MENOS que
    Sonnet, y es al revés; Sonnet son 1.024).
    LO IMPORTANTE, y la razón de anotarlo con el número delante: la API acepta
    cache_control sobre un bloque demasiado corto SIN ERROR y lo ignora en
    silencio ("no error is returned"). El commit habría parecido correcto para
    siempre y no habría cacheado nada. Cualquiera que reabra esto debe tener
    el conteo delante antes de tocar.
    DESCARTADA TAMBIÉN la vía de mover style-check (o el juez) a Sonnet para
    alcanzar el umbral de 1.024: es comprar un descuento subiendo el precio
    base.

  B.95 — [FALSO NEGATIVO CONOCIDO, por diseño] Columnas sinónimas entre tablas
    distintas no se cruzan. R2 (finding-rules.ts) exige que el nombre de la
    columna citada coincida EXACTO entre los dos lados. Si dos tablas nombran el
    mismo concepto de forma distinta —"Puesto" en una y "Oficio" en otra— no
    comparten columna citada y el hallazgo muere con
    descartado.sin_columna_comun, aunque los valores sean genuinamente
    incompatibles ("Higienista" contra "Médico de familia" para la misma
    persona).
    Es un falso negativo SILENCIOSO: el juez sí podría emitirlo —él lee y puede
    ver la incompatibilidad—, y es nuestra regla la que lo mata después.
    NO SE ARREGLA EMPAREJANDO NOMBRES PARECIDOS. Reconocer que "Puesto" y
    "Oficio" significan lo mismo exige entender palabras, que es exactamente lo
    que F-23 y F-26 descartaron dos veces: fue lo que tumbó la primera versión de
    la regla de equivalencia. Aflojar ahí devuelve un sistema que empareja por
    parecido y falla con sinónimos, abreviaturas o columnas en otro idioma.
    POSIBLE VÍA, sin trabajo nuevo: el paso 5 (citas estructuradas {sheet,
    rowKey, cells}) cambia el terreno — el juez pasará a declarar de qué hoja y
    fila viene cada cita en vez de que se deduzca del texto. Revisar entonces si
    el caso encaja solo; no antes.
    DECISIÓN COMERCIAL (director, 23/08): no se aborda en esta versión. Con un
    corpus de ~40 documentos de una clínica no es lo que decide si el producto
    sirve, y si aparece hay respuesta honesta que dar: "el sistema compara
    columnas con el mismo nombre; si tus tablas usan nombres distintos para lo
    mismo, no las cruza". Limitación explicable, no fallo. Anotado para que, si un
    cliente lo reporta, ya esté diagnosticado.

  B.96 — [ABIERTO, 25/08] La misma ficha en el modal de análisis.
    AnalysisModal.tsx:250-263 pinta las contradicciones del modo exhaustivo con
    dos <p> de citas crudas: exactamente la forma que F-70 acaba de retirar del
    modal de mejora. El dato ya llega (comparedValues, newDocRow y
    existingDocRow viajan desde d384a315 y sobreviven al double-check, que es el
    camino del exhaustivo).
    LA PREGUNTA NO ES SI COPIAR ProblemDetail, sino si ESE modal debería
    reutilizarlo. Son dos fichas con propósitos distintos: la de AnalysisModal se
    ve una vez, al terminar un análisis, y decide si indexar; la del modal de
    mejora es donde el cliente convive con los hallazgos y los resuelve uno a
    uno. Puede que la primera quiera ser más escueta, no más rica. Decidirlo
    antes de escribir nada.
    Ojo si se reutiliza: ProblemDetail recibe un `Problem`, no una discrepancia
    de FinalAnalysis, y usa el namespace 'analysis' de next-intl (el mismo que
    AnalysisModal). Y CollapsibleSection de AnalysisModal no está exportado —
    ProblemDetail lleva su propio plegado, copiado del patrón de ChatPanel.
    SIN URGENCIA: el sitio donde el cliente convive con los hallazgos es el de
    mejora, y ese ya está.

  B.97 — [EN OBSERVACIÓN, 25/08] El !== crudo de la capa determinista.
    finding-rules.ts:155 compara los valores de la columna compartida con
    `newCells[c] !== existingCells[c]`, sin normalizar. Dos celdas que difieran
    solo en un espacio de más, un acento o una mayúscula cuentan hoy como
    discrepancia y llegan a la ficha como contradicción confirmada por
    estructura.
    CON CITAS NO SE NOTABA: la ficha volcaba las dos filas enteras y el ojo no
    iba a la diferencia. CON LA FRASE DE F-70 QUEDARÁ EN EVIDENCIA — un par
    visualmente idéntico se leerá "figura como «Implantólogo»; en X, como
    «Implantólogo»", y el cliente pensará que el sistema está roto.
    NO APARECIÓ EN LA TANDA DEL 25/08: los dos hallazgos medidos (las dos
    direcciones de Pablo Reyes) difieren de verdad. Es un riesgo razonado, no un
    fallo observado.
    NO ARREGLAR A CIEGAS. Normalizar aquí es aflojar una regla determinista, y
    F-23/F-26 ya tumbaron dos veces una regla que empezó "tolerando pequeñas
    diferencias" y acabó siendo juicio semántico disfrazado. Si aparece un caso
    real, medirlo primero: cuántos, de qué tipo (espacios, acentos,
    mayúsculas, cero a la izquierda) y si normalizar mata algún verdadero
    positivo. Con el dato delante, decidir. Sin él, no.

    ────────────────────────────────────────────────────────────────────────
    MEDIDO EL 28/08 (F-82 P3). SE ACTUALIZA, NO SE CIERRA. Se preparó el
    commit que hacía a R2 adoptar el comparador de tres niveles de F-82 P2, y
    SE RETIRÓ antes de escribirlo, con estos datos:

    CERO FILAS LO MUEVEN. Sobre los cuatro .xlsx del corpus —1.330 celdas, 26
    columnas— se compararon TODOS los pares de valores de una misma columna:
    10.174 comparaciones, CERO variantes de escritura. Y con la cota más
    ancha posible (los 330 valores distintos contra todos los demás, sin
    exigir misma columna): también cero.

    EL MOTIVO, que es lo que importa y es un límite del corpus y no una
    propiedad del mundo: las variantes de escritura nacen de que DOS PERSONAS
    escriban lo mismo de forma distinta, y estas tablas se generaron
    programáticamente. Con documentos de un cliente real la cifra puede ser
    otra, y probablemente lo sea.

    DOS SUPOSICIONES QUE LA MEDICIÓN DESMINTIÓ, anotadas porque las dos
    parecían razonables:
      · «La comparación es de fila entera, así que una variante en una columna
        enterraría una discrepancia real en otra.» NO. R2 ya compara COLUMNA A
        COLUMNA: `sharedColumns.filter(c => newCells[c] !== existingCells[c])`.
        No hay ningún JSON.stringify en el camino. El riesgo no existe, y
        adoptar el comparador sería sustituir un predicado dentro de ese
        filtro, no rediseñar la capa.
      · «Una fila marcada como equivalentes desaparece.» NO. `reclassify` hace
        `movedToOverlaps.push(...)` (pipeline.ts:353) y eso entra en
        `overlappingContent` (pipeline.ts:452): el hallazgo pasa de
        contradicción a SOLAPAMIENTO. El cliente lo sigue viendo, en otra
        sección.

    POR QUÉ SE RETIRÓ AUN ASÍ: tocar la única capa que sostiene la detección
    tabular desde B.81 sin una sola fila que lo mueva es riesgo sin
    contrapartida. Un test que dice «si existiera una variante, se
    clasificaría así» es una tautología del propio test, no una demostración
    de que el cambio sirve — la condición 1 de la regla de entrada pide un
    caso que FALLE sin el cambio, y ninguno existe.

    QUÉ HACE FALTA PARA REABRIRLO, como condición de entrada y no como deseo:
    AL MENOS UNA FILA REAL, de un corpus de cliente, donde la variante de
    escritura cambie el veredicto. Y el mecanismo que la va a encontrar ya
    existe: `TableDiffCounts.discrepanciasVarianteDeEscritura` (fase 2 del
    diff, commit b735fe33) cuenta exactamente eso. OJO: ese contador NO está
    todavía en el catálogo de counters.ts ni tiene dónde volcarse — hace falta
    resolver B.110 y que la fase 3 lo conecte. Hasta entonces no hay de dónde
    sacar el caso, y una tanda ad hoc no sirve: mediría este corpus otra vez.

    CORRECCIÓN DE REFERENCIA: la línea ya no es finding-rules.ts:155 sino
    :141.
    ────────────────────────────────────────────────────────────────────────

  B.98 — [ANOTADO SIN MEDIR, 25/08] Peso del jsonb con las filas completas.
    Desde d384a315 cada contradicción confirmada por estructura carga DOS filas
    de tabla enteras (newDocRow + existingDocRow) dentro de
    analysis_results.analysis. Una fila de RRHH-06 son ~300 caracteres; una de
    OPE-02, ~210. Con varios hallazgos por análisis y documentos como OPE-06 (94
    filas en su tarifa), eso suma en cada fila persistida.
    SIN MEDIR TODAVÍA. Antes de tocar nada hace falta el dato: pg_column_size
    sobre analysis_results.analysis antes y después de d384a315, y cuántos
    hallazgos por estructura trae un análisis típico. Puede que sea irrelevante.
    Si resultara que no lo es, la salida NO es dejar de mandarlas —la ficha las
    usa— sino no persistirlas: son contexto de presentación, no hallazgo, y la
    bandeja las relee del jsonb precisamente porque ahí están.

  B.99 — [COBRO INDEBIDO, 26/08] Si el pipeline lanza una EXCEPCIÓN, los créditos
    no vuelven. Distinto de lo que arregló 38d3fd22: aquello eran FALLBACKS (la
    etapa atrapa el error y sigue con un valor por defecto); esto es un THROW que
    se propaga y aborta el análisis entero.
    RÁPIDO: el catch de app/api/analyze-v2/route.ts:550 hace tres cosas —
    console.error, logUsage(success:false, creditsConsumed) y responder 500. No
    llama a refundCredits. El finally (:570) solo libera el semáforo.
    EXHAUSTIVO: el catch del worker marca el job 'failed' y tampoco reembolsa.
    Ya estaba anotado como gotcha en CLAUDE.md, o sea que se sabía.
    LA VENTANA DEL RÁPIDO SON 230 LÍNEAS: se cobra en :159 y el reembolso de
    F-71 está en :392. En medio caben la descarga de Storage con sus tres
    reintentos, extractSegments (donde caen los "bad XRef" de B.39), chunkText y
    la lectura de chunks persistidos. Cualquiera de ellos lanza y el cliente paga.
    ES PEOR QUE LO QUE ARREGLÓ 38d3fd22: allí el cliente recibía un informe
    engañoso; aquí no recibe nada y paga igual.
    DOS PREGUNTAS QUE HAY QUE DECIDIR ANTES DE TOCAR:
      1. ¿Se devuelve en TODO el tramo posterior al cobro, o solo si la excepción
         ocurre después del pipeline? Devolver siempre es lo honesto con el
         cliente, pero convierte el catch en un punto de escritura de créditos
         que corre también cuando el fallo es suyo (fichero corrupto, texto
         insuficiente) — aunque esos casos hoy salen por `return`, no por throw.
      2. ¿El catch del worker hace lo propio? Ahí el job ya está marcado
         'failed' y credits_consumed está en la fila, así que el dato para
         devolver existe; falta decidir si el worker es el sitio o si debe
         hacerlo un barrido aparte sobre jobs 'failed' con credits_consumed > 0.
    Cruzar con B.100 antes de implementar: cuantos más caminos de reembolso,
    más ocasiones de que muerda la falta de atomicidad.

  B.100 — [PREEXISTENTE, agravado por F-71, 26/08] refundCredits no es atómico.
    lib/credits.ts hace read-modify-write sobre organizations.credits_extra: lee
    el valor, suma y escribe. El COBRO, en cambio, es la RPC atómica
    consume_credits — asimetría que CLAUDE.md ya declara doctrina ("nunca
    decrementar créditos directamente: peticiones paralelas pueden sobregirar"),
    y que el reembolso incumple.
    QUÉ PUEDE PASAR: un cobro concurrente de otro endpoint (p. ej. /api/ask, 1
    crédito, sin semáforo de análisis) entre el read y el write de un reembolso
    pisa el reembolso, o al revés. Dos reembolsos entre sí son menos probables —
    el semáforo de concurrencia por organización separa los análisis— pero el
    semáforo protege el ANÁLISIS, no la escritura de créditos: el reembolso del
    worker ocurre después de marcar el job, y el del rápido dentro del try.
    NO LO INTRODUCE 38d3fd22: los dos reembolsos del worker (reanálisis con
    pocos hallazgos y precio variable) llevan con este defecto desde que
    existen. Lo que cambia es la frecuencia — ahora hay un tercer camino de
    reembolso y se dispara con cada fallo del proveedor.
    LO QUE HARÍA FALTA: una RPC `refund_credits(p_org_id, p_amount)` con
    `UPDATE organizations SET credits_extra = credits_extra + p_amount WHERE
    id = p_org_id`, atómica como su gemela, y que refundCredits pase a llamarla.
    Es SQL en Supabase antes del push, como consume_credits.
    Cruzar con B.99: si se añade el reembolso en los dos catch, este pasa de
    deuda latente a defecto con tres llamadores.

  B.101 — [REGLA DUPLICADA, 26/08] El veto del exhaustivo se comprueba por
    NOMBRE DE PLAN, y en dos sitios. El backend lo hace en
    app/api/analyze-v2/route.ts:141 (`if (orgPlan?.plan === 'free')` → 403), y
    desde ff652755 el frontend hace el espejo en
    app/(authenticated)/settings/review/page.tsx:41
    (`credits.plan !== 'free'`) para no ofrecer un botón de 30 créditos por
    documento a quien va a recibir un 403.
    NO LO INTRODUCE ff652755: el backend lleva comparando el plan a pelo desde
    que existe el exhaustivo. Lo que ese commit añade es el segundo sitio, y con
    él el problema real — una regla viviendo en dos lugares se rompe CALLADA el
    día que cambien los planes: se añade un plan nuevo sin exhaustivo, o 'free'
    pasa a llamarse de otra forma, y una de las dos mitades se entera y la otra
    no. El síntoma sería un botón ofrecido que devuelve 403, o uno escondido a
    quien sí puede pagarlo.
    CONTRADICE LA DOCTRINA QUE EL PROPIO PROYECTO YA TIENE ESCRITA. CLAUDE.md,
    en la sección de créditos y facturación: "Use getOrgFeatures(supabase,
    orgId) from lib/plan-features.ts to gate Drive, analytics panel, and
    variable pricing — never hardcode plan names in feature checks". Y en las
    reglas de trabajo: "Always gate Drive and analytics panel features via
    getOrgFeatures(), never hardcode plan names". Drive y el panel de analíticas
    SÍ tienen su flag (hasDrive, hasAnalyticsPanel); el exhaustivo es la
    excepción, y por eso es el único que se compara por nombre.
    LO QUE HARÍA FALTA, y son tres piezas pequeñas en el mismo commit:
      1. `hasExhaustive` en PLAN_FEATURES (lib/stripe.ts:68) y por tanto en lo
         que devuelve getOrgFeatures (lib/plan-features.ts:36).
      2. Exponerlo en /api/usage/summary, junto a hasAgent y hasAnalyticsPanel,
         y añadirlo a AccountFeatures (contexts/types.ts:37) — que ya declara
         hasDrive "for forward-compatibility", así que el patrón está hecho.
      3. Que los DOS lados pregunten por él: el endpoint sustituye su consulta
         a organizations.plan por getOrgFeatures, y la bandeja lee
         features.hasExhaustive en vez de credits.plan.
    Mientras no exista, el espejo se queda: ofrecer algo que el usuario no puede
    comprar es peor que duplicar una comparación. El comentario de la prop
    `planAllowsExhaustive` en components/review/ReviewSelectionBar.tsx:25-37 ya
    apunta a este pendiente.

  B.102 — [ABIERTO, 27/08] El tope de 25 piezas se come el presupuesto antes que
    los caracteres. MAX_FRAGMENTS_PER_DOC_QUICK = 25
    (lib/analysis/retrieval.ts:97) es una "red de seguridad" contra documentos
    con miles de fragmentos diminutos, y hoy limita a los DOS modos: es la única
    constante del reparto que no se parametrizó en F-73.
    APARECE EN DOS FRENTES A LA VEZ, y por eso es pendiente propio:
      · F-65 (descarte de ajenas). El caso que se creía de referencia, OPE-06,
        no lo es porque su tabla son 94 filas + resumen = 95 piezas: topa en 25
        con cualquier presupuesto, así que descartar ajenas no libera nada
        medible ahí. Ver claude/Descarte_Filas_Ajenas.md §6.
      · La medición de "destilado y grande" (la esquina sin medir de la tanda
        del 27/08, ver claude/Tandas_Harness.md). El nivel 1 exige
        `level1Count <= remainingCount`, así que una tabla de más de ~24 filas
        NO puede alcanzarlo por mucho presupuesto que se le dé. Medir esa
        esquina sin tocar el tope mide el tope, no la destilación.
    EL CASO QUE LO HACE URGENTE, y no es teórico: 15-20 discrepantes REALES en
    la misma tabla se comen el tope antes que el presupuesto. Ahí el descarte de
    ajenas no ayuda —las ajenas ya no estaban entrando— y el sistema recorta
    hallazgos legítimos por una constante escrita contra otro problema.
    NO SE TOCA A CIEGAS: subirlo cambia lo que un modelo lee, así que entra la
    regla de F-59/F-61 y exige su tanda. Y antes de subirlo hay que decidir si
    el tope debe contar PIEZAS o algo más cercano a lo que protege (fragmentos
    diminutos), porque una tabla de 40 filas cortas y un documento de 40
    fragmentos de ruido no son el mismo riesgo.

  B.103 — [DEUDA MEDIDA, 28/08] El double-check no copia confirmedBy: cuando
    Sonnet no confirma, la procedencia del hallazgo DESAPARECE. Y contamina una
    columna de la base de datos desde que existe.
    EL MECANISMO. verifyBatch (lib/analysis/double-check.ts) reconstruye el
    objeto con lista cerrada y `d.confirmedBy` NO se copia nunca; el campo solo
    se escribe en una rama:
        ...(isContradiction ? { confirmedBy: 'double_check' as ConfirmedBy } : {}),
    Con lo cual:
      · Sonnet confirma  → confirmedBy: 'double_check', PISA al anterior.
      · Sonnet NO confirma → confirmedBy AUSENTE: el 'juicio' se pierde.
    El catch del mismo fichero tampoco lo copia. La única vía que sí lo conserva
    es el push de las de huella ya descartada, que usa spread — inconsistente
    con las otras dos, y por accidente, no por decisión.
    LA CONTAMINACIÓN DE LA MÉTRICA, que es lo que lo hace deuda y no manía:
    lib/persist-analysis.ts:52 cuenta
        contradictions_confirmed: analysis.discrepancies.filter(d => d.confirmedBy !== undefined).length
    así que un hallazgo degradado por Sonnet TAMBIÉN sale de esa cuenta. La
    columna analysis_results.contradictions_confirmed no mide "confirmadas":
    mide "lo que Sonnet no tumbó", y lleva haciéndolo desde que el double-check
    existe. Cualquier análisis histórico sobre esa columna arrastra el sesgo.
    ALCANCE TRAS LA FRONTERA DE F-71 PASO 3: los confirmedBy 'estructura' ya NO
    pasan por esas ramas —no se envían a Sonnet—, así que a ellos deja de
    tocarles. SIGUE VIVO PARA LOS DE 'juicio', que son los únicos que quedan
    yendo al double-check.
    NO SE ARREGLÓ EN F-71 PASO 3 a propósito: aquel commit fijaba una frontera,
    y abrir además la lista cerrada habría mezclado dos cambios de
    comportamiento en el mismo diff. Cruzar con B.97 y B.98 al tocar
    double-check.ts.

  B.104 — [AFINAR CUANDO EXISTA F-65, 28/08] El aviso de alcance dice cuántas
    filas quedaron fuera, no cuántas eran DISCREPANTES.
    LO QUE PIDIÓ FABLE en F-74 P2 era el contador con clasificación —
    "seleccion.discrepantes_fuera: N" y el aviso "N filas discrepantes no
    mostradas por tamaño"—. Lo desplegado cuenta las filas RECUPERADAS que no
    entraron, a secas, sin decir si eran interesantes.
    POR QUÉ NO SE HIZO HOY, y el motivo es el que importa: sin el predicado de
    F-65 (claude/Descarte_Filas_Ajenas.md) la aproximación disponible es
    "cruza y no es idéntica" (crossingsByIndex > 0 en restRows), y esa falla en
    los DOS sentidos:
      · POR ARRIBA: una fila que solo cruza en una columna trivial (Clínica:
        Chamberí, cardinalidad ~1) contaría como discrepante siendo ajena.
      · POR ABAJO, y este es el que decide: el caso de los totales — "Total
        horas equipo/semana: 256" contra "248" — comparte columna y DIFIERE en
        el valor, pero countCrossings cuenta COINCIDENCIAS y devuelve 0. La
        llamaría ajena y el aviso callaría justo el tipo de fila que más
        importa no perder.
    Un aviso que se equivoca en el sentido que OCULTA es peor que no avisar,
    así que se desplegó la versión que no depende del predicado: honesta,
    medible hoy, y sin afirmar de más.
    QUÉ HACER CUANDO F-65 ESTÉ: añadir el desglose sobre lo que ya viaja —
    SelectionLimit (lib/analysis/types.ts) ya lleva rowsLeftOut y rowsRecovered
    por tabla, así que basta un tercer número y ajustar el texto del aviso en
    components/SelectionLimitNotice.tsx. El camino arriba ya está abierto.
    CRUZAR CON B.102: el tope de 25 piezas es hoy la otra vía por la que se
    queda material fuera, y los casos D (ni el resumen cupo) y E (la unidad ni
    se intenta) siguen contando solo en unitsOut, sin clave propia.

  B.105 — [REGLA ABIERTA, 27/08] Una batería sobre un PROMPT tiene que
    EJECUTARSE contra el modelo, no trazarse a mano.
    LA BATERÍA DE 87db03cc (verificador de hallazgos, la llamada corta) se
    trazó, en palabras de su propio mensaje de commit, "a mano, sin llamar al
    LLM": once casos —los siete falsos positivos de F-22 en sus tres patrones,
    los dos aciertos vivos (Puesto de Pablo Reyes y conservación 15/5 años), el
    caso mixto de las 44 horas y un lote con respuesta incompleta simulada—, y
    su conclusión fue "ninguno reveló falta de información en el prompt".
    ESA CONCLUSIÓN ES CIERTA Y ES INSUFICIENTE. "El prompt CONTIENE la
    información para decidir" y "un modelo delante de este prompt DECIDE bien"
    son dos afirmaciones distintas, y solo la primera estaba medida. Trazar a
    mano vale para verificar código determinista; no vale para verificar una
    instrucción que lee un LLM, porque el trazador es quien decide, no el
    modelo.
    ES LA CAUSA DE QUE F-77 LLEGARA A PRODUCCIÓN. En el fallo del 26/08 sobre
    87a76112 la información estaba entera delante del modelo —las dos citas
    correctas, con "no el Director Clínico" explícito en la de CLI-12— y
    respondió mismo_dato_sin_oposicion igual. Una batería trazada a mano no
    podía cazarlo: trazándolo a mano, ese caso se decide bien.
    LA OBJECIÓN QUE YA NO VALE: hasta hoy faltaba corpus de prueba versionado
    para poder ejecutar una batería de verdad. Desde los commits de corpus del
    27/08 los ONCE documentos del harness están en corpus-pruebas/ —los cinco
    del piloto, los cuatro del ampliado y los dos del control NOR-11/CLI-13—,
    con su registro de siembra. La regla de admisión del protocolo del harness
    (claude/Protocolo_Harness_Tasas.md) se cumple. No queda excusa material.
    QUÉ QUEDA POR DECIDIR: cómo se ejecuta. Los once documentos dan el ground
    truth, pero no hay todavía forma de lanzar una batería de prompt sin pasar
    por un análisis completo desde la bandeja. Decidir si se hace así —caro y
    lento pero real— o con un arnés que llame a verifyFindings con hallazgos
    construidos a mano y citas sacadas de los documentos del repo.
    APLICA YA AL BLOQUE DE F-77 (commit del 27/08, lib/analysis/verify-findings.ts):
    se escribió con el ground truth delante y comprobado contra los siete de
    F-22 uno por uno, que es más de lo que tuvo 87db03cc, pero NO se ha
    ejecutado contra el modelo. Mientras no haya tanda, no se puede afirmar que
    arregle el 0 de 3 de prosa.
    Y UNA ADVERTENCIA PARA CUANDO SE MIDA: el ejemplo del bloque es "mismo rol,
    dos personas", que es el mismo objeto que la siembra A. Si la siembra A pasa
    a detectarse, eso NO prueba que el bloque generalice — prueba que el modelo
    aplica un ejemplo casi idéntico. La parte no circular son las siembras B
    (frecuencia semanal/mensual) y C (plazo 6/12 meses).
    [CONTESTADO 27/08 por el caso de control NOR-11/CLI-13: dos superficies
    nuevas —un plazo (72 h vs 7 días) y un topónimo (Chamberí vs Retiro)—
    confirmadas, ninguna sobre roles. El bloque generaliza POR MECANISMO y la
    circularidad queda descartada para el verificador. Detalle en
    claude/Tandas_Harness.md. Lo que sigue abierto es el resto de B.105: la
    batería como tal sigue sin ejecutarse contra el modelo de forma sistemática;
    una tanda de producción no es una batería.]

  B.106 — [MEDIDO 27/08, SIN DIAGNOSTICAR] El juez emite UNA SOLA contradicción
    por par de documentos.
    LA MEDIDA: en el caso de control NOR-11/CLI-13 —documentos de 4 y 5
    páginas, los más pequeños del corpus, con TRES contradicciones sembradas—
    cada análisis devuelve exactamente "1 contradicciones" en el log crudo del
    juez, ANTES de cualquier filtro de la cascada. Y cada dirección devuelve una
    DISTINTA. Nunca dos.
    NO ES LA SELECCIÓN, y esto es lo que lo hace interesante: entran 4
    fragmentos de 16 y 4 de 11, y el descarte de un solapamiento cita
    literalmente "contenedor negro habilitado en cada gabinete" — es decir, el
    material de la TERCERA contradicción estaba delante del juez y no lo emitió
    como contradicción. No se puede echar la culpa a lo que no llegó.
    HIPÓTESIS A INVESTIGAR, ninguna elegida todavía:
      · Que el prompt del juez empuje a devolver EL hallazgo más claro en vez de
        TODOS los que encuentre.
      · Que una negación categórica en párrafo separado ("nunca en el contenedor
        negro") sea más difícil de emparejar que dos cifras enfrentadas, que se
        alinean solas.
      · Que el overlapPercent que el juez calcula primero acote implícitamente
        cuánto sigue buscando después.
    POR QUÉ IMPORTA: es un techo DISTINTO del de la selección (F-76) y aparece
    en el caso MÁS FÁCIL del corpus. Si con cinco páginas y tres siembras emite
    una, en documentos de dieciocho páginas el problema no es solo qué llega al
    prompt: también cuánto devuelve el juez de lo que sí llegó. Los dos techos
    se multiplican, y hasta hoy solo se estaba mirando el primero.
    DÓNDE MEDIR MÁS: NOR-11/CLI-13 es el par barato para esto — tres siembras,
    documentos pequeños, todo el material entra. Contar cuántas emite el juez
    crudo en cuatro pasadas por dirección antes de tocar nada.

  B.107 — [DOS FALLOS MEDIDOS 27/08, UNA SOLA ENFERMEDAD — doctrina F-80] La
    cita literal la escribe el MODELO, y por eso se rompe de dos maneras.
    FALLO 1 — LA GLOSA ENTRE CORCHETES. El juez cita "...recae siempre sobre
    esta figura [Director Clínico]": añade una aclaración propia para
    desambiguar un pronombre, en beneficio del lector, y la verificación
    literal la rechaza porque esos corchetes no están en el documento. DOS de
    CUATRO contradicciones de una misma pasada murieron así.
    FALLO 2 — LA NARRACIÓN QUE ESQUIVA EL DETECTOR. El juez escribe "No se
    especifica un criterio de caducidad de 12 meses en el fragmento disponible
    del protocolo NOR-10". El prompt lo prohíbe con esas palabras ("Prohibido
    escribir cosas como... 'Este documento no menciona...'") y existe
    NARRATION_PATTERNS (judge.ts) para cazarlo, pero sus tres patrones exigen
    "fragmento[N]", "el corpus" o "el documento nuevo": "el fragmento
    disponible" no casa con ninguno. Pasa el detector y muere después en
    verifyQuote como citaNoVerificable — con el NOMBRE EQUIVOCADO: no es que la
    cita no se localice, es que no era una cita.
    LA DOCTRINA (Fable, F-80): son la misma enfermedad. El error de diseño es
    que el TEXTO de la cita lo escriba el modelo. Mientras lo escriba, habrá una
    familia infinita de formas de escribirlo mal, y cada parche cubre una.
    SOLUCIÓN ESTRUCTURAL: citas POR REFERENCIA — el modelo devuelve
    {fragmentId, ancla} y el CÓDIGO extrae el texto del documento. La cita deja
    de poder ser incorrecta porque deja de ser generada. Existe además la API de
    Citations de Anthropic, que da offsets garantizados por el servidor y no
    contados por el modelo; evaluar si encaja antes de construir lo propio.
    MÓDULO INTERINO, si hace falta antes: una normalización de BÚSQUEDA DE
    CITAS (solo findBestMatch, nunca el normalize global — regla de F-46) que
    agrupe las glosas entre corchetes y el quince/15 de F-77, con batería
    conjunta. ETIQUETADO COMO DESTINADO A MORIR cuando lleguen las citas por
    referencia: es andamio, no cimiento, y hay que escribirlo sabiéndolo.
    RECHAZADO EXPLÍCITAMENTE: ampliar NARRATION_PATTERNS con más patrones. Es
    perseguir síntomas —la lista nunca estará completa— y con citas por
    referencia el detector entero se vuelve innecesario, porque el modelo ya no
    escribe texto que pueda narrar.
    CRUZAR CON F-77 commit 1 (la normalización del quince/15), que quedó sin
    hacer: es la misma pieza que el módulo interino, y hoy sabemos que por sí
    sola no movía ninguna de las tres siembras de prosa.

    VISTO EN PRODUCCIÓN EL 30/08, y es la primera vez: hasta hoy este pendiente
    tenía casos CONSTRUIDOS y ninguna aparición medida. En las dos pasadas
    rápidas de OPE-11 contra OPE-10, el juez escribió «No aparece EST-03 en el
    documento nuevo» donde debía ir una CITA — el FALLO 2, la narración— y el
    detector lo descartó bien.
    CONFIRMA LAS DOS MITADES: que el juez narra en vez de citar, y que el
    detector lo caza. El fallo es real y no una posibilidad teórica.
    Registrado también en claude/Tandas_Harness.md, entrada del 30/08.
    ⚠️ CUARTA FORMA, MEDIDA EL 01/09 — Y LA ÚNICA CON CAUSA NUESTRA: EL JUEZ
    COPIA LA ETIQUETA DE LA FILA DENTRO DE LA CITA.
    En la tanda de la siembra, cuatro solapamientos murieron por «cita no
    verificable, lado=ambos» con las dos citas IDÉNTICAS entre sí:
        nuevo    = "[F0] Dra. Marta Gil | Chamberí | Cirugía | Mañana | 35"
        existente= "[F0] Dra. Marta Gil | Chamberí | Cirugía | Mañana | 35"
    LA CAUSA, verificada en el código: `renderTableRow` (table-structure.ts:171)
    prefija cada fila con `[F{rowIndex}] ` para enseñársela al juez. El juez la
    copió ENTERA, etiqueta incluida — o sea que fue MÁXIMAMENTE FIEL— y
    `alignQuoteToCells` la rechaza por eso: parte por `|` y compara segmento a
    segmento, así que «[F0] Dra. Marta Gil» no casa con la celda «Dra. Marta
    Gil». Falla UN segmento de cinco y la función devuelve null.
    POR QUÉ ES DISTINTA DE LAS OTRAS TRES: la glosa, la narración y la
    enumeración son cosas que el modelo hace de su cosecha. Ésta no: nosotros
    etiquetamos la línea y él copió la etiqueta. LA FIRMA ES INCONFUNDIBLE —
    falla exactamente un segmento, siempre el primero, siempre por el prefijo.
    Y NO NECESITA EL FRENTE 4: las otras tres esperan a las citas por
    referencia; ésta se cura quitando el prefijo antes de alinear. No se arregla
    hoy porque toca el camino del juez y hay medición en curso, pero conviene
    saber que NO comparte destino con las otras tres.
    LA FRASE QUE LA SEPARA DE LAS OTRAS TRES, y conviene citarla entera:
    COPIÓ LA FILA EXACTAMENTE COMO SE LA ENSEÑAMOS Y LO PENALIZAMOS.
    La glosa, la narración y la enumeración son cosas que el modelo añade de su
    cosecha; ésta es obediencia castigada. Cualquier arreglo que se plantee para
    las otras tres —enseñarle a citar mejor— aquí sobra: el modelo ya citó
    perfecto.

    ══ CANDIDATO A ARREGLARSE ANTES DEL FRENTE 4 ══
    No lo necesita: las otras tres esperan a las citas por referencia, y ésta se
    cura quitando el prefijo `[F\d+] ` antes de alinear. Arreglo pequeño, causa
    nuestra, y devuelve hallazgos que hoy se tiran.
    NO SE HACE HASTA SABER CUÁNTO PESA. La decisión del director es medir
    primero, y con razón: el dato que hay es de UN par de documentos.

    ══ LO QUE SE SABE DE SU FRECUENCIA, con las cifras exactas ══
    ⚠️ EN LA SIEMBRA FUE 4 DE 4, NO 4 DE 5. Contado del log: la pasada de
    OPE-13 emitió 3 solapamientos y descartó los 3; la de RRHH-08 emitió 1 y
    descartó 1. CIEN POR CIEN de los solapamientos del par, las dos direcciones.
    PERO NO SE HA VISTO EN NINGÚN OTRO PAR, y eso es la mitad importante del
    dato:
      · OPE-10 / OPE-11 (31/08): 5 solapamientos por pasada, muertos por
        `citaDeContexto` — el juez citaba LA LÍNEA DE CONTEXTO de las filas
        colapsadas, no una fila con prefijo. Causa distinta.
      · RRHH-06 / OPE-02 (31/08): un `lado=ambos`, pero con las dos citas
        CONCATENADAS a mano por el juez —la forma de enumeración— sin ningún
        `[F`. Causa distinta.
    LUEGO LA FORMA DEL PREFIJO SOLO HA APARECIDO DONDE LAS FILAS SON CASI
    IDÉNTICAS entre los dos documentos, que es lo que empuja al juez a emitir
    solapamientos citando FILAS ENTERAS. Extrapolar el 100% al corpus de tablas
    sería exactamente el error que el protocolo prohíbe: una tasa de un par no
    es una tasa.

    ══ CÓMO MEDIRLO, cuando toque ══
    Contar, sobre las tandas de pares TABULARES, cuántos `citaNoVerificable`
    con `lado=ambos` llevan una cita que empieza por `[F` y cuántos no. El
    prefijo hace la firma inconfundible, así que la cuenta es mecánica y no
    necesita juicio: si empieza por `[F` y falla exactamente un segmento, es
    ésta.

  B.108 — [ENCONTRADO 27/08, DOS CAUSAS Y SOLO UNA ES DEL PROYECTO] El build
    local no se puede ejecutar, así que no da veredicto antes de un push.
    Reproducido con el árbol limpio antes de instalar vitest: NO lo introduce
    esa infraestructura.
    CAUSA 1 — SIN CI=true, la fase de lint se para en un prompt interactivo. No
    hay configuración de ESLint en el repositorio y next lint pregunta cómo
    configurarlo ("How would you like to configure ESLint?") dentro de un
    worker que lo mata (exit 3221226505). Es del proyecto y tiene su propio
    punto: B.109.
    CAUSA 2 — CON CI=true el prompt se salta, el type-check pasa limpio, y
    muere después en "Generating static pages" con JavaScript heap out of
    memory (exit 134). Sube el heap a 6 GB con NODE_OPTIONS y muere igual. Es
    RAM de la máquina, no del proyecto: AQUÍ NO HAY NADA QUE ARREGLAR.
    CONSECUENCIA, que es lo que lo hace pendiente: el único gate real antes de
    un push es npm run typecheck. El build solo se verifica en Vercel, DESPUÉS
    del push, que es al revés de lo que conviene. Un error que typecheck no
    caza —un import roto en tiempo de build, una página que no compila— llega a
    producción y se descubre por el correo de Vercel.
    CRUZAR CON LA INFRAESTRUCTURA DE VITEST (commit bf95dc5a): la suite corre
    en local y da veredicto; el build no. Son dos huecos distintos y solo uno
    está tapado.

  B.109 — [PREEXISTENTE, encontrado 27/08] "lint" figura en package.json y no
    es una puerta. next lint está declarado como script pero el repositorio no
    tiene NINGUNA configuración de ESLint, así que lanza un asistente
    interactivo en vez de comprobar nada.
    No es de hoy: lleva así desde que existe el script, y cualquiera que lo lea
    en package.json creerá que hay linting. CLAUDE.md lo documenta además como
    "Lint + type-check".
    DOS SALIDAS Y NINGUNA ELEGIDA:
      · Configurar ESLint de verdad. Decisión con alcance propio, porque un
        linter mal calibrado sobre código existente produce cientos de avisos
        que nadie va a atender.
      · Retirar el script y dejar constancia de que este proyecto no tiene
        linter. Es honesto y cuesta una línea.
    LO QUE NO VALE ES DEJARLO COMO ESTÁ: declara una garantía que no existe,
    que es la misma familia que contradictions_confirmed midiendo "lo que
    Sonnet no tumbó" (B.103).

  B.110 — [ABIERTO 27/08, LO EXIGE LA REGLA DE ENTRADA] La condición 3 de la
    regla de entrada no tiene dónde cumplirse. Esa regla (protocolo,
    claude/Protocolo_Harness_Tasas.md, 27/08) exige que todo cambio deje
    CONTADOR EN PRODUCCIÓN: cuántas veces actúa. Hoy no hay sitio para eso.
      · logUsage registra endpoint, modelo, tokens y latencia
        (lib/usage-logger.ts, una fila por llamada a endpoint). No es una pieza
        del pipeline actuando.
      · analysis_results guarda el resultado del análisis. discardedFindings
        cuenta hallazgos que se DESCARTAN, no piezas que se EJECUTAN.
      · Y discardedFindings ya arrastra su propia mezcla: descartes, recuentos
        de camino y averías bajo el mismo nombre — 'confirmado.por_estructura'
        y 'confirmado.por_juicio' viven dentro de un campo llamado
        "descartados". Ver el pendiente del renombrado, abierto desde la
        SESIÓN 47 (Bitacora_Sesiones.txt, 24/08) y todavía sin cerrar.
    EL RIESGO CONCRETO, que es lo que lo hace pendiente y no nota: el primer
    cambio que invoque la regla tendrá que decidir dónde va su contador BAJO LA
    PRESIÓN DE QUERER COMMITEAR. Así se acaba con un contador por cambio, cada
    uno en un sitio, y ninguna forma de leerlos juntos — que es exactamente el
    estado del que discardedFindings viene.
    QUÉ HARÍA FALTA, sin elegir: decidir si los contadores de incidencia viven
    en el jsonb del análisis, en una tabla propia, o en el sistema de
    observabilidad que la deuda técnica del proyecto lleva pidiendo desde el
    principio. Y sobre todo, decidirlo ANTES de que llegue el primer cambio que
    los necesite.
    CRUZAR con la regla de entrada, que es quien lo exige, y con el pendiente
    del renombrado de discardedFindings.
    CONFIRMADO POR LA FASE 1 (commit fa22ca4e, 27/08): KeyCounts ya existe, con
    los contadores del consenso —candidatasSimples, candidatasCompuestas,
    consensoVacuo, biyectivas, el reparto en pares/soloNueva/soloExistente, las
    cinco clases de ambigüedad y discrepanciaPorNormalizar—, y NO TIENE DÓNDE
    VOLCARSE. Hoy no molesta porque la fase 1 no tiene
    consumidores: nadie la llama, así que nadie tiene que decidir dónde va su
    log. La fase 3 va a chocar con eso, y ESE es el momento en que la decisión
    de este punto deja de ser aplazable.
    AGRAVADO POR F-91 (30/08): AHORA HAY UN CENTINELA EN LA BOLSA LIBRE. La
    clase de contadores CENTINELA —los que vigilan un invariante declarado y
    cuyo cero es su forma de funcionar— quedó escrita en el contrato con dos
    miembros fundacionales, y SOLO UNO está en COUNTER_CATALOGUE:
    verificador.confirmados_por_estructura. El otro, r2.sin_ancla, vive en
    DiscardedFindings, o sea en bumpCount(counts, key: string) — la bolsa de
    cadena libre que este mismo punto describe.
    POR QUÉ ES PEOR QUE UN CONTADOR CORRIENTE EN EL MISMO SITIO: un centinela
    sin garantía de transporte puede DESAPARECER de una fusión sin que nadie se
    entere, y desaparecer es indistinguible de valer cero — que es justo lo que
    se lee como «funcionando». Un contador normal perdido se echa de menos
    cuando alguien va a mirarlo; un centinela perdido no se echa de menos nunca.
    NO SE ARREGLÓ EN SU COMMIT, y con razón escrita: el vocabulario entero de la
    cascada (descartado.*, a_juicio.*) vive en esa bolsa, así que mover un solo
    nombre al catálogo dejaría la cascada contando en dos sitios. La decisión de
    este punto es la que desatasca eso, no un parche por nombre.

  B.111 — [LÍMITE DE EVIDENCIA, abierto 27/08 con la fase 1] El consenso del
    diff SOLO está validado con casos construidos. Ninguno de los dos pares
    reales del corpus lo ejercita: OPE-10/OPE-11 tiene candidatas biyectivas
    —Código y Tratamiento, que no pueden discrepar— y OPE-02/RRHH-06 tiene una
    sola (Empleado). Los once casos que validan el mecanismo central son
    literales dentro de lib/analysis/table-key.test.ts, escritos a propósito.
    El 35/0 de OPE-10/OPE-11 no es el consenso funcionando: es el consenso sin
    nada que hacer.
    NO ES UN DEFECTO DEL CORPUS: dos candidatas que discrepan es un accidente
    en un Excel real, y no se puede sembrar sin fabricar un documento con un
    renombrado de columna a propósito.
    QUÉ MIRAR CUANDO LLEGUE UN CORPUS DE CLIENTE, y es lo PRIMERO que hay que
    mirar: cuántas veces saltan candidatas_discrepan, candidatas_parciales,
    valor_repetido_enfrente y valor_repetido_en_su_tabla.
      · Si NUNCA saltan, el consenso es maquinaria que no actúa y habrá que
        decidir si se conserva.
      · Si saltan MUCHO, la regla («se conserva lo que coincide bajo todas»)
        es demasiado estricta, y eso también es dato.
    CRUZAR con la condición 3 de la regla de entrada (protocolo,
    claude/Protocolo_Harness_Tasas.md), que es quien exige el contador, y con
    B.110, que es donde ese contador todavía no tiene sitio.

  B.112 — [PREEXISTENTE, encontrado 28/08 al explorar F-82] supabase-setup.sql
    está desfasado: quien reconstruya la base desde ese fichero obtiene una
    tabla que el código no puede usar.
    SU CREATE TABLE public.analysis_results (línea 392) NO incluye dos columnas
    que llevan meses en la base de producción:
      · `analysis jsonb` — añadida por supabase-analysis-jsonb.sql (B.5,
        ejecutada el 13/07/2026). Es la que guarda el FinalAnalysis entero y de
        la que vive la bandeja de revisión.
      · `document_id` — añadida por B.1. Es el match preciso de
        app/api/documents/[id]/analysis/route.ts.
    Sin las dos, saveAnalysisResult falla al insertar y la bandeja no tiene de
    dónde leer.
    NO ES DE HOY y no lo introduce F-82: el patrón del repositorio es que cada
    cambio de esquema vive en su propio supabase-*.sql como registro de lo ya
    ejecutado, y setup.sql quedó como la foto del día que se escribió. El
    problema no es tener ficheros de ALTER —eso está bien— sino que setup.sql
    se PRESENTA como el esquema completo y no lo es.
    QUÉ HARÍA FALTA, sin elegir: o se regenera setup.sql desde la base real, o
    se le pone una cabecera que diga que es la foto inicial y que hay que
    aplicar los supabase-*.sql posteriores en orden, con la lista. Lo segundo
    cuesta diez líneas y es honesto; lo primero es mejor y hay que hacerlo con
    la base delante.
    OJO AL ARREGLARLO: la lista de ALTERs posteriores hay que sacarla de los
    ficheros, no de memoria — el mismo criterio de la regla de admisión. Y
    F-82 va a añadir uno más (pipeline_counters).

  B.113 — [MANTENIMIENTO, anotado 28/08] El protocolo del harness ha vuelto a
    cruzar las 400 líneas: claude/Protocolo_Harness_Tasas.md son 403 desde que
    F-82 le añadió el puntero al contrato de contadores.
    ACTUALIZADO 28/08 (tarde): son 466 líneas — el protocolo creció otra vez con
    la CUARTA plantilla de cierre (F-84). La cifra de arriba es la del momento
    en que se anotó; el patrón es lo que importa, pero que el número no mienta.
    ACTUALIZADO 31/08 (cierre del frente 1): son 729 LÍNEAS. En tres días ha
    pasado de 466 a 729 —un 56% más— y otra vez por la misma razón buena: el
    frente 1 promovió SEIS reglas nuevas. La regla de cierre (F-89), la de la
    doble lectura (F-91), la guarda de red en el §1-bis (B.126), y de F-92 las
    tres piezas por regla dictada, la ambigüedad de alcance del cuantificador y
    el «cero en N» con su tasa.
    LA CIFRA SE ACTUALIZA AQUÍ CADA VEZ QUE SE MIDE, y no es burocracia: este
    pendiente es un vigilante, y un vigilante con el número viejo es
    exactamente el fallo que lo hizo nacer.

    ══ EL CORTE NATURAL, IDENTIFICADO EL 31/08 — no se ejecuta, se deja escrito ══
    El 28/08 la pregunta del corte era «separar el método de las tandas de las
    reglas de entrada y admisión». HA CAMBIADO, porque el material ha cambiado:
    el frente 1 metió en el protocolo un bloque que el 28/08 no existía.

    HAY DOS ESPECIES DE REGLA AHÍ DENTRO, Y NO SE PARECEN:
      · REGLAS DE MEDICIÓN — cómo se lanza una tanda, qué se apunta, cuántas
        pasadas hacen falta, qué invalida una pasada, el alcance de Vitest, la
        lista de cierre. Hablan de CÓMO SE MIDE.
      · REGLAS DE ESPECIFICACIÓN — la doble lectura (F-91), las tres piezas por
        regla dictada y la ambigüedad de alcance del cuantificador (F-92 P4).
        Hablan de CÓMO SE ESCRIBE UNA REGLA ANTES DE IMPLEMENTARLA.

    LA DIFERENCIA NO ES DE TEMA, ES DE MOMENTO. Las primeras se usan cuando ya
    hay código y se quiere saber si funciona. Las segundas se usan ANTES de que
    exista el código, leyendo una respuesta de Fable con la intención de
    traducirla. Quien va a implementar una regla dictada no necesita saber
    cuántas pasadas lleva una tanda, y quien va a lanzar una tanda no necesita
    la ambigüedad de alcance del cuantificador. Hoy tienen que leerse las dos.

    UN CASO FRONTERIZO, para que el corte no se decida creyéndolo limpio: el
    «cero en N con la tasa que excluye» (F-92) es de MEDICIÓN —se aplica al
    reportar una tanda— aunque naciera en la misma consulta que las otras dos.
    Va con las de medición, no con las de especificación.

    NO SE PARTE HOY, y la razón sigue siendo la del 28/08 con un motivo más: los
    frentes 2 y 3 van a promover reglas, y partir antes obliga a decidir dos
    veces dónde va cada una. Pero el corte ya no hay que buscarlo: está aquí.
    ES LA TERCERA VEZ EN TRES DÍAS que ese fichero crece por un frente distinto:
    el 27/08 se partió sacando el catálogo de casos a claude/Casos_Harness.md
    (39d15163), ese mismo día entró la regla de entrada de F-81 P3 (8632576e),
    y hoy el puntero de F-82. No es un fichero que engorde solo: engorda porque
    es donde se escriben las reglas, que es lo que tiene que pasar.
    NO SE PARTE AHORA, y es una decisión, no un olvido: hacerlo en medio de un
    frente es cómo se decide mal un corte. Cuando se mire con calma, la
    pregunta es qué se separa —el método de las tandas de las reglas de
    entrada/admisión, que hoy conviven ahí— y no simplemente por dónde cortar
    para bajar de 400.
    PRECEDENTE ÚTIL: el corte del 27/08 salió bien porque separó por DOMINIO
    (el método en uno, el catálogo en otro) y dejó escrito en los dos cuál es
    cuál. El de F-82 también: el contrato de contadores es código, no tandas, y
    por eso fue a claude/Contrato_Contadores.md en vez de a una sección más.

  B.114 — [PREEXISTENTE, encontrado 28/08 al medir F-82 P2] Las comillas
    tipográficas NO están en la clase de caracteres de normalize, pese a que su
    cabecera dice que quita «el marcado Markdown junto a la puntuación».
    EL RASTRO: en el fuente (lib/analysis/normalize.ts) la clase tiene `"` TRES
    veces y `'` DOS. Son 27 posiciones y 24 caracteres distintos. Esos
    duplicados son lo que queda de unas comillas tipográficas que se quisieron
    poner y que algún editor convirtió en rectas al escribirlas.
    MEDIDO: `"Caso A"` se funde con `Caso A` y `“Caso A”` NO; `O'Brien` se funde
    con `OBrien` y `O’Brien` NO. Y las tipográficas son justo las que produce
    cualquier procesador de textos, así que son la forma que más va a llegar
    desde un .docx real — el caso frecuente es el que no está cubierto.
    DÓNDE MUERDE: normalize es la comparación AGRESIVA, la que localiza citas
    (verifyQuote) y la que decide el colapso de filas idénticas de F-44/F-45.
    Una cita del juez con comillas tipográficas puede no localizarse hoy.
    NO SE ARREGLA A LA LIGERA: F-46 dice que cualquier ampliación de normalize
    —tildes, sinónimos, distancia de edición— amplía lo que el colapso
    considera «la misma fila» y debe pasar por su batería de medición antes de
    tocarse. Añadir `“ ” ’` es una ampliación, aunque parezca una errata.
    ESTADO: el comportamiento de hoy está FIJADO en lib/analysis/normalize.test.ts
    ("las comillas tipográficas NO están en la clase"). Si alguien las añade,
    ese test se pone rojo y le manda aquí antes de darlo por bueno.
    NO AFECTA al nivel seguro de F-82 P2 (esVarianteDeEscritura): ése no toca
    puntuación de ninguna clase, así que las dos formas le dan igual.

  B.115 — [FALLO MEDIDO 28/08, no arreglado] La rama de PROSA de R2 tiene la
    misma enfermedad que F-82 P2 curó en la de celdas: declara equivalentes
    dos frases que difieren en un factor de cien.
    finding-rules.ts:154-158 reclasifica a solapamiento cuando
    `normalize(newDocSays) === normalize(existingDocSays)`. Y `normalize`
    borra los separadores decimales. Medido llamando a la función:
      "La tarifa es de 25,00 euros" vs "La tarifa es de 2500 euros" -> reclassify
      "Conservar 1.500 unidades"    vs "Conservar 1500 unidades"    -> reclassify
      "Abre a las 10:30"            vs "Abre a las 1030"            -> reclassify
    Las tres se mueven de contradicción a solapamiento sin que nadie lo mire.
    NO SE ARREGLA CON EL NIVEL SEGURO de F-82 P2, y por eso no entró en aquel
    commit: `esVarianteDeEscritura` no toca puntuación de ninguna clase, así
    que en prosa perdería el borrado de MARCADO, que ahí sí hace falta y no es
    teórico — medido sobre el corpus: NOR-10 tiene 82 encabezados `#` y 7
    cursivas `_`, CLI-12 tiene 105 y 7, CLI-03 tiene 9. (Negritas `**`: cero
    en todo el corpus, así que el ejemplo de la cabecera de normalize no es el
    caso que ocurre.)
    LO QUE HARÍA FALTA: un TERCER comparador, que quite marcado y NO toque la
    puntuación numérica. Es otra decisión y otro commit; no se improvisa
    dentro de uno que iba de otra cosa.
    CRUZAR con B.97 (la rama de celdas, en observación) y con F-82 P2
    (b735fe33), que es donde está medida la asimetría entre las dos ramas. La
    advertencia para quien quiera unificarlas está en la cabecera de
    finding-rules.ts.

  B.116 — [MANTENIMIENTO, anotado 28/08] table-key.ts y su batería superan el
    límite de 400 líneas del proyecto: 498 el módulo, 575 el test.
    NO SE PARTEN, y es una decisión con motivo, no un olvido. La batería y su
    módulo tienen que poder BORRARSE JUNTOS —el argumento con el que nació la
    fase 1 el 27/08— y en el módulo el corte natural que quedaría, separar la
    nominación del consenso, es precisamente el que no debe hacerse: son las dos
    mitades de una misma decisión, y leerlas en ficheros distintos es cómo se
    acaba nominando con una comparación y emparejando con otra. Está escrito en
    la cabecera del propio fichero.
    ES LA SEGUNDA VEZ que un fichero de este frente cruza el umbral: table-key.ts
    lo cruzó al nacer (F-81, 469 líneas) y su batería lo cruzó en F-84 1a (467).
    Desde entonces los dos han seguido creciendo con la huella y con N5.
    DECIDIR APARTE Y CON CALMA, no en medio de un frente — mismo criterio que
    B.113, que es el mismo problema en el protocolo del harness. La pregunta al
    mirarlo no es por dónde cortar para bajar de 400: es si hay dos dominios ahí
    dentro o uno solo que ocupa mucho.
    NO CONFUNDIR CON B.113: aquel es sobre claude/Protocolo_Harness_Tasas.md.
    Los mensajes de commit de F-84 1b y del paso 2 citan «B.113» refiriéndose a
    este punto, que entonces no existía; la referencia correcta es ésta.

  B.117 — [CONDICIÓN 3 PENDIENTE, abierto 28/08 con F-84 1b] El criterio de
    emparejamiento seguro no tiene contador de incidencia:
    `rechazadas_por_escritura`.
    QUÉ FALTA: cuántas parejas rechaza el criterio estricto por una diferencia
    que `normalize` habría fundido. Es el coste conocido de F-84 1b, el que N5
    declara con «1.500» contra «1,500».
    POR QUÉ NO LO PUEDE RESPONDER EL CORPUS: la medición de F-84 1a dio CERO
    sobre los 12 pares ordenados —90 parejas, ninguna dependiente de la
    normalización agresiva— y ese cero no es una propiedad del mundo. Estas
    tablas se generaron PROGRAMÁTICAMENTE y no pueden producir un separador de
    millares mal escrito por accidente. Solo los clientes reales pueden.
    CUÁNDO: EN EL COMMIT DE EMISIÓN, que es cuando se tocan contadores de todas
    formas. Vocabulario cerrado, en el catálogo de counters.ts antes de emitirse
    (cláusula 4).
    SIN ÉL, esta pieza se queda con condición 1 (mecanismo demostrado, N1 y N4)
    y condición 2 (no-regresión medida) y SIN condición 3 — la cuarta seguida de
    este frente, después del consenso de la fase 1, del comparador de F-82 P2 y
    del emparejamiento de 1b. Cuatro veces no es casualidad: es que este corpus
    se sembró para medir discrepancias de CONTENIDO, no de escritura.

  B.118 — [ABIERTO 28/08 con F-85] Completar el archivo retrospectivo de
    consultas a Fable, F-70 a F-84.
    ARCHIVADO EL 28/08: F-83, con su respuesta íntegra (claude/consultas-fable/
    F-83.md). Su CONSULTA ENVIADA NO SE CONSERVA y no se puede reconstruir: es
    una pérdida PERMANENTE, no una deuda de este punto. Ese par nunca se
    completará, y el fichero lo declara en su cabecera.
    F-84 QUEDA COMPLETO el 28/08: consulta y respuesta, las dos íntegras. Es el
    PRIMER PAR COMPLETO del archivo. La consulta llegó en un segundo envío.
    F-85 TAMBIÉN COMPLETO el 28/08 — es la consulta que creó el archivo, así que
    queda archivada dentro de sí misma. Quedan DOS pares completos de F-70 a
    F-85; el resto del rango sigue siendo la deuda de este punto.
    F-86 y F-87 archivados el 28/08, los dos COMPLETOS. Van CUATRO pares
    completos (F-84 a F-87) y el rango del índice se amplía hasta F-87; la
    deuda de este punto sigue siendo F-70..F-83.
    F-88 archivado el 29/08, COMPLETO. Van CINCO pares (F-84 a F-88) y el rango
    llega a F-88. La deuda NO se mueve: sigue siendo F-70..F-83. F-88 es además
    la primera entrada que supera a DOS consultas a la vez —F-83 y F-84— y por
    el mismo supuesto tácito, que es justo lo que el archivo íntegro permite
    ver: el supuesto no estaba en ninguna de las dos como afirmación, estaba en
    un singular.
    EL AGUJERO, MEDIDO: de F-70 a F-85 no hay NI UN SOLO PAR completo archivado.
    El archivo y su índice están creados (claude/consultas-fable/), con cada
    entrada en estado «pendiente de archivar», así que el hueco queda visible en
    vez de olvidado — pero los textos no están en el repositorio y no se pueden
    reconstruir: hay que pegarlos.
    LO QUE SÍ HAY, para no volver a buscarlo:
      · F-70 — la RESPUESTA íntegra y literal, en claude/Cierre_F70.md §2. Ese
        fichero existe porque la respuesta se perdió una vez al migrar el chat.
        FALTA LA CONSULTA ENVIADA.
      · F-81 — la CONSULTA íntegra, en claude/Consulta_Fable_F81.md. FALTA LA
        RESPUESTA.
      · F-75 — rastro indirecto: claude/Descarte_Filas_Ajenas.md dice ser «lo
        que vino de vuelta» al pedir la batería, pero está reformateado como
        especificación y no se declara literal. No cuenta.
      · F-82 — una sola frase literal, citada en Contrato_Contadores.md:19. Una
        frase no es un archivo.
    LO QUE NO HAY EN ABSOLUTO: F-72 y F-85 no tienen ni una mención en todo el
    repositorio. F-71, F-73, F-74, F-76, F-77, F-78, F-79, F-80, F-83 y F-84
    existen solo como REFERENCIAS y resúmenes en la bitácora, los pendientes y
    el protocolo — y un resumen no cuenta, que es lo que F-85 descarta con todas
    las letras.
    FUERA DE RANGO, mismo patrón incompleto: Consulta_Fable_F22_Juez.md
    (consulta sin respuesta) y Consulta_Fable_F47_Paso5.md (veredicto sin
    consulta).
    CÓMO SE CIERRA: pegando los textos, uno por consulta, en el formato del
    índice. No es trabajo de código y no lo puede hacer nadie que no tenga los
    originales. Cada uno que entre actualiza su línea del índice y su cabecera
    de estado.
    OJO AL CERRARLO: la cabecera de estado la escribe quien cierra el hecho, y
    el estado solo cambia con un HECHO —una medición, un commit, una consulta
    posterior—. Archivar un texto no es opinar sobre su vigencia.

  B.119 — [ABIERTO 29/08 con F-86 paso 3] LOS CINCO CAMINOS QUE SIGUEN
    OLVIDANDO. El commit de la persistencia de descartes (F-86 paso 3) hace que un
    «No es error» sobreviva a recargar, pero NO en todos los caminos. Se anotan
    los cinco aquí porque el hueco que no se escribe se redescubre a mitad del
    commit siguiente.
    LO QUE SÍ PERSISTE HOY: contradicciones e inconsistencias menores (especie
    PROSA), por las dos entradas — directa desde la bandeja (al instante) y en
    la indexación desde el chat (al pulsar indexar).

    1. SOLAPAMIENTOS / DUPLICIDAD — DECISIÓN DE DISEÑO PENDIENTE, no descuido.
       El usuario los descarta con el mismo botón que todo lo demás y se
       olvidan igual que antes. Motivo: un solapamiento NO TIENE DOS CITAS
       ENFRENTADAS — no hay «lo que dice este documento» contra «lo que dice el
       otro», hay una descripción generada. Ninguna de las dos especies de
       huella de huella-hallazgo.ts le sirve: la tabular pide tabla y clave de
       fila, la de prosa pide las dos citas. Necesita una TERCERA ESPECIE o
       redefinir qué identifica a un solapamiento. No se puede parchear sin
       decidir eso antes.
       Y OJO con la salida fácil: usar la "description" del solapamiento como
       si fuera la cita del otro lado haría que la identidad dependiera de
       texto GENERADO en los DOS lados — peor que el límite ya declarado en la
       especie prosa, que al menos tiene una mitad escrita por el cliente.

    2. PROBLEMAS DE ESTILO — DECISIÓN DE DISEÑO PENDIENTE. Van por otra rama
       del código (setStyleProblems en ImprovementModal) y no se guardan. Son
       hallazgos de UN SOLO DOCUMENTO, y toda la maquinaria de huellas está
       construida sobre PARES de documentos: el orden canónico, los dos lados
       atados a su id, el desempate. Nada de eso significa nada con un solo
       documento. Necesitan su propio mecanismo, no una adaptación del que hay.

    3. CHAT SIN INDEXAR — bendecido por el encargo de F-86 paso 3 («durante la
       revisión los descartes son estado de la pantalla, legítimamente y sin
       fingir persistencia»), pero se escribe para que no sorprenda: si el
       usuario revisa, marca diez cosas y CIERRA SIN INDEXAR, se pierden las
       diez. También si recarga a mitad de la revisión. Muere con el frente del
       ciclo de vida (F-87 P4): con la fila creada en la subida, el documento
       tiene identidad desde el primer momento y no hay nada que aplazar.

    4. ANÁLISIS ANTERIORES A d13e125f — sus discrepancias guardadas en el jsonb
       no traen "existingDocumentId", así que no tienen identidad posible. Se
       descartan en pantalla y se olvidan. No tiene arreglo retroactivo: el id
       no está en ninguna parte de esas filas. Se cura solo, según se
       reanalicen los documentos.

    5. LA PARÁFRASIS DEL JUEZ — es el límite YA DECLARADO en huella-hallazgo.ts
       (cabecera de huellaDeProsa) y el ÚNICO de los cinco que el usuario
       percibirá como «el sistema ha olvidado». La identidad de prosa se
       construye con la CITA QUE ESCRIBIÓ UN MODELO; si en un análisis
       posterior el juez cita la misma contradicción con otras palabras, la
       huella es otra y el hallazgo VUELVE A APARECER aunque el usuario ya lo
       cerrara. Sucesor conocido: las citas por REFERENCIA ({fragmentId,
       ancla}), la cura estructural de F-80/B.107 — que el texto lo extraiga el
       CÓDIGO y no el modelo. Cuando eso exista, la identidad de prosa deja de
       derivar de texto generado y este punto desaparece sin tocar la función.

  B.120 — [ABIERTO 29/08 con F-86 paso 3] LA MUERTE DE
    makeDiscrepancyFingerprint: sin fecha, con dos salidas escritas.
    QUÉ ES LA DEUDA, con precisión: NO es que la función vieja conviva con la
    huella nueva. Hacen DOS TRABAJOS DISTINTOS y el segundo no necesita
    identidad estable:
      · Trabajo 1 — IDENTIDAD EN EL TIEMPO. «¿Este hallazgo es el mismo que el
        usuario ya juzgó, quizá con los documentos al revés?» Lo hace
        huellaDeProsa: los dos ids, hasheada, bidireccional.
      · Trabajo 2 — MEMORIA DE PANTALLA. «El usuario acaba de marcar tres
        cosas; al reanalizar, no gastes Sonnet en esas tres.» Lo hace
        makeDiscrepancyFingerprint, y le basta una clave que cliente y servidor
        calculen igual y que dure lo que dura la pestaña.
    La deuda es que el trabajo 2 NO TIENE FECHA NI SALIDA. Se escriben las dos:

    SALIDA PRINCIPAL — el frente del ciclo de vida del documento (F-87 P4). Con
    la fila creada en la subida, el documento en revisión tiene id SIEMPRE, así
    que el servidor puede mandar la huella real pegada a cada discrepancia y el
    trabajo 2 pasa a usarla. La función vieja muere ahí, y con ella la entrada
    de escritura en la indexación, que F-87 P4 declaró PUENTE INTERINO CON
    SUCESOR CONOCIDO. OJO: la emisión del diff NO mata esta función — no le da
    un id a nadie. Esa promesa se llegó a escribir y se corrigió.

    SALIDA ALTERNATIVA — la opción (b), si el frente del ciclo de vida tarda.
    El trabajo 2 deja de usar huellas: el cliente manda las CITAS EN CRUDO
    (newDocSays + existingDocumentId) y el double-check compara sobre eso. No
    necesita el id del documento nuevo PORQUE NO ES UNA IDENTIDAD PERSISTENTE,
    solo una lista de «esto ya lo miré» dentro de un análisis. Coste: toca la
    columna analysis_jobs.exclude_fingerprints y su significado (deja de
    guardar huellas y pasa a guardar coordenadas), y hay que decidir qué se
    hace con los jobs en vuelo al desplegar. Se descartó en F-86 paso 3 por no
    meter ese cambio de semántica en un commit que ya llevaba SQL.

  B.121 — [LÍMITE DE EVIDENCIA, abierto 30/08 con F-88 paso 1] El par real del
    corpus NO PUEDE CAZAR una confusión de lados, porque es simétrico.
    QUÉ SE COMPROBÓ: al mutar el emparejador de tablas intercambiando los dos
    argumentos del descubrimiento de clave —pasar el candidato donde va el
    documento analizado, y al revés— NO FALLABA NINGÚN CASO apoyado en el
    corpus. OPE-10 y OPE-11 tienen 25 filas exclusivas CADA UNO (verificado en
    corpus-pruebas/SIEMBRA_corpus_ampliado.md §4), así que los dos montones
    salen iguales del derecho y del revés.
    QUÉ SE ROMPERÍA EN PRODUCCIÓN, y por qué importa más de lo que parece: el
    indicativo de F-83 P2, que esa respuesta declaró innegociable. El sistema
    diría «presente solo en OPE-10» donde toca «solo en OPE-11». La cifra
    seguiría siendo 25, el recuento cuadraría, y la frase estaría invertida —
    un fallo que NINGÚN CONTADOR puede ver, porque no mueve ningún número.
    NO ES UN DEFECTO DEL CORPUS, igual que en B.111: dos tarifarios de la misma
    clínica con el mismo número de exclusivas es un dato realista, y sembrar la
    asimetría solo para poder medir esto sería fabricar un documento contra la
    regla de admisión.
    LO QUE SE HIZO: el caso vive como fixture CONSTRUIDO en
    lib/analysis/table-pairing.test.ts («los LADOS no se pueden intercambiar»),
    con montones de 0 y 2 a propósito. La mutación que lo destapó ahora muere.
    QUÉ MIRAR CUANDO LLEGUE UN CORPUS DE CLIENTE: cualquier par cuyos dos
    montones de exclusivas difieran en tamaño sirve para lo que éste no puede.
    Es la misma familia que B.111 y B.117 — tres veces seguidas en este frente
    el corpus no ha podido responder la condición 3, y las tres por la misma
    causa: se sembró para medir discrepancias de CONTENIDO.

  B.122 — [ABIERTO 30/08 con la emisión del diff, F-88 paso 2] EL AVISO DE
    ALCANCE Y EL SOLAPAMIENTO ESTRUCTURAL: UN SOLO FRENTE, NO DOS PENDIENTES.
    Los dos aparecieron en la MISMA pantalla al mirar la primera emisión en
    producción (OPE-10 contra OPE-11, 30/08), y no es casualidad.

    ══ LA CAUSA ÚNICA ══
    QUE LA TABLA NO CUPO EN EL PROMPT DEL JUEZ. Si hubiera cabido entera
    (nivel 1 del reparto), NO EXISTIRÍA NINGUNO DE LOS DOS: ni el aviso de
    alcance —no habría filas fuera que declarar— ni el solapamiento con las
    veinte filas dentro —el colapso de idénticas solo se construye en el nivel
    2, retrieval.ts:765—. Son dos síntomas del mismo recorte, y por eso se
    arreglan juntos o no se arreglan.

    ══ 1. EL AVISO DE ALCANCE DICE ALGO QUE YA NO ES VERDAD ══
    LO QUE SE VE: «38 de 60 filas de OPE-10 no se han comparado por tamaño»,
    arriba del análisis. Pero el diff comparó las SESENTA: 15 discrepantes +
    20 idénticas + 25 ajenas.
    QUÉ MIDE DE VERDAD, y su medida es correcta: cuántas filas RECUPERADAS no
    cupieron en el PROMPT DEL JUEZ. Nada más.
      selectUnitsWithinBudget → result.tableLog[].rowsLeftOut
      → selectionLimits (retrieval.ts:326-333)
      → FinalAnalysis.selectionLimits → SelectionLimitNotice.tsx
    Su contador hermano se llama "seleccion.filas_fuera_por_tamano", con
    prefijo "seleccion." precisamente porque es material descartado de la
    SELECCIÓN, no un hallazgo.
    POR QUÉ EL DIFF NO PASA POR ESE PRESUPUESTO: lee de otro sitio. El juez ve
    "selected" (lo que cupo); el diff ve "chunksByDocument", que viene de
    getChunksForDocuments (read-chunks.ts:144) y trae TODAS las filas de
    document_chunks sin filtro. Ese mapa ya existía —lo necesita el reparto por
    unidades— y el diff lo reutiliza.
    EL AVISO NO HA CAMBIADO NI MIENTE SOBRE LO SUYO. Lo que ha cambiado es que
    ya no es lo único que mira el documento, y su frase —«no se han
    comparado»— habla del análisis EN CONJUNTO cuando solo sabe del juez.

    LAS TRES OPCIONES:
      (a) Acotar la frase: «no las ha visto el juez». Barato y honesto, pero
          deja al usuario con una distinción interna que no le importa.
      (b) RESTAR LO QUE EL DIFF SÍ COMPARÓ. ← RECOMENDADA. El emparejador sabe
          qué filas emparejó; una tabla cubierta por un par emitido no tiene
          filas «sin mirar» aunque el juez no las viera: se compararon celda a
          celda, que es MÁS de lo que el juez hace. Para OPE-10/OPE-11 el aviso
          desaparecería, y desaparecer es lo correcto.
      (c) Declarar dos alcances, el del juez y el del diff, porque miden cosas
          distintas.

    EL MATIZ DE LA (b), QUE NO SE PUEDE PERDER: ES POR TABLA, NO POR DOCUMENTO.
    El diff solo cubre la tabla que emparejó. Las filas de OTRAS tablas del
    mismo documento siguen fuera del prompt del juez y nadie las ha mirado: el
    aviso tiene que seguir hablando de ellas. Restar por documento apagaría un
    aviso verdadero.

    EL ARGUMENTO QUE HAY QUE CONSERVAR, que es el que decide entre las tres:
    el aviso existe (F-74 P2) para convertir un recorte INVISIBLE en un límite
    DECLARADO. Si el recorte ya no implica ceguera —porque otra etapa cubrió
    ese terreno, y mejor— declararlo es el FALLO INVERSO: una promesa de más
    sobre nuestra propia incompetencia. Decir que no miramos algo que sí
    miramos es tan falso como callar lo que no miramos.

    ══ 2. EL SOLAPAMIENTO F-45 VUELCA VEINTE FILAS DEL CLIENTE EN UN TEXTO ══
    LO QUE SE VE: un solapamiento cuya descripción lista las 20 filas
    idénticas ENTERAS, celda a celda, con nombres y precios dentro de la
    frase.
    LA CADENA ENTERA:
      buildContextFragment (retrieval.ts:616-646)
        labels = por cada fila idéntica, el VALOR de TODAS las columnas
                 compartidas, unidas por " / "   (retrieval.ts:630)
      → StructuralOverlap.labels
      → pipeline.ts:442-447, que las mete en la "description" del
        overlappingContent:
        «20 de 60 filas de "Tarifas" de "OPE-11" coinciden exactamente, celda
         a celda, en Código y Tratamiento y Precio con filas de "OPE-10":
         CIR-01 / Implante unitario / 950, CIR-02 / …»
    POR QUÉ MUESTRA TODAS LAS COLUMNAS, y NO es un descuido: elegir una
    columna como «la identidad» sería la heurística por nombre que F-23, F-26
    y F-36 prohíben. El comentario de retrieval.ts:609-614 lo justifica —«sin
    que el código sepa ni necesite saber que Empleado identifica a una
    persona»—. La decisión es correcta; lo que ha cambiado es que ahora hay
    otra forma de decir lo mismo.

    LOS DOS AGRAVANTES, encontrados al investigar esto:
      · Esa "description" ES LO QUE LEEN LOS TRES PROMPTS de ImprovementModal
        (dicho en components/improvement/problems.ts). O sea, las veinte filas
        del cliente se le mandan a un modelo como texto.
      · Y se PERSISTE EN EL JSONB de analysis_results, que la bandeja relee
        meses después.

    NINGUNA DE LAS DOS COSAS ES NUEVA NI LA INTRODUJO EL DIFF. Vienen de
    F-44/F-45. LO QUE EL DIFF HACE ES VOLVER REDUNDANTE LA FORMA CARA: la
    misma verdad —«estas 20 filas son idénticas»— cabe en un NÚMERO más una
    lista estructurada que la ficha puede plegar, sin volcarla en una frase que
    viaja a tres prompts y a la base de datos.

    ══ ESTADO ══
    LA PRIMERA MITAD (el aviso de alcance) QUEDA HECHA el 30/08, adelantada
    porque el aviso estaba en pantalla diciendo algo falso y ocupaba mucho: se
    aplicó la opción (b) —restar lo que el diff sí comparó, POR TABLA— en
    lib/analysis/alcance.ts, con su batería y su mutación. El texto se comprimió
    a una línea. Para OPE-10/OPE-11 el aviso desaparece, que es lo correcto.
    LA SEGUNDA MITAD (el solapamiento F-45 que vuelca las veinte filas) SIGUE
    ABIERTA.

    ══ CUÁNDO ══
    DESPUÉS DE LA FICHA, en commit propio. Decidido el 30/08 con el usuario:
    las dos son consecuencias de que el diff exista y se entienden mejor con la
    ficha delante. La segunda además RETIRA información que hoy se enseña
    —aunque sea para enseñarla mejor—, y eso no entra de rebote en un commit
    que va de otra cosa.



    ══ 2ª MITAD, HECHA EL 01/09 — el volcado fuera (F-94 P2) ══
    La plantilla del solapamiento ya no lleva `labels.join(', ')`. La frase
    conserva lo que informa —recuento, total y columnas, que son CAMPOS PROPIOS
    y no se derivan de las filas— y pierde ~2.200 caracteres de datos del
    cliente que viajaban a los tres prompts y al jsonb SIN QUE NADIE LOS LEYERA
    en pantalla.
    Y DE PASO SE RETIRÓ LA PROPAGACIÓN: `labels` subía desde
    `buildContextFragment` hasta `StructuralOverlap` solo para esa plantilla.
    Sin lector, la copia sobra — es la regla de F-94 P3 aplicada literalmente.
    LAS FILAS SIGUEN DONDE SÍ TIENEN LECTOR: dentro del fragmento de contexto
    del prompt del juez, que las necesita. Ese fragmento es la 1ª mitad de este
    pendiente y solo desaparece si la tabla cabe.
    EL HALLAZGO NO SE RETIRA, con sucesor declarado: dice dos cosas que el diff
    aún no dice —en qué columnas coinciden y qué porcentaje—. Cuando el diff las
    calcule por celdas sobre los pares que cubre, absorbe el solapamiento como
    una sección más de su tarjeta (F-94 P2). Va a la especificación del diff.
    CASO QUE LO VIGILA: `solapamiento-estructural.test.ts`, «NINGUNA fila del
    cliente aparece en la descripción». Mutado devolviendo el volcado: 1 rojo.
    Hacía falta, porque el volcado NO SE VE en pantalla y nadie lo echaría de
    menos si volviera.

    ══ Y LA COMPROBACIÓN DE P3: LAS FILAS AJENAS NO LLEGAN A NINGÚN PROMPT ══
    Verificado con grep sobre `soloEnNuevo`/`soloEnOtro`: sus únicos
    consumidores son `diff-emision.ts:209-210` (donde se construyen), `types.ts`
    (donde se declaran) y `components/improvement/table-coverage.ts` (LA
    INTERFAZ, el bloque de cobertura). Ningún prompt.
    Luego se quedan: es el criterio de F-94 P3 —se persisten porque SE MUESTRAN,
    y el jsonb del análisis es el resultado—. Cincuenta filas ajenas
    persistidas son el resultado; cincuenta de solapamiento en tres prompts eran
    una fuga.
    Y la higiene que Fable pedía para ellas —que su texto vaya sin el prefijo de
    fila— YA SE CUMPLÍA: `aFila` usa `r.text`, que es el texto ALMACENADO
    («[Hoja "X"] Columna: valor | …»), no el renderizado con `[F0]`. El prefijo
    nunca estuvo ahí.

    ══ 3. TERCER SÍNTOMA, MEDIDO EL 31/08: CINCO SOLAPAMIENTOS QUE CITAN LA
       LÍNEA DE CONTEXTO Y MUEREN ══
    LO QUE SE VE, en la tanda 1 del frente 1 y SOLO en la dirección OPE-10 →
    OPE-11: cinco solapamientos POR PASADA, todos descartados con «cita de
    línea de contexto, no citable» (citaDeContexto, judge.ts:522).
    ES LA MISMA CAUSA ÚNICA de arriba y por eso vive aquí y no en un pendiente
    nuevo: el fragmento de contexto con las filas idénticas colapsadas SOLO se
    construye en el nivel 2 del reparto, o sea porque la tabla no cupo. Si
    cupiera entera no habría fragmento, no habría línea marcada «no citar», y no
    habría nada que citar mal.
    LO QUE AÑADE AL PENDIENTE, y es lo que lo hace anotable: el síntoma 2 es un
    solapamiento que SOBREVIVE y vuelca veinte filas del cliente en su
    descripción; éste es el juez CITANDO la línea marcada y muriendo por ello.
    El mismo fragmento produce las dos cosas según por dónde entre. Arreglar el
    recorte los apaga los tres; arreglar solo el volcado deja éste vivo.
    Y ES GASTO PURO: cinco hallazgos por pasada que el juez produce, viajan, y
    se tiran. No es un falso positivo publicado —el detector hace su trabajo—
    pero sí trabajo del modelo pagado para nada.

    ⚠️ LA HIPÓTESIS OBVIA SE COMPROBÓ Y ERA FALSA (31/08): no es que una tabla
    quepa en el prompt y la otra no. Las dos tienen 60 filas y OPE-11 es incluso
    MAYOR (15.459 caracteres frente a 14.540). Anotado antes de tener el log,
    que es lo que manda «datos antes que hipótesis».

    ⚠️ ASIMETRÍA RESUELTA EL 31/08 CON EL LOG DELANTE: ES DEL JUEZ, NO DEL
    REPARTO. Y se resolvió por el criterio que quedó escrito aquí mismo antes de
    tener el dato, que es lo que lo hace una respuesta y no una racionalización.
    LO QUE DICE EL LOG, las dos direcciones:
      OPE-10 como corpus:  nivel 2, resumen + 22/60 filas, 20 colapsadas en 1
                           línea, 38/60 FUERA por tamaño
      OPE-11 como corpus:  nivel 2, resumen + 21/60 filas, 20 colapsadas en 1
                           línea, 39/60 FUERA por tamaño
    Prácticamente idénticos. Y LAS DOS CONSTRUYEN LA LÍNEA DE CONTEXTO: el juez
    recibe `contexto_no_citable: 1` en las DOS direcciones.
    LUEGO EL FRAGMENTO MARCADO EXISTE SIEMPRE, y la diferencia está en lo que el
    juez hace con él. Por el criterio escrito arriba —«si selectionLimits es
    parecido en las dos, la asimetría es del JUEZ»—, es del juez.
    Y NO ES QUE EN UNA DIRECCIÓN NO HAYA SOLAPAMIENTOS: hay CINCO en las dos.
      OPE-11 → OPE-10:  "5 solapamientos", NINGUNO descartado → los cinco SE
                        PUBLICAN. Es el síntoma 2 de arriba, el volcado.
      OPE-10 → OPE-11:  "5 solapamientos", los CINCO descartados por
                        citaDeContexto → el síntoma 3.
    LOS DOS SÍNTOMAS SON EL MISMO HALLAZGO SEGÚN DÓNDE ATERRICE LA CITA. El juez
    produce cinco solapamientos sobre filas idénticas en las dos direcciones; en
    una cita algo verificable y se publican, en la otra cita la línea marcada y
    mueren. No hay dos problemas: hay uno con dos salidas.
    LA ÚNICA DIFERENCIA MEDIBLE en el reparto, por si alguien la quiere seguir:
    el juez recibe DOS fragmentos table_row cuando el corpus es OPE-10 (filas
    [44, 32]) y UNO solo cuando es OPE-11 (fila [33]); y 43 caracteres de prosa
    frente a 243. Es consistente con «con menos filas citables, el juez tira de
    la línea prohibida», pero eso es una HIPÓTESIS y no está medida — dos
    direcciones no son una tasa. Queda escrita como hipótesis, no como causa.

    ⚠️ REFINADO EL 31/08 CON LA TANDA 2: LOS SÍNTOMAS NO SON UN TRÍO FIJO, SON
    DOS ESCALONES. La predicción de aquella tanda acertó el corte —OPE-02 con
    2.116 caracteres da nivel 1 y ni colapso ni línea de contexto; RRHH-06 con
    2.397 da nivel 2 con las dos cosas— pero FALLÓ en el aviso de alcance: en
    RRHH-06 no lo hay, porque el log dice «resumen + 15/15 filas», NINGUNA
    fuera.
    LA RAZÓN, y es la que hay que retener: EL COLAPSO DE LAS IDÉNTICAS ES LO
    QUE HACE QUE QUEPA. Nivel 2 no implica filas fuera.
      · ESCALÓN 1 — no cabe entera → colapso + línea de contexto. De aquí salen
        el volcado de filas (síntoma 2) y el citaDeContexto (síntoma 3).
      · ESCALÓN 2 — no cabe NI DESPUÉS de colapsar → además, filas fuera y
        aviso de alcance (síntoma 1). Es el caso de OPE-10/OPE-11, 38/60.
    La causa única sigue en pie —el recorte— pero produce dos, tres o cuatro
    síntomas según cuánto se pase de presupuesto, y un arreglo que solo mire el
    aviso de alcance no toca los otros dos.

  B.123 — [ABIERTO 30/08, nombrado por el director] «AUNQUE NO SE REVISEN
    TODAS, SE REVISARÁN LAS MÁS IMPORTANTES». Hoy NO es así.
    LO QUE SE DICE, y es una promesa razonable de producto: si el análisis no
    puede mirarlo todo, que al menos mire lo que importa.
    LO QUE HACE EL SISTEMA: la selección elige las filas que CABEN, no las que
    IMPORTAN. El reparto por unidades (retrieval.ts) ordena por score de
    Pinecone y mete unidades ENTERAS hasta agotar el presupuesto; dentro de una
    tabla que entra a nivel 2, las filas se ordenan por cruce y score y entran
    las que quepan. Ninguno de esos criterios sabe si una fila es DISCREPANTE:
    saben si se parece a lo que se buscó.
    POR QUÉ NO ES UN FALLO SINO UN LÍMITE: para elegir «las más importantes»
    haría falta saber cuáles lo son ANTES de compararlas, y eso es exactamente
    el predicado de F-65 (claude/Descarte_Filas_Ajenas.md), que no existe. La
    aproximación disponible —«cruza y no es idéntica»— falla en los dos
    sentidos, y está medida en B.104.
    NO SE RESUELVE AQUÍ, y decirlo importa: mientras no exista el predicado, el
    sistema NO PUEDE prometer que revisa lo importante. Lo que sí puede —y hace
    desde F-74 P2— es DECLARAR lo que no miró. Prometer selección por
    importancia sin el predicado sería la misma clase de promesa de más que
    B.122 acaba de quitar del aviso de alcance, con el signo cambiado.
    DÓNDE SE JUNTA CON LO QUE YA HAY:
      · B.104 — el aviso dice cuántas filas quedaron fuera, no cuántas eran
        DISCREPANTES. Es este mismo hueco visto desde el aviso.
      · B.102 — el tope de 25 piezas se come el presupuesto antes que los
        caracteres, y es la otra mitad del frente de la selección.
      · F-65 — el predicado que falta, del que dependen los tres.
    QUÉ MIRAR CUANDO SE RETOME: si con el diff de tablas corriendo (F-88) la
    pregunta cambia de sitio. El diff NO pasa por el presupuesto —compara la
    tabla entera— así que en documentos TABULARES la promesa ya se cumple sin
    selección ninguna. Lo que queda sin resolver es la PROSA, que es donde el
    presupuesto sigue mandando y donde nadie sabe qué párrafo importa más.

  B.124 — [FALLO MEDIDO EN PRODUCCIÓN 30/08 · CURADO Y VERIFICADO EN
    PRODUCCIÓN EL 31/08 · la consulta a Fable ya se hizo y volvió: F-89, F-90 y
    F-91] R2 CONFIRMA POR ESTRUCTURA UN EMPAREJAMIENTO FALSO DEL JUEZ.
    Es «fabricar mentiras con sello matemático», que es como F-84 P2 llamó al
    riesgo más grave del sistema. Lo curamos en el emparejamiento del DIFF y la
    puerta del JUEZ quedó abierta.

    ══ VERIFICADO EN PRODUCCIÓN EL 31/08, y llegó por SUERTE ══
    En la pasada 0 de la tanda del frente 1 (commit cceddf86) EL JUEZ VOLVIÓ A
    EMITIR EL MISMO FALSO, y esta vez salió descartado. Del log:
      [dc678e1b] "Profesional asignado para Carilla de composite (EST-03)"
         → descartado: cubierto_por_diff
    `dc678e1b` es el identificador del falso ORIGINAL del 30/08 — el hash es
    `hashCitationPair(newDocSays, existingDocSays)`, o sea las dos citas, así
    que es literalmente el mismo hallazgo y no uno parecido.
    LO QUE ESTO CAMBIA Y LO QUE NO: la verificación en producción EXISTE, y
    B.125 decía que no se podía garantizar que ocurriera. No se equivocaba: lo
    que no se podía garantizar era que OCURRIERA, no que FUNCIONARA. B.125 sigue
    vigente como límite; lo que caduca es la frase «no hay verificación en
    producción», no su razonamiento.
    Y DE PROPINA, LA FABRICACIÓN QUEDÓ VALIDADA: el caso de
    `cascada-emparejamiento.test.ts` produce EL MISMO HASH `dc678e1b` que
    producción. No es «un hallazgo parecido al del log»: es el mismo por la
    función de identidad del propio sistema. Lo que la fabricación reprodujo
    fiel son las DOS CITAS —que es lo que el hash cubre—; las columnas citadas y
    su alineación no entran en él y no quedan verificadas por esta vía.

    ══ ⚠️ MUERE POR DOMINANCIA, NO POR VERIFICACIÓN — nota de método ══
    Salió por `cubierto_por_diff`, NO por `emparejamiento_invalido`. Es
    correcto: el par EST/OPE-10 está EMITIDO, así que la supresión (F-89 P4) se
    lo lleva antes de que R2 llegue a verificar identidad.
    CONSECUENCIA QUE HAY QUE TENER ESCRITA: la guarda de identidad de la 3ª
    puerta NO está cazando el caso que motivó el frente — está DE RESERVA. Si
    algún día se suprimiera menos (una 3ª puerta más estricta, un emparejador
    que emita menos), sería ella quien lo cazaría. Su contador a cero NO
    significa que el falso positivo no ocurra: significa que muere antes.
    Está anotado también junto a las dos guardas en `pipeline.ts`, que es donde
    lo leerá quien vaya a tocarlas.
    ══ EL CASO, visto en pantalla ══
    Análisis de OPE-11 contra OPE-10. El juez emitió:
      "Profesional asignado para Carilla de composite (EST-03)"
      En este documento, Código figura como «EST-02»; en OPE-10, como «EST-03».
      ...y siete columnas más, TODAS distintas.
    Y la cascada lo selló CONFIRMADO POR ESTRUCTURA. Del log:
      [dc678e1b] ... -> confirmado por estructura (columnas: Código,
      Tratamiento, Precio base, Precio con seguro, Duración (min),
      Profesional asignado, Clínica, Revisión)  <- las OCHO.

    ══ EL GROUND TRUTH, del registro de siembra ══
    corpus-pruebas/SIEMBRA_corpus_ampliado.md §2 y §3:
      · EST-02 es una de las 20 FILAS IDÉNTICAS — "mismo código, mismo
        tratamiento y mismo valor en TODAS las columnas en ambos ficheros".
      · EST-03 es una de las 15 SEMBRADAS, y difiere en EXACTAMENTE UNA
        columna: Precio base, 180 € contra 160 €.
    O sea: el juez cogió una fila que es IDÉNTICA en los dos documentos y la
    enfrentó a otra distinta. El título miente en los dos sentidos — dice
    "EST-03" sobre una fila que en el documento analizado es EST-02.

    ══ EL MECANISMO: cada eslabón es correcto por separado ══
    El emparejamiento lo hace EL JUEZ, en su cabeza. No hay ninguna etapa que
    empareje filas antes de él: ve las filas de las dos tablas en el prompt y
    decide qué enfrenta con qué.
      1. verifyQuote        -> ¿la cita existe en el documento? SÍ, las dos
                               filas son reales.
      2. alignQuoteToCells  -> alinea la cita contra las celdas por POSICIÓN y
                               exige que TODOS los segmentos casen
                               (table-structure.ts:266). Las ocho columnas son
                               legítimamente de esa fila.
      3. applyDeterministicRules (R2) -> comparten columnas citadas y difiere
                               algún valor -> confirm.
    NINGUNA GUARDA DEBIÓ SALTAR, PORQUE NO EXISTE NINGUNA QUE PUDIERA. R2 tiene
    cuatro salidas para fila/fila (finding-rules.ts:154-190):
      · sin columnas citadas          -> pass
      · sin columnas comunes          -> discard 'sin_columna_comun'
      · todos los valores iguales     -> reclassify 'equivalentes'
      · AL MENOS UNO DIFIERE          -> confirm 'valores_distintos_misma_columna'
    Ninguna pregunta si las dos filas SON LA MISMA FILA. R2 nunca fue una
    verificación del emparejamiento: fue diseñada CONFIANDO en que el juez
    empareja bien, y solo comprueba que los valores difieran.

    ══ LA SEÑAL ESTÁ INVERTIDA, y es el corazón del fallo ══
    Hoy, cuanto MÁS difieren las filas, más se «confirma» el hallazgo: una
    columna distinta y ocho distintas entran por la misma puerta. Pero difieren
    en las OCHO, INCLUIDA LA QUE SERÍA CLAVE. Dos filas de la misma tabla que
    no comparten ni el código no son la misma fila con valores distintos: son
    DOS FILAS DISTINTAS. Diferir en TODO es la firma de un emparejamiento
    falso, no la de un hallazgo fuerte.

    ══ POR QUÉ R2 NO PODÍA — Y POR QUÉ AHORA SÍ ══
    ES LO QUE HACE ESTO ACCIONABLE. R2 escribió su propia razón para no
    identificar la fila, en el comentario de la función:
      «entity es siempre null: ... no hay forma estructural de saber cuál lo
       sería sin comparar nombres de columna (buscar "Nombre", "Empleado",
       "Código"...) — exactamente el juicio semántico disfrazado de estructura
       que F-23/F-26 prohíben.»
    ERA CIERTO CUANDO SE ESCRIBIÓ. HA DEJADO DE SERLO. discoverTableKey
    (table-key.ts, F-78/F-81) determina la clave ESTRUCTURALMENTE, por unicidad
    >=90% en AMBAS tablas, SIN NINGUNA heurística de nombres. Es exactamente la
    pieza que R2 declaró imposible, y lleva en main desde F-81.

    ══ Y EL DIFF LO DESMIENTE EN LA MISMA PASADA ══
    Sobre la MISMA tabla y en el MISMO análisis, el diff emparejó por Código:
      EST-02 <-> EST-02  -> idénticas
      EST-03 <-> EST-03  -> discrepante en Precio base
    El diff SABE que EST-02<->EST-03 no es una pareja. No hay que ir a buscar
    la verdad a ninguna parte: ya está calculada, en el mismo resultado.

    ══ EL APUNTE QUE AGRAVA: DOS PRODUCTORES, UN SOLO ARRAY ══
    Desde el commit de emisión (d1a94d71), una MISMA pareja de tablas produce
    hallazgos por DOS VÍAS —el juez y el diff— y los dos caen en el array de
    contradicciones, INDISTINGUIBLES para el contador plano. Este falso
    positivo del juez SUMA a las quince del diff: el usuario ve dieciséis donde
    hay quince, y la de más lleva el sello más fuerte del producto.

    ══ NO ESTÁ CUBIERTO POR NINGÚN PENDIENTE. Los tres vecinos, y por qué no ══
      · B.82 — el juez no es estable ni consigo mismo, con falsos positivos
        DISTINTOS entre pasadas. Aquello es INESTABILIDAD; esto es un fallo
        SISTEMÁTICO que la cascada SELLA.
      · B.115 — R2 tiene una enfermedad en su rama de PROSA (declara
        equivalentes dos frases que difieren en un factor de cien). Ésta es la
        rama de CELDAS y es otra distinta.
      · B.103 — el double-check pierde confirmedBy. Otro asunto.

    ══ QUÉ HACER: NADA TODAVÍA ══
    VA A FABLE. R2 lleva años en producción y toca TODO el sistema, no solo las
    tablas: la rama de celdas la usan todos los hallazgos fila/fila vengan de
    donde vengan. Cualquier arreglo cambia qué se confirma en el producto
    entero, y eso no se decide desde un caso.
    LO QUE SÍ ESTÁ ESTABLECIDO y no hace falta volver a averiguar: el
    diagnóstico completo de arriba, el ground truth del registro de siembra, y
    que la pieza que R2 declaró imposible existe desde F-81 y ya corre en la
    misma pasada.

  B.125 — [LÍMITE DE EVIDENCIA, abierto 30/08 con el frente 1] LA REMEDIACIÓN
    DE UN FALLO DEL JUEZ DEPENDE DE QUE EL JUEZ LO REPRODUZCA, Y NO LO HACE.
    QUÉ SE INTENTÓ: verificar en pantalla el arreglo de B.124 —R2 descarta el
    emparejamiento inválido— lanzando el análisis de OPE-11 contra OPE-10.
    QUÉ PASÓ: en CUATRO pasadas (tres rápidas y una exhaustiva) el juez NO
    volvió a emitir el falso EST-02/EST-03 ni una sola vez. En el rápido emitió
    UNA contradicción; en el exhaustivo DOS, y las dos legítimas —EST-03 se
    confirmó con UNA sola columna (Precio base), que es la discrepancia real
    sembrada, mientras que la falsa citaba ocho—.
    Los contadores lo confirman: hallazgos_entrantes 1, descartados 0, ningún
    emparejamiento_invalido. LA GUARDA NO CAZÓ NADA PORQUE NO TUVO NADA QUE
    CAZAR. Eso NO es que el arreglo funcione: es que la pasada no lo pisa.
    LA CAUSA es B.82: el juez no es estable ni consigo mismo — el mismo par de
    documentos, con el mismo prompt byte a byte, produce falsos positivos
    DISTINTOS entre ejecuciones. Un fallo que aparece de forma intermitente no
    se puede usar como prueba de su propia cura.
    ES LA MISMA CLASE DE LÍMITE QUE B.121, y conviene verlos juntos: un
    INSTRUMENTO QUE NO PUEDE FALLAR DONDE DEBERÍA.
      · B.121 — el corpus es simétrico (25 y 25), así que no puede detectar una
        confusión de lados por mucho que se mire.
      · B.125 — el juez es intermitente, así que no puede reproducir su propio
        fallo por mucho que se lance.
    En los dos casos el verde de la verificación NO ES EVIDENCIA, y en los dos
    la salida fue la misma: construir el caso a mano.
    LO QUE SE HIZO: lib/analysis/cascada-emparejamiento.test.ts fabrica el
    hallazgo TAL COMO EL JUEZ LO EMITIÓ —título, citas y las ocho columnas,
    copiados del log— y ejerce la cascada ENTERA sin tocar ningún modelo
    (applyCascadeToCandidate solo alcanza verifyFindings si un hallazgo
    sobrevive hasta toVerify, y el del caso se descarta antes). ⚠️ ESA
    GARANTÍA ERA UNA FRASE Y NO SE CUMPLÍA: otro caso del mismo fichero sí la
    alcanzaba y pasaba en verde. Corregido el 30/08 con la guarda de red — ver
    B.126. Ahora lo garantiza la guarda, no el paréntesis.
    LO QUE ESE CASO DEMUESTRA Y LO QUE NO, escrito también en su cabecera
    porque es lo que se leerá mal dentro de seis meses:
      · DEMUESTRA el CABLEADO — si llega, se descarta.
      · NO DEMUESTRA la INCIDENCIA — con qué frecuencia llega. Eso solo lo
        puede decir un corpus de cliente, o muchas pasadas contadas.
    REGLA PRÁCTICA QUE DEJA, para no volver a discutirlo: la verificación en
    pantalla de un arreglo sobre un fallo INTERMITENTE del juez no es
    concluyente por diseño. Sirve para detectar regresiones en lo que sí es
    estable (que las quince sigan siendo quince, que el reparto no cambie),
    NUNCA para dar por bueno el arreglo. Lo que concluye es el caso fabricado.
    ⚠️ ACTUALIZADO EL 31/08 — EL JUEZ REPITIÓ, UNA VEZ, Y POR SUERTE.
    En la pasada 0 de la tanda del frente 1 volvió a emitir el falso
    EST-02/EST-03 (hash dc678e1b, el mismo) y salió descartado. Ver B.124.
    ESTE PENDIENTE NO SE CIERRA, y la distinción es exactamente la que lo
    fundó: lo que no se podía garantizar era que el fallo OCURRIERA, no que la
    guarda FUNCIONARA. Cinco pasadas sin él y una con él siguen sin ser una
    tasa; siguen siendo un juez intermitente (B.82). Que esta vez tocara no
    convierte la pantalla en instrumento — mañana vuelve a no tocar.
    LO QUE SÍ CADUCA: la frase «no hay verificación en producción». La hay.
    Lo que no hay es forma de PEDIRLA.
    Y LA REMEDIACIÓN QUEDÓ VALIDADA POR EL CAMINO: el caso fabricado de
    cascada-emparejamiento.test.ts da EL MISMO HASH que el hallazgo real, así
    que la fabricación no era «parecida» — era el mismo hallazgo por la función
    de identidad del sistema. Es la mejor prueba posible de que construir el
    caso a mano fue la salida correcta y no un apaño.
    ⚠️ 31/08, TANDA 1 — SU PREMISA IMPLÍCITA, DESMENTIDA: EL FALLO ES FRECUENTE.
    De seis pasadas de OPE-11 → OPE-10, dc678e1b apareció en TRES con hash
    idéntico, y en dos más el detector de narración mató antes lo que parecía el
    mismo enfrentamiento (esas dos NO llevan el mismo hash: una narración no es
    una cita, y el hash es hashCitationPair — así que no se pueden contar como
    dc678e1b).
    El 30/08, cuatro pasadas dieron CERO, y de ahí salió «no se puede
    reproducir». AQUEL CERO ERA MALA SUERTE, NO UNA TASA BAJA.
    EL LÍMITE NO CAMBIA y por eso el pendiente no se cierra: sigue sin poderse
    PEDIR que ocurra, y una pasada verde sigue sin distinguir «arreglado» de
    «hoy no tocó». Lo que cambia es que ya no se puede decir «el fallo es raro»
    para restarle importancia: es frecuente, y estuvo publicándose.


  B.126 — [HALLAZGO SOBRE LA BATERÍA ENTERA, abierto y cerrado el 30/08] UN
    TEST QUE LLAMABA AL MODELO, FALLABA CON 401 Y PASABA EN VERDE. LA
    BATERÍA NO PODÍA AVISAR, Y LO QUE LO GARANTIZABA ERA UNA FRASE.
    CÓMO SALIÓ: preparando el punto 4 del frente 1, un caso nuevo empezó a
    alcanzar verifyFindings. La llamada salió DE VERDAD a api.anthropic.com,
    volvió 401 «invalid x-api-key», el fail-open del cliente se la tragó —que
    es lo CORRECTO en producción— y el caso pasó. Un test que no puede fallar
    por la razón que vigila: vigilaba un descarte determinista y lo daba por
    bueno porque el modelo no había contestado.
    LA AUDITORÍA, porque el hallazgo no era del caso sino de la batería: se
    escribió la guarda y se pasó la batería ENTERA con ella. Cayó UN caso de
    234, ya commiteado y verde desde el frente 1:
      · «un cruce TABLA-PROSA sobre la misma tabla NO se suprime»
        (cascada-emparejamiento.test.ts). Dos llamadas externas por pasada, y
        cinco segundos de backoff que nadie había mirado.
    Ningún test toca Supabase ni Pinecone: verificado en la misma pasada.
    LA CAUSA DE FONDO, que es la que importa: EL ALCANCE ESTABA DECLARADO Y NO
    VIGILADO. vitest.config.mts y el §1-bis del protocolo dicen desde el 27/08
    «nada que necesite Supabase, Pinecone o Anthropic». Estaba escrito en dos
    sitios y no lo hacía cumplir nadie, así que se rompió sin que saltara nada.
    Y LA CABECERA DEL FICHERO AFIRMABA LO CONTRARIO DE LO QUE PASABA — escrita
    por Claude en el mismo frente:
      «SIN LLAMAR A NINGÚN MODELO, y por construcción... Si algún día un caso
       de este fichero intentara alcanzarla, fallaría por falta de clave de
       API, que es un aviso ruidoso y no un falso verde.»
    Falsa en las dos mitades: un caso ya la alcanzaba, y no avisaba. La misma
    frase se había propagado a B.125, entre paréntesis, y también se corrigió.
    ES LA CUARTA PLANTILLA EN SU FORMA MÁS PURA —dar por existente algo que no
    está— pero con un agravante que las otras tres no tienen: aquí lo daba por
    existente UNA GARANTÍA ESCRITA POR NOSOTROS, no una consulta a Fable.
    LO QUE SE HIZO: vitest.setup.ts, guarda de red.
      1. Intercepta fetch — por ahí pasan los tres, Anthropic, Supabase y
         Pinecone, así que se cierran de una vez.
      2. ANOTA la violación y rompe el caso DESDE afterEach, NO desde la
         excepción. Ésta es la pieza que importa: el código de producción está
         DISEÑADO para tragarse los fallos de red, así que se tragaría también
         el de la guarda. Que un test no pueda silenciar a su propia guarda es
         la única forma de que la guarda sirva de algo.
    LA GUARDA SE MUTÓ, porque una guarda que no puede dispararse es decoración:
      · caso sucio que llama y hace catch, igual que el fail-open real → ROJO.
        El caso limpio de detrás, verde: no se contagia.
      · (m1) quitar el afterEach y dejar solo la excepción → EL CASO SUCIO PASA
        EN VERDE. Es la demostración directa de que la frase vieja era falsa.
      · (m2) no anotar la violación → EL CASO SUCIO PASA EN VERDE.
      Las dos mitades cargan peso.
    LÍMITE DECLARADO: la guarda ve la llamada que SALE, no la intención de
    llamar. Supabase revienta al CONSTRUIR el cliente (supabaseUrl is required)
    antes de tocar fetch, así que un caso que solo pisara el rate-limiter no
    sería detectado. Hoy no hay ninguno — está declarado por si mañana lo hay.
    LA REGLA QUE DEJA, escrita en la cabecera del fichero y en el §1-bis:
    EN LA BATERÍA SOLO CABEN CAMINOS QUE TERMINEN EN DESCARTE. Lo que
    sobrevive, sobrevive HACIA EL MODELO, y eso no se mide en una batería
    determinista — se mide en una tanda. La propiedad del cruce tabla-prosa se
    quedó donde sí es determinista, sobre la función pura.
    LO QUE SE PIERDE, dicho y no escondido: el cableado del lado de prosa, o
    sea que la cascada le pase a veredictoDeEmparejamiento los argumentos que
    cree. No tiene forma determinista de probarse.

  B.127 — [OBSERVACIÓN DE PRODUCTO, medida el 31/08 en la tanda del frente 1 ·
    CORREGIDA EL MISMO DÍA con el log delante] SOBRE UN PAR DE TABLAS
    EMPAREJADO, EL EXHAUSTIVO NO ENVÍA NADA A SONNET.
    ⚠️ CÓMO SE ESCRIBIÓ MAL LA PRIMERA VEZ, y va delante porque es el aviso:
    la primera redacción decía «el exhaustivo no hace NADA que el rápido no
    haga». ES FALSO. Se escribió sobre el RESUMEN de la pasada, no sobre el
    log, y el log dice otra cosa:
      [style-check] "OPE-11...": 15 problemas de estilo (9255ms)
    El exhaustivo CORRIÓ EL ANÁLISIS DE ESTILO —nueve segundos de modelo, 15
    problemas— que el rápido no hace. Es la regla de «datos antes que
    hipótesis» incumplida por mí en su forma más tonta: tenía el log a una
    petición de distancia y escribí sobre el resumen.
    LO QUE SÍ ES CIERTO, y es lo que queda del pendiente:
      [pipeline-exhaustive] Candidatas al double-check: 15
      [pipeline-exhaustive] Frontera del double-check: 15 por estructura NO se
        envian a Sonnet, 0 por juicio si
    CERO llamadas a Sonnet. Y es correcto: particionDoubleCheck (pipeline.ts:631)
    separa por confirmedBy === 'estructura', y desde el frente 1 las quince lo
    son. Ningún modelo revierte un veredicto determinista (F-64/F-71).
    LA CUENTA REAL, ya sin el error: el exhaustivo entregó rápido + estilo. Al
    precio de tarifa eso son 5 + 2 = 7 créditos; el exhaustivo costó 30 y
    devolvió 5 por precio variable (tramo medio, 15 contradicciones), o sea 25
    NETOS. La diferencia es 25 contra 7, no 30 contra 5.
    NO ES UNA AVERÍA. Es la doctrina funcionando. Lo que es, es un cambio de
    valor que nadie decidió.
    ⚠️ ALCANCE, para no exagerarlo: vale para un par de tablas que EMPAREJA y
    donde todo lo publicado viene del diff. Con prosa, tablas sin clave o
    hallazgos de juicio, el double-check vuelve a tener qué hacer — y el corpus
    del harness es el caso extremo, no el típico. Lo decide el director con la
    tanda entera delante, incluida la prosa.
    QUÉ HARÍA FALTA, sin decidirlo aquí: (a) medir en un corpus de cliente qué
    fracción de análisis acaba con cero candidatas a Sonnet; (b) si es alta,
    decidir si el reembolso variable ya lo cubre o si el worker debería detectar
    «cero enviadas al double-check» y devolver más.
    CRUZAR con la lógica de reembolso del exhaustivo (CLAUDE.md) y con B.110:
    la fracción de (a) es justo el tipo de cifra que hoy no tiene dónde vivir.

  B.128 — [AGUJERO MEDIDO EN PRODUCCIÓN 31/08, tanda 2 · VA A FABLE, no
    arreglar sin consultar] LA SUPRESIÓN SUPRIME POR **FILA**; EL DIFF COMPARA
    POR **COLUMNA COMPARTIDA**. EL HUECO ENTRE LAS DOS COSAS ES EXACTAMENTE EL
    TERRITORIO DE LAS OMISIONES.

    ══ EL FILO, y es lo que hace importante al pendiente ══
    Una columna que SOLO EXISTE EN UN DOCUMENTO es territorio que F-78 y F-90
    le reservaron AL JUEZ —el diff no puede decir nada de ella, porque no tiene
    con qué compararla—. Y es precisamente ahí donde la supresión se lleva sus
    hallazgos, diciendo en el log que el diff ya los comparó.
    ES LO CONTRARIO DE «LO MOSTRADO ES REAL», APLICADO A NUESTROS PROPIOS
    DESCARTES. La regla vale para lo que publicamos y también para lo que
    tiramos: un motivo de descarte que no es verdad es una mentira interna, y
    las internas son peores porque nadie las revisa.

    ══ LOS DOS CRITERIOS, uno al lado del otro ══
    LA SUPRESIÓN (emparejamiento-juez.ts, veredictoDeEmparejamiento):
      mismo par de tablas  Y  las dos filas son una PAREJA de filas
      → 'pareja' → descartado.cubierto_por_diff
      NO MIRA NINGUNA COLUMNA. Ni una.
    EL DIFF (table-diff.ts:201-204):
      shared          = nueva.columns ∩ existente.columns
      comparedColumns = shared MENOS las columnas clave
      → solo compara ESO.
    La mitad de la clave es inofensiva —dos filas emparejadas coinciden en la
    clave por construcción, no pueden discrepar ahí—. LO QUE NO ES INOFENSIVO
    ES LA DIFERENCIA SIMÉTRICA: las columnas que están en una tabla y no en la
    otra. El diff no las mira NUNCA, y la supresión se lleva igual lo que el
    juez diga de ellas.

    ══ LA CIFRA DEL PAR QUE LO DESTAPÓ (RRHH-06 / OPE-02) ══
      RRHH-06: 10 columnas (Empleado, Puesto, Clínica, Fecha evaluación,
               Puntualidad, Calidad técnica, Trabajo en equipo, Atención al
               paciente, Media, Comentarios)
      OPE-02:  10 columnas (Empleado, Puesto, Box asignado, Lunes..Sábado,
               Horas semana)
      compartidas: 2 (Empleado, Puesto) · clave: Empleado
      → EL DIFF COMPARA **UNA** COLUMNA (Puesto) DE **DIECIOCHO** DISTINTAS.
      → LA SUPRESIÓN CUBRE HALLAZGOS SOBRE LAS DIECIOCHO.

    ══ CÓMO SE DESTAPÓ ══
    El juez emitió "Horas semanales de Dra. Ana Belmonte" en dos pasadas.
    `Horas semana` existe SOLO en OPE-02; RRHH-06 no tiene esa columna. El
    hallazgo PASÓ la verificación de citas —no hay descarte en judge.ts para
    8fd17cb9— y murió después por cubierto_por_diff, con el motivo
    «el diff ya comparó esas dos tablas celda a celda», que para esa columna es
    LITERALMENTE FALSO.
    HOY NO SE PIERDE NADA: ese hallazgo era malo, porque no hay nada en RRHH-06
    que pueda contradecir un dato que RRHH-06 no tiene. PERO EL MECANISMO SE
    LLEVARÍA IGUAL UNO BUENO, y el caso bueno tiene nombre: una omisión.

    ══ ⚠️ POR QUÉ NO SE ARREGLA AQUÍ: LA MISMA AMBIGÜEDAD DE F-91, OTRA VEZ ══
    F-89 P4 dice «el juez no aporta nada sobre un par de tablas QUE EL DIFF YA
    COMPARÓ». Y «comparó» admite dos lecturas:
      Lectura T — comparó LAS TABLAS. Se suprime todo hallazgo sobre ese par de
                  filas, mire la columna que mire. Es lo implementado.
      Lectura C — comparó ESE DATO. Se suprime solo lo que cae dentro de
                  comparedColumns; lo demás sigue siendo del juez.
    Es EXACTAMENTE la forma de «todas las columnas comunes» de F-90 P3, que
    F-91 tuvo que desambiguar. Y ya tenemos el caso medido que las separa: una
    columna de dieciocho.
    LA REGLA DE LA DOBLE LECTURA (protocolo, promovida de F-91 el 30/08) manda
    lo que hay que hacer: EJERCERLA CONTRA UN CASO MEDIDO Y PREGUNTAR. Hecho lo
    primero; queda lo segundo. VA A FABLE.
    LO QUE HAY QUE LLEVARLE, además de la cifra: que la lectura C tiene un
    coste evidente —vuelve a dejar pasar al juez sobre filas emparejadas, que
    es de donde salía B.124— y que por tanto no es «la buena» sin más; es una
    frontera que hay que trazar sabiendo las dos consecuencias.

    ══ ALCANCE: DÓNDE MUERDE Y DÓNDE NO ══
    Solo muerde donde las dos tablas emparejadas tienen columnas DISTINTAS.
      · OPE-10 / OPE-11 (tanda 1): 9 columnas y las 9 compartidas. Diferencia
        simétrica VACÍA → el agujero no existe ahí, y por eso la tanda 1 no lo
        vio ni podía verlo.
      · RRHH-06 / OPE-02 (tanda 2): 2 compartidas de 18 → el agujero es casi
        todo el par.
    NINGUNA MEDICIÓN TOMADA QUEDA INVALIDADA: la supresión se comportó igual en
    las veintiuna pasadas, y en la tanda 1 no había hueco que pisar.

    ══ QUÉ ES DEL FRENTE 1 Y QUÉ NO ══
    EL DEFECTO ES DE CÓDIGO DEL FRENTE 1 —punto 2, la supresión—, pero NO
    reabre el frente: su objetivo era que el juez no volviera a firmar
    'estructura', y eso está conseguido y verificado en veintiuna pasadas.
    Esto es un defecto de ALCANCE de la supresión, con especificación ambigua,
    y arreglarlo cambia lo que se publica. Necesita su consulta, su predicción
    y su tanda propias. Frente aparte.

  B.129 — [LÍMITE DE INSTRUMENTO · CERRADO EL 01/09 POR LA SIEMBRA] EL CORPUS DEL HARNESS NO PUEDE MEDIR LA DEGRADACIÓN DEL PUNTO 4.
    NO ES QUE SALGA CERO: ES QUE NO PUEDE SALIR OTRA COSA.
    QUÉ HACE FALTA PARA QUE `a_juicio.sin_clave` SE ACTIVE, las tres a la vez:
      1. Un par de tablas SIN clave (cae por la 1ª puerta), y
      2. con AL MENOS DOS columnas compartidas, y
      3. que el juez cite filas donde una compartida DIFIERA (para que R2
         confirme) y otra COINCIDA (para que haya ancla).
    Con UNA sola columna compartida las dos condiciones se pisan:
      · si esa columna difiere → R2 confirma, pero el ancla se calcula sobre
        las mismas columnas compartidas y sale VACÍA → r2.sin_ancla, descarte.
      · si coincide → differingColumns vacío → reclassify 'equivalentes'.
    Nunca `confirm` CON ancla. La degradación es IMPOSIBLE.
    LO MEDIDO EN EL CORPUS, los cuatro cruces de tablas sin clave:
      OPE-10 × OPE-02 → 0 columnas compartidas
      OPE-11 × OPE-02 → 0
      OPE-10 × RRHH-06 → 1 (Clínica)
      OPE-11 × RRHH-06 → 1 (Clínica)
    MÁXIMO UNA. Luego no hay ni un par en todo el corpus donde la rama pueda
    dispararse, y la «pasada extra» que se había planificado para medir el
    coste habría devuelto cero POR CONSTRUCCIÓN.
    SE PILLÓ ANTES DE GASTARLA, calculándolo en vez de lanzándolo. Es la
    tercera vez que el instrumento no puede fallar donde debería —B.121 (corpus
    simétrico), B.125 (juez intermitente), y ésta— y la primera que se detecta
    ANTES de la medición y no después.
    CONSECUENCIA PARA FABLE: su predicción de F-90 —«de una a cinco llamadas
    cortas por análisis»— NO SE PUEDE CONTRASTAR con este corpus. Lo que se le
    debe no es una cifra: es este porqué. Va en F-92.
    QUÉ HARÍA FALTA, sin decidirlo aquí: un documento de siembra nuevo, con una
    tabla que comparta DOS O MÁS columnas con el tarifario y NO tenga clave
    común con él (p. ej. columnas repetidas que no alcancen el 90% de
    unicidad). Es una pieza de corpus con su registro de siembra, no una
    pasada. Mientras no exista, la rama de degradación solo se ve en la batería
    —donde tampoco puede ejercerse, porque degradar es sobrevivir y sobrevivir
    llama al modelo (B.126)— o en un corpus de cliente.
    ⚠️ SEGUNDA BARRERA, MEDIDA EL 31/08 EN LA PASADA EXTRA: EL RERANK.
    La pasada se lanzó igual —OPE-11 analizado con OPE-10 y RRHH-06 en el
    corpus— sabiendo que no daría la cifra, para ver moverse r2.sin_ancla al
    menos una vez. Del log:
      Retrieval: 2 candidatos
      Rerank: 1 seleccionados
    RRHH-06 LLEGÓ AL RETRIEVAL Y EL RERANK LO DESCARTÓ. El juez no lo vio
    nunca, así que el cruce sin clave NO SE EJERCIÓ. La pasada no mide nada, y
    así queda anotada: no se reinterpreta.
    SON DOS BARRERAS INDEPENDIENTES, y conviene verlas juntas porque una sola
    no explicaría el bloqueo:
      1. NINGÚN cruce del corpus tiene DOS columnas compartidas sin clave —el
         máximo es una, Clínica—, así que aunque el juez comparara, la rama no
         podría dispararse.
      2. Y el único cruce que tiene ESA una, el rerank no lo deja llegar al
         juez, porque un tarifario y unas evaluaciones de desempeño no se
         parecen.
    La segunda es la que hace fútil cualquier intento de forzarlo con los
    documentos actuales: aunque se construyera el par de columnas, si los dos
    documentos no se parecen el rerank los separa. EL DOCUMENTO DE SIEMBRA QUE
    HAGA FALTA TIENE QUE PARECERSE AL TARIFARIO Y NO COMPARTIR CLAVE CON ÉL, y
    eso es más difícil de sembrar de lo que parecía.
    LO QUE SÍ VALIÓ LA PASADA, y no es nada: 15 discrepancias contra OPE-10 y
    el hallazgo del juez suprimido, CON TRES DOCUMENTOS EN EL CORPUS en vez de
    dos. El diff no se despista con más candidatos. Control de regresión
    gratis, aunque no fuera el objetivo.
    ══════════════════════════════════════════════════════════════════════
    ✅ CERRADO EL 01/09 — LA SIEMBRA LO RESOLVIÓ, Y LAS DOS BARRERAS A LA VEZ
    ══════════════════════════════════════════════════════════════════════
    `RRHH-08_asignacion-de-guardias.xlsx` y `OPE-13_cobertura-por-clinica.xlsx`
    (ver corpus-pruebas/SIEMBRA_guardias_cobertura.md) tienen TRES columnas
    compartidas —Clínica, Especialidad, Turno—, ninguna clave simple ni
    compuesta, y se parecen lo bastante para pasar el rerank: score máximo
    0,956 y candidato seleccionado EN LAS DOS DIRECCIONES.
    LA RAMA SE EJERCIÓ: «Turno de Dra. Ana Belmonte» bajó por `a_juicio.
    sin_clave` con 2 anclas y salió confirmada por juicio, en las dos pasadas.
    LO QUE RESOLVIÓ CADA BARRERA:
      · la primera —ningún cruce con dos columnas compartidas sin clave— se
        resolvió sembrando el par.
      · la segunda —el rerank— se resolvió con la pieza de diseño: LA COLUMNA DE
        IDENTIDAD SE LLAMA DISTINTO EN CADA DOCUMENTO (Profesional /
        Responsable), así que los dos hablan de las mismas personas —y por eso
        se parecen— pero esa columna no es compartida y no puede ser clave.
    LA LECCIÓN SOBREVIVE AL CIERRE, y es la que hizo que la siembra saliera a la
    primera: se calculó antes de gastar créditos. La sonda verificó unicidad
    simple y compuesta, columnas compartidas, nivel del reparto y el veredicto
    de R2 sobre las dos filas sembradas, todo sin lanzar nada.
    ⚠️ LEER JUNTO A ESO: la rama está SIN PROBAR por los dos lados a la vez —ni
    batería ni tanda—. Es el hueco de verificación más grande que deja el
    frente 1, y está declarado, no escondido.

  B.130 — [DEFECTO DE UNIVERSO DENTRO DE R2 · ARREGLADO EL 01/09] `'equivalentes'` AFIRMA SOBRE LAS FILAS ENTERAS
    CUANDO LA ESTRUCTURA SOLO MIRÓ LAS COLUMNAS CITADAS COMUNES.
    ES EL DEFECTO DE UNIVERSO POR TERCERA VEZ EN ESTE FRENTE, y las tres veces
    la misma pregunta sin respuesta escrita:
      · «todas las columnas COMUNES» (F-90 P3) → ¿comunes entre qué? → F-91
      · «el par de tablas que el diff ya COMPARÓ» (F-89 P4) → ¿comparó qué? → F-92
      · «todos los valores IGUALES» (R2) → ¿iguales entre qué? → aquí
    Las dos primeras eran de Fable. ESTA ES NUESTRA, y lleva en el código desde
    F-55.

    ══ EL MECANISMO ══
    R2 calcula `sharedColumns` = las columnas CITADAS por los dos lados, y si
    ninguna difiere devuelve `reclassify: 'equivalentes'`. La palabra afirma
    que las dos FILAS dicen lo mismo; lo que se comprobó es que coinciden en
    las columnas CITADAS COMUNES. No se miró nada de las demás.

    ══ POR QUÉ NO SE VE HOY, Y POR QUÉ SE VERÁ MAÑANA ══
    Hoy la supresión corre ANTES que R2, así que sobre un par emitido el
    hallazgo muere antes de llegar aquí. Con el reordenado de F-93 —R2 primero,
    la supresión consume su veredicto— este camino pasa a recorrerse SIEMPRE.
    El defecto no nace con el reordenado: se DESTAPA con él.

    ══ EL CASO MALO Y EL CASO BUENO, que dan el mismo resultado por razones
       opuestas ══
    · BELMONTE (hallazgo malo): columnas citadas comunes = {Empleado, Puesto},
      las dos coinciden → 'equivalentes' → desaparece. El resultado es correcto
      POR ACCIDENTE: no había nada que oponer, pero la estructura no lo sabía.
    · 44 CONTRA 40 HORAS bajo «Horas semana» y «Jornada semanal» (hallazgo
      BUENO, y es exactamente el territorio que F-92 le acaba de reservar al
      juez): las compartidas coinciden igual → 'equivalentes' → DESAPARECE UN
      HALLAZGO VERDADERO, enterrado bajo una palabra que afirma de más.

    ══ LA CORRECCIÓN, de F-93 P2 ══
    `'equivalentes'` solo se emite cuando TODAS las columnas citadas, DE LOS DOS
    LADOS, son compartidas —ninguna asimétrica citada— y todas coinciden.
    Entonces sí: la estructura vio todo lo que el hallazgo trata.
    Si el juez citó columnas que solo existen en un lado y las compartidas
    coinciden, lo único que la estructura puede decir es «sin oposición en lo
    compartido; columnas asimétricas citadas: [...]» → pasa a la llamada corta
    con ESE motivo, literal. Es la regla de MOTIVO LITERAL (F-92 P2) aplicada a
    una reclasificación en vez de a un descarte.
    Y `a_juicio.columna_no_comparada` —creado en F-92 para la supresión— pasa a
    contarse AQUÍ, que es donde de verdad ocurre.

    ══ LO QUE OBLIGA EN LA BATERÍA ══
    El caso 1 de F-92 se reescribe con su objetivo COMPLETO: no «sobrevive a la
    supresión» sino «LLEGA A LA LLAMADA CORTA con motivo columnas asimétricas
    citadas». Es la plantilla de «probamos el mecanismo y no el camino»
    aplicada a un caso de prueba: un test que verifica una puerta y no el
    destino no prueba lo que cree.
    Y hace falta un tercer caso que la corrección NO puede llevarse por
    delante: todo-compartido-todo-igual sigue siendo 'equivalentes'.

    ══ ARREGLADO EL 01/09, EN COMMIT PROPIO ══
    R2 renuncia a 'equivalentes' cuando el juez citó columnas asimétricas: la
    salida es `pass` con `asimetricasCitadas`, y la cascada baja el hallazgo a
    la llamada corta contando `a_juicio.columna_no_comparada` y nombrando esas
    columnas en el motivo (motivo literal, F-92 P2).
    LOS TRES CASOS, en finding-rules.test.ts: Belmonte (asimétricas citadas →
    pass), el construido 44 contra 40 bajo «Horas semana» y «Jornada semanal»
    (→ pass, y es el que de verdad justifica el arreglo: Belmonte sola se
    arreglaba «por accidente» porque era un hallazgo malo), y todo-compartido-
    todo-igual (sigue siendo 'equivalentes').
    MUTACIÓN: volver a la regla vieja → 2 rojos. Mirar las asimétricas de un
    solo lado → 2 rojos. Pasar siempre → 2 rojos, y caen los dos casos que
    protegen 'equivalentes'.
    ⚠️ LÍMITE DECLARADO, y es el mismo de siempre: F-93 P2 pedía que el caso
    verificara EL DESTINO —«llega a la llamada corta»— y NO SE PUEDE. Llegar a
    la llamada corta es SOBREVIVIR, y sobrevivir alcanza el modelo, que la
    guarda de red rompe (B.126). Lo probado es LA DECISIÓN de R2; el contador y
    el motivo viven en la rama `pass` de la cascada y quedan sin probar. Es el
    mismo hueco que dejó el punto 4, no uno nuevo — y ya van tres ramas que solo
    se pueden ver en producción.

  B.131 — [LÍMITE DE VERIFICACIÓN, abierto 01/09 · VA A FABLE] TRES RAMAS DEL
    FRENTE 1 SOLO SE PUEDEN VER EN PRODUCCIÓN, Y LAS TRES POR LA MISMA CAUSA.
    TRES NO ES UNA EXCEPCIÓN: ES UN PATRÓN.

    ══ LAS TRES ══
    1. LA DEGRADACIÓN DEL PUNTO 4 — sin clave pero con ancla, el hallazgo baja
       a la llamada corta. Ni batería ni tanda (B.129: el corpus no puede
       producirla, y el rerank tapa el único cruce que se acercaba).
       ⚠️ REDUCIDA EL 01/09, y conviene saber cuánto: la DECISIÓN se extrajo a
       `destinoSinClave` y HOY SE PRUEBA — la mutación «con ancla se descarta
       en vez de degradar», que había SOBREVIVIDO el 31/08 y se declaró como
       límite, ahora cae con dos rojos. Lo que queda sin probar es LA JUNTA:
       que la cascada, con el destino `degradar_a_juicio`, empuje de verdad el
       hallazgo hasta la llamada corta.
       Es la plantilla de F-93 P3 funcionando —partir en decisión y junta— y
       mide cuánto de este pendiente era el instrumento y cuánto la forma del
       código: la mitad.
    2. EL DESTINO DE B.130 — que el hallazgo con columnas asimétricas citadas
       LLEGUE a la llamada corta con su motivo. F-93 P2 lo pedía expresamente
       —«el caso verifica el destino, no la puerta»— y no se pudo. Se probó la
       DECISIÓN de R2; el contador `a_juicio.columna_no_comparada` y su motivo
       viven en la rama `pass` de la cascada, sin ejercer.
    3. EL CONTADOR DE LA JUNTA — `r2.degradado` al salir y
       `juicio.recibido_tabular` al entrar, que F-93 pide para que una junta
       rota se vea en el primer análisis real. Mismo problema: para verlos
       moverse hay que atravesar la junta, y atravesarla es sobrevivir.

    ══ LA CAUSA ÚNICA ══
    SOBREVIVIR ALCANZA EL MODELO. Todo lo que no muere en la capa determinista
    acaba en `verifyFindings`, y desde B.126 la guarda de red rompe cualquier
    test que llame fuera — que es lo correcto y no se toca: la guarda nació
    porque un test llevaba días llamando a la API, fallando con 401 y pasando
    en verde.
    Luego la batería puede probar TODO LO QUE MUERE y NADA DE LO QUE VIVE. Es
    una asimetría del instrumento, no de las ramas.

    ══ POR QUÉ IMPORTA MÁS DE LO QUE PARECE ══
    Las tres ramas no son casos raros: son EL DESTINO NORMAL del territorio que
    el frente 1 acaba de reservarle al juez. La degradación es lo que pasa en
    todas las tablas sin clave; el destino de B.130 es lo que pasa con toda
    columna asimétrica citada. O sea que lo NO verificable no es el margen: es
    la mitad viva del sistema.
    Y la regla de entrada exige mecanismo demostrado. Hoy se cumple para la
    mitad que muere y se DECLARA para la que vive.

    ══ LO QUE YA SE INTENTÓ, para que no se repita ══
    · Ejercerlo en la cascada → la guarda de red lo rompe (correctamente).
    · Ejercerlo en una tanda → B.129, el corpus no puede producir el caso.
    · Partirlo en decisión y junta (F-93 P3) → la decisión SÍ se prueba; la
      junta sigue sin probarse, solo con contadores a los extremos, que es
      vigilancia y no demostración.

    ══ ⚠️ REDUCIDO A LA MITAD EL 01/09, Y LA MITAD QUE SE FUE NO ERA DEL
       INSTRUMENTO: ERA DE LA FORMA DEL CÓDIGO ══
    La decisión de degradar vivía dentro de un `if` de la cascada, así que solo
    se observaba recorriendo un camino que acaba en el modelo. Extraída a
    `destinoSinClave(anclas)` —sin cambiar comportamiento ninguno, misma
    condición y mismo destino— la decisión quedó observable y mutable.
    LA MEDIDA, y es una cifra y no una impresión: la mutación «con ancla se
    descarta en vez de degradar» SOBREVIVIÓ el 31/08 y se declaró aquí como
    límite; hoy CAE con dos rojos. La mitad de este pendiente se arregló
    moviendo código de sitio.
    LO QUE ESO CAMBIA EN LO QUE HAY QUE CONSULTAR: la pregunta ya no es «cómo
    probamos estas tres ramas». Es más estrecha y más útil:
      · LAS DECISIONES SÍ SE PUEDEN PROBAR, y si no se podían era porque
        estaban escritas donde no se podía mirar. Eso lo arreglamos nosotros y
        no necesita consulta.
      · LAS JUNTAS NO. Que la cascada empuje el hallazgo hasta `toVerify`, que
        el contador se emita, que el motivo llegue — todo eso exige sobrevivir,
        y sobrevivir alcanza el modelo.
    ESTADO DE LAS TRES, con esa distinción aplicada:
      1. Degradación del punto 4 → DECISIÓN probada (destinoSinClave). Junta,
         no.
      2. Destino de B.130 → DECISIÓN probada (R2 devuelve `pass` con las
         asimétricas nombradas). Junta —contador y motivo en la cascada—, no.
      3. Contadores de la junta → son la junta por definición. Enteros sin
         probar.
    LA PREGUNTA PARA FABLE QUEDA ASÍ: ¿hay forma de ejercer una JUNTA que
    termina en el modelo sin llamar al modelo y sin mocks —que el alcance de
    Vitest prohíbe— o las juntas son declarables por naturaleza y lo que
    corresponde es exigirles contador en los dos extremos y una siembra?
    Antes de preguntarlo conviene aplicar la lección de esta reducción: mirar
    si lo que parece un límite del instrumento no es una decisión escrita en el
    sitio equivocado. Dos de las tres lo eran.


    ══════════════════════════════════════════════════════════════════════
    ⚠️ ESTADO EL 01/09: DOS DE LAS TRES, MEDIDAS EN PRODUCCIÓN. QUEDA UNA, Y
       CAMBIA DE NATURALEZA.
    ══════════════════════════════════════════════════════════════════════
    La tanda de la siembra (RRHH-08/OPE-13) ejerció las dos ramas que este
    pendiente declaraba imposibles de ver:
      1. DEGRADACIÓN DEL PUNTO 4 → MEDIDA. «Turno de Dra. Ana Belmonte» bajó
         por `a_juicio.sin_clave` con 2 anclas y salió confirmada por juicio, en
         las dos direcciones.
      2. DESTINO DE B.130 → MEDIDO. «Jornada semanal de Dr. Carlos Medina» bajó
         por `a_juicio.columna_no_comparada` con las cuatro columnas asimétricas
         nombradas en el motivo, y también salió confirmada.
      3. CONTADORES DE LA JUNTA → SIGUEN SIN EXISTIR. Verificado: `r2.degradado`
         y `juicio.recibido_tabular` no están en el código. F-93 P3 los pidió y
         no se escribieron.
    EL TERCERO YA NO ES «NO SE PUEDE VERIFICAR», ES «FALTA ESCRIBIRLOS». Y lo
    que iban a vigilar —que la junta funcione— ACABA DE VERSE FUNCIONAR: los dos
    hallazgos atravesaron la cascada entera hasta la llamada corta y volvieron
    confirmados. Los contadores siguen valiendo la pena, pero para el día que se
    rompa, no para demostrar que hoy funciona.
    LA PROPIEDAD DEL INSTRUMENTO SOBREVIVE Y SE QUEDA DECLARADA: la batería
    prueba todo lo que muere y nada de lo que vive. Lo que cambia es que ya no
    es un pendiente sin salida — LA SIEMBRA ES LA SALIDA, y está demostrada.
    LO QUE HAY QUE LLEVARLE A FABLE, ya sin la parte resuelta: su plantilla de
    F-93 P3 —partir en decisión y junta, probar la decisión en la suite y la
    junta con contadores y siembra— FUNCIONÓ ENTERA. La decisión se probó al
    extraer `destinoSinClave`, y la junta se probó sembrando. Lo único que queda
    de la receta sin aplicar son los contadores.

    NO SE PROPONE SOLUCIÓN AQUÍ, a propósito. Va a Fable en la próxima consulta.

  B.132 — [AVERÍA MEDIDA EN PRODUCCIÓN 01/09 · FRENTE 2, higiene] EL SYNC
    CUENTA SUS FALLOS EN UNA VARIABLE LOCAL Y LOS TIRA. Y EL CAMINO DE
    EMBEDDINGS NO REINTENTA UN 5xx: SOLO EL 429.
    ES EL MISMO PATRÓN QUE F-93 MANDÓ AL FRENTE 2 —«toda avería de red deja
    contador aunque el flujo continúe con fallback»— visto en un TERCER camino,
    y con evidencia de producción en vez de con un caso construido.

    ══ EL CASO ══
    Sincronizando la siembra de RRHH-08/OPE-13 (01/09): OPE-13 falló con un 500
    del endpoint de embed de Pinecone. RRHH-08 se indexó bien. El sync reportó
    «1 failed» y siguió con los demás — que es el aislamiento correcto y no se
    discute.

    ══ 1. LO QUE SE PIERDE, verificado en el código ══
    `failedCount` es una VARIABLE LOCAL de la función (sync/route.ts:159), se
    incrementa en cinco sitios y muere en dos salidas:
      · `console.log([DRIVE SYNC] Complete: ... N failed ...)` — vive lo que
        viva la retención de logs de Vercel.
      · el `stats.failed` de la respuesta HTTP — muere al cerrar la pestaña.
    NADA SE PERSISTE. Ni una fila, ni un contador, ni un campo en `documents`.
    QUÉ SE PIERDE EXACTAMENTE, y no es el número: es QUÉ DOCUMENTO falló y POR
    QUÉ. El nombre solo está en la línea de `console.error` de arriba. Mañana,
    la única señal de que OPE-13 no está en el corpus será que no está.

    ══ 2. LO QUE SÍ SE HIZO BIEN, y conviene decirlo ══
    El embed corre ANTES de escribir en Supabase (`generateEmbeddings` en la
    línea 253; el update y el insert en la 356 y la 394). Al fallar, la
    excepción sale por el `catch` del bucle SIN haber insertado la fila. NO
    QUEDA DOCUMENTO FANTASMA — nada de B.112 por esta vía. El aislamiento es
    correcto; lo que falta es la memoria.

    ══ 3. ⚠️ Y LO PEOR, QUE NO ES EL CONTADOR: EL 5xx NO SE REINTENTA ══
    `lib/embeddings.ts`:
        if (!isPineconeRateLimitError(error) || attempt >= MAX_EMBED_RETRIES)
          throw error;
    y `isPineconeRateLimitError` solo casa `429` o `RESOURCE_EXHAUSTED`. UN 500
    LANZA AL PRIMER INTENTO. El backoff exponencial con jitter que hay montado
    encima —cinco intentos— no cubre el caso que acaba de ocurrir.
    ES UNA REGLA DE CLAUDE.md INCUMPLIDA EN EL CAMINO DE INDEXACIÓN: «retry con
    backoff y fallback determinista». El retry existe y está bien hecho; su
    condición es demasiado estrecha.
    Y LA COMPARACIÓN QUE LO SITÚA: el cliente de análisis SÍ reintenta 5xx —
    cinco intentos, 2→5→10→15→20s (`lib/analysis/llm-client.ts`). O sea que el
    mismo proyecto trata mejor una caída de Anthropic que una de Pinecone, y no
    porque se decidiera: porque una condición se escribió pensando solo en el
    rate limit.
    UN 500 ES TRANSITORIO POR DEFINICIÓN. Reintentarlo habría metido OPE-13 sin
    que nadie se enterara.

    ══ EL PATRÓN, ya con tres apariciones ══
      · B.126 — el cliente del LLM se traga los fallos de red con fail-open, y
        un test llevaba días en verde sobre un 401.
      · F-93 → frente 2 — «toda avería de red deja contador aunque el flujo
        continúe con fallback».
      · ÉSTA — el sync cuenta y tira, y encima no reintenta.
    Las tres son la misma especie: ALGO FALLA, EL FLUJO SIGUE —correctamente— Y
    NO QUEDA RASTRO CONSULTABLE. Lo que las une no es la red: es que el
    aislamiento del fallo se implementó y la memoria del fallo no.

    ══ QUÉ HARÍA FALTA, sin decidirlo aquí ══
    (a) Ampliar la condición del reintento a 5xx en el camino de embeddings.
        Es el arreglo pequeño y probablemente el primero.
    (b) Que el sync deje constancia persistente de qué documentos fallaron y
        por qué — el sitio natural es el mismo debate de B.110, que sigue sin
        decidir dónde viven los contadores de incidencia.
    CRUZAR con B.110 (los contadores no tienen dónde vivir) y con la higiene
    que F-93 mandó al frente 2.
    ══ LA REGLA MÁS VIEJA DEL PROYECTO, MEDIDA POR PRIMERA VEZ (F-94 P4) ══
    Esto NO ES DEUDA NUEVA. `CLAUDE.md` dice, en «Reglas obligatorias»,
    «RETRY CON BACKOFF Y FALLBACK DETERMINISTA», y lleva ahí desde el principio.
    Lo que nunca se había hecho es medir cuánto se cumple. Medido el 01/09:

      Anthropic (llm/anthropic-client.ts:135)  5 intentos  429, 529 y todo >=500  ✔
      Pinecone embed (embeddings.ts:24)        6 intentos  SOLO 429 / RESOURCE_EXHAUSTED
      Pinecone datos (pinecone/vectors.ts)     ninguno     —
      Drive / OneDrive (lib/drive/*.ts)        ninguno     —

    CUATRO CAMINOS EXTERNOS, UNO CON RETRY DE 5xx, DOS SIN RETRY NINGUNO. Y el
    único que lo cubre es el que menos falta hacía: Anthropic ya era el más
    robusto. La laguna del de indexación —cubrir el límite de peticiones y no el
    5xx— es EXACTAMENTE por la que se perdió OPE-13 el 01/09.

    ⚠️ Y UNA PRECISIÓN SOBRE EL TEXTO DE LA REGLA, que hay que tener delante al
    implementarla: F-94 la cita como «regla 6» y con una tercera cláusula —«y
    mensaje útil al usuario si falla»— que NO ESTÁ ESCRITA EN EL REPOSITORIO. En
    CLAUDE.md la regla tiene dos cláusulas y es el punto DUODÉCIMO de la lista,
    no el sexto. El número y la tercera cláusula vienen del documento
    fundacional que vive FUERA del repositorio — el mismo sitio del que venía la
    lista de deuda técnica (B.134).
    Se anota porque el frente 2 va a construir cuatro commits sobre esta regla, y
    LA VERSIÓN QUE SE VA A IMPLEMENTAR ES MÁS LARGA QUE LA QUE ESTÁ ESCRITA.

    ══ ⚠️ CONFIRMADO TRANSITORIO — Y ESO AGRAVA EL PUNTO 3 ══
    Dato del director, mismo día: OPE-13 falló DOS VECES con el mismo 500 y
    entró A LA TERCERA sincronización, sin cambiar nada.
    LUEGO EL 500 ERA TRANSITORIO Y EL DOCUMENTO ERA INDEXABLE. Lo que hicieron
    tres sincronizaciones manuales es exactamente lo que un reintento con
    backoff habría hecho solo, en segundos y sin que nadie se enterara. No es
    que faltara una mejora: es que la que hay estaba escrita para el 429 y este
    caso pasó por al lado.
    Y EL TAMAÑO LO EMPEORA: los dos documentos tienen 15 chunks, o sea UN SOLO
    LOTE de embeddings (BATCH_SIZE = 20). Un único 500 se llevó el documento
    entero. Con más chunks el daño sería parcial y más visible; con pocos, es
    todo o nada y silencioso.

    ══ ⚠️ EL FICHERO QUEDA EXONERADO — investigación cerrada el 01/09 ══
    Cuando OPE-13 falló por CUARTA vez, y esta vez en un endpoint DISTINTO, se
    revisó el .xlsx byte a byte contra RRHH-08, que nunca falla.
    LOS DOS SON GEMELOS: mismo generador, las mismas 9 entradas en el zip,
    5.571 contra 5.567 bytes, 15 chunks cada uno, ninguno vacío, y —lo que
    cierra la vía— EL MISMO INVENTARIO DE CODEPOINTS, 61 distintos en los dos,
    SIN NI UN CODEPOINT QUE ESTÉ EN UNO Y NO EN EL OTRO. Se buscaron control
    (U+0000-1F), DEL, C1, BOM, U+FFFD, zero-width, marcas RTL, surrogates
    sueltos y U+00A0: cero en los dos.

    ══ EL ARGUMENTO QUE LO CIERRA, y no es la limpieza del fichero ══
    LA CUARTA LLAMADA NO LLEVABA NI UN BYTE DEL DOCUMENTO.
    `https://api.pinecone.io/indexes/documentation-hub` NO es upsert ni query:
    es el PLANO DE CONTROL, `describeIndex`. Y se llama porque
    `lib/pinecone.ts` pide el índice POR NOMBRE y sin host:
        return pc.index(process.env.PINECONE_INDEX || 'documentation-hub');
    así que el SDK tiene que preguntar dónde vive el índice antes de cualquier
    operación de datos. Esa petición lleva el NOMBRE DEL ÍNDICE y la CLAVE, y
    nada más.
    Un fallo ahí NO PUEDE CAUSARLO EL CONTENIDO DE UN DOCUMENTO. Es la clase de
    argumento que cierra una hipótesis sin depender de haber buscado bien.

    ══ Y LA METADATA TAMPOCO PUDO SER, por orden de ejecución ══
    En el sync el embed corre en la línea 253 y el upsert en la 273. Al fallar
    el embed, LA METADATA NUNCA SE ENVIÓ. Los tres fallos de `/embed` no
    pudieron deberse a los vectores ni a su metadata: no habían salido.

    ══ CORRECCIÓN A LO ESCRITO MÁS ARRIBA ══
    El bloque «CONFIRMADO TRANSITORIO» dedujo que las tres sincronizaciones
    manuales equivalían a un reintento automático. ERA PREMATURO: con un cuarto
    fallo en otro endpoint no se puede afirmar que el caso estuviera resuelto
    por insistir. Lo que SÍ se sostiene sin depender de eso es el hueco de este
    pendiente — el retry solo casa 429 y un 5xx lanza al primer intento, venga
    de donde venga.

    ══ LO QUE QUEDA POR DISCRIMINAR, con su experimento ══
    Si el patrón «siempre OPE-13» es CAUSAL o POSICIONAL. Tres pruebas, de más
    barata a menos:
      C. `GET /api/admin/diagnose-vectors?names=…` varias veces. Resuelve el
         índice por la MISMA llamada del cuarto fallo y no manda ni un byte de
         documento. Un 500 ahí cierra el caso: es de ellos.
      B. Sincronizar cada documento EN SOLITARIO. Si los dos entran solos, el
         fallo es de la segunda posición del lote y no del documento.
      A. Una copia byte a byte de OPE-13 con otro nombre. Si la copia entra y
         el original no, es del nombre o del id.
    ⚠️ NO HIZO FALTA PASAR DE LA C. Ver el cierre justo debajo.
    ══════════════════════════════════════════════════════════════════════
    ⚠️ CERRADO POR EXPERIMENTO EL 01/09: ES PINECONE. NO ES NUESTRO FICHERO
       NI NUESTRO CÓDIGO.
    ══════════════════════════════════════════════════════════════════════
    LA PRUEBA: `GET /api/admin/diagnose-vectors?names=RRHH-08…` —el documento
    que NUNCA falla— devolvió **500**, en el mismo endpoint del cuarto fallo y
    SIN NINGÚN DOCUMENTO DE POR MEDIO. El plano de control de Pinecone estaba
    caído.
    CERRADO POR EXPERIMENTO Y NO POR DESCARTE, y la distinción importa: la
    comparación byte a byte del fichero solo decía «no encontré nada», que es
    compatible con haber buscado mal. Una llamada que NO LLEVA EL FICHERO y
    falla igual lo exonera sin depender de la calidad de la búsqueda.

    LO QUE ESTO LE HACE A ESTE PENDIENTE: lo REFUERZA con evidencia de
    producción del peor caso posible. No es que se pierda el rastro de un fallo
    raro: ES QUE UN PROVEEDOR PUEDE ESTAR CAÍDO Y EL SYNC LO CUENTA EN UNA
    VARIABLE LOCAL Y LO TIRA. Cuatro fallos del mismo documento, tres
    sincronizaciones y un análisis, y al día siguiente la única señal habría
    sido que OPE-13 no está en el corpus.
    Y LA OTRA MITAD, igual de concreta: SI EL RETRY CUBRIERA LOS 5xx, ESTO SE
    HABRÍA REINTENTADO SOLO. Los cinco intentos con backoff ya están escritos;
    solo la condición los deja fuera.
    Las dos mitades siguen enrutadas al frente 2 y NO SE ARREGLAN HOY: tocan el
    camino de indexación, por donde entra todo al corpus.

    LA LECCIÓN DE INSTRUMENTO está promovida al protocolo — «para saber si la
    culpa es del dato, ejecuta el camino sin el dato». El fallo se atribuyó al
    documento durante cuatro intentos porque el único camino donde se veía
    llevaba un documento dentro; la prueba de carga cero lo separó en un minuto.


    ══ CONFIRMADO POR EL PROVEEDOR — el desenlace, con horas ══
    Pinecone abrió incidencia a las **15:04 UTC**: «5xx errors on some control
    plane operations», con **Index Management y Console en Partial Outage**.
    El primer fallo de OPE-13 fue a las **15:27**, veintitrés minutos después.
    A la hora de escribir esto, **sin resolver, más de cuarenta minutos abierta**.
    EL EXPERIMENTO APUNTÓ AL PLANO DE CONTROL Y EL PROVEEDOR LO CONFIRMA CON ESE
    NOMBRE. No es una coincidencia interpretada a favor: la prueba de carga cero
    señaló `api.pinecone.io/indexes/…` —control plane— antes de que hubiera
    ninguna incidencia publicada que leer.

    ══ ⚠️ Y ESTO AFINA EL PENDIENTE, no lo debilita: DOS MITADES DISTINTAS ══
    Escrito arriba quedó que «si el retry cubriera los 5xx, esto se habría
    reintentado solo». CON CUARENTA MINUTOS DE CAÍDA, ESO ES FALSO: cinco
    intentos con backoff exponencial cubren segundos, no una hora. El retry
    habría salvado un pico y no habría salvado esto.
    LA DISTINCIÓN QUE ORDENA EL PROBLEMA, y es la que hay que conservar:
      · EL RETRY cubre LO TRANSITORIO. Sigue faltando y sigue valiendo la pena
        —la mayoría de los 5xx son picos— pero su alcance es de segundos.
      · LA MEMORIA DEL FALLO cubre LO QUE DURA. Cuando la caída es larga, el
        reintento no puede hacer nada y lo único que queda es que el fallo deje
        rastro consultable: qué documento no entró, cuándo y por qué.
    Hoy no hay ninguna de las dos. Y son mitades independientes: arreglar el
    retry no quita la necesidad de la memoria, y al revés tampoco.

    ESTADO DE LA SIEMBRA: los dos documentos están indexados con 15 chunks cada
    uno y la siembra está verificada (ver corpus-pruebas/SIEMBRA_guardias_
    cobertura.md). La tanda queda PARADA hasta que Pinecone se recupere, y
    cuando vuelva se lanza sin más — no hay nada que rehacer.



  B.133 — [ABIERTO 01/09 CON EVIDENCIA DE PRODUCCIÓN · SIN RESOLVER · VA A
    FABLE CON EL FRENTE 2] QUÉ PASA CUANDO EL PROVEEDOR DE VECTORES ESTÁ CAÍDO
    MÁS DE LO QUE AGUANTA UN REINTENTO.

    ══ EL CASO QUE LO ABRE ══
    Pinecone, 01/09: incidencia abierta a las 15:04 UTC —«5xx errors on some
    control plane operations», Index Management y Console en Partial Outage—,
    **más de cuarenta minutos sin resolver**. Primer fallo nuestro a las 15:27.
    No es un supuesto: es lo que acaba de pasar.

    ══ LO QUE HOY SABEMOS QUE OCURRE, las tres cosas ══
    1. **LA APLICACIÓN NO PUEDE ANALIZAR NADA.** Todo el análisis pasa por
       recuperar candidatos, y recuperar pasa por resolver el índice. Sin
       Pinecone no hay pipeline.
    2. **EL DOCUMENTO QUE ESTABA ENTRANDO SE PIERDE SIN RASTRO CONSULTABLE.**
       El sync lo cuenta en una variable local y lo tira (B.132). Mañana la
       única señal de que no está en el corpus es que no está.
    3. **EL USUARIO VE UN ERROR TÉCNICO DE PINECONE.** No una frase que le diga
       qué ha pasado ni qué puede hacer: el mensaje del proveedor.

    ══ LA DISTINCIÓN QUE ORDENA EL PROBLEMA, aprendida hoy ══
    UN RETRY CUBRE LO TRANSITORIO; UNA CAÍDA LARGA NECESITA OTRA COSA.
    Cinco intentos con backoff exponencial cubren segundos. Con cuarenta
    minutos de caída no habrían salvado nada. Son dos problemas distintos con
    dos soluciones distintas, y confundirlos lleva a creer que arreglando el
    retry queda cubierto:
      · lo transitorio  → reintento (falta, y es B.132)
      · lo que dura     → memoria del fallo, y lo que decida esta entrada
    Ninguna de las dos sustituye a la otra.

    ══ NO SE PROPONE SOLUCIÓN AQUÍ, a propósito ══
    Va a Fable cuando toque el frente 2, junto con B.132 y la deuda de «SIN
    FALLBACK ENTRE PROVEEDORES».
    ⚠️ SOBRE ESA DEUDA, un aviso para quien vaya a cruzarla: **no la he
    encontrado escrita en el repositorio.** Se buscó en
    Puntos_Pendientes_Doclity.txt, Bitacora_Sesiones.txt, Decisiones_Fase_C.txt
    y claude/*.md. Existe en la lista del director; si va a citarse en una
    consulta, hay que traerla o escribirla, porque hoy no está donde se
    buscaría. (Es la regla de verificación al usar, aplicada a nuestra propia
    lista en vez de a una respuesta de Fable.)
    LO QUE SÍ APORTA HOY ESE CRUCE: esa deuda está escrita PARA EL LLM, y hoy
    hemos visto que **aplica igual a Pinecone**. La dependencia de un solo
    proveedor no es un problema del modelo: es un problema de cada pieza
    externa del camino, y el vectorial tiene la particularidad de que sin él no
    hay ni chat ni análisis.


    ══ IDEA PARA EL ARCHIVO, NO PARA AHORA (F-94 P5) ══
    MODO DEGRADADO DE RECUPERACIÓN POR BÚSQUEDA LÉXICA. Los textos de los chunks
    ya viven en Postgres (`document_chunks`), así que la búsqueda léxica que
    Postgres trae de serie permitiría recuperar candidatos y ANALIZAR CON EL
    ÍNDICE CAÍDO, declarándolo como degradado. Es «fallback determinista» en el
    sentido literal de la regla de CLAUDE.md.
    ⚠️ SU FLECHA DE RIESGO, que va pegada a la idea y no aparte:
        candidatos distintos → resultados distintos → DECLARAR SIEMPRE.
    Un análisis hecho en modo degradado no es el mismo análisis: la recuperación
    léxica y la semántica no eligen los mismos documentos. Si eso no se declara
    en pantalla y en el jsonb, el modo degradado se convierte en dos productos
    con el mismo nombre.
    NO SE DISEÑA AHORA y no es de este frente. Se mide cuando toque, y antes de
    medir hay que saber contra qué: la comparación es «qué candidatos elige cada
    modo sobre el mismo corpus», no «funciona o no funciona».

    CRUZAR con B.132 (la memoria del fallo y el retry) y con B.110 (dónde viven
    los contadores de incidencia, que sigue sin decidir).

  B.134 — [AUDITORÍA, hecha el 01/09] LA LISTA DE DEUDA TÉCNICA VIVE FUERA DEL
    REPOSITORIO, Y DE SUS CINCO PUNTOS **DOS ESTÁN RESUELTOS Y DOS ESTÁN MAL
    ENUNCIADOS**. Solo uno se sostiene tal como está escrito.

    ══ POR QUÉ SE HIZO ══
    Al abrir B.133 hubo que cruzar la deuda de «sin fallback entre proveedores»
    y NO SE ENCONTRÓ EN EL REPOSITORIO. Eso destapó lo de fondo: la lista de
    deuda no está versionada, así que nadie la verifica al usarla — y ya había
    precedente, el punto de los tokens OAuth se arrastró meses estando resuelto.
    Se revisaron los cinco contra el código. UNO A UNO Y CON RUTA, para que
    quien los cite pueda comprobarlo sin repetir el trabajo.

    ══ 1 · TOKENS OAUTH EN TEXTO PLANO — RESUELTO, y ya se sabía ══
    `lib/crypto.ts` (AES-256-GCM, clave `DRIVE_ENCRYPTION_KEY`) se usa en los
    cuatro puntos de escritura, y una consulta a `drive_connections` confirmó
    las dos filas cifradas. Retirado sin código (Bitacora_Sesiones.txt:4002).
    Es el precedente que justifica esta auditoría: se arrastraba en la lista
    SIN HABERSE COMPROBADO.

    ══ 2 · SIN OBSERVABILIDAD DE USO DE LLM POR USUARIO — ENUNCIADO FALSO ══
    Está registrado **por partida doble**, y las dos con `user_id`:
      · `logUsage` → tabla `usage_logs` (`lib/usage-logger.ts`): user_id, org_id,
        endpoint, modelo, tokens de entrada/salida, tokens de caché de escritura
        y lectura, latencia, éxito, créditos y la consulta del usuario.
      · `persistLLMUsage` → tabla `llm_usage` (`lib/observability/record-usage.ts`):
        además **coste en dólares calculado** (`computeCostUsd`, `llm-pricing.ts`).
    Los dos están CABLEADOS en los cuatro endpoints —ask, analyze-v2,
    analyze-style, improve— y `recordUsage` se llama desde tres puntos del
    cliente de Anthropic.
    LO QUE DE VERDAD FALTA, y es otra frase: **una VISTA por usuario**. La
    pantalla de `/settings/usage` agrega por organización y no desglosa por
    persona. El dato está; la pregunta no se puede hacer desde la interfaz.
    ⚠️ Y DE PASO, UN HALLAZGO QUE NADIE BUSCABA: hay **DOS SISTEMAS PARALELOS**
    de registro de uso escribiendo en dos tablas. No se ha mirado si uno
    sustituye al otro o si conviven a propósito. Merece decidirse antes de
    construir la vista, o la vista nacerá eligiendo tabla a ciegas.

    ══ 3 · ANÁLISIS SÍNCRONO CON EL USUARIO ESPERANDO — RESUELTO A MEDIAS ══
    El EXHAUSTIVO ya no lo es: crea un job en `analysis_jobs` y lo ejecuta el
    worker de Railway, con sondeo desde el cliente
    (`app/api/analysis-jobs/[id]/route.ts`).
    El RÁPIDO sí sigue siendo síncrono, con `maxDuration = 120` en
    `analyze-v2/route.ts`. Medido en las tandas de esta semana: **10 a 18
    segundos** con un candidato. Hay margen contra el tope, pero el margen no
    está medido con muchos candidatos.
    El enunciado hay que partirlo en dos: la mitad cara está hecha.

    ══ 4 · SIN FALLBACK ENTRE PROVEEDORES — VIVO, Y ES EL ÚNICO INTACTO ══
    Sin rastro en el repositorio: se buscó en Puntos_Pendientes_Doclity.txt,
    Bitacora_Sesiones.txt, Decisiones_Fase_C.txt y claude/*.md.
    `lib/pinecone.ts` y `lib/llm/anthropic-client.ts` tienen un solo proveedor
    cada uno, sin alternativa ni degradación.
    Y HOY SE HA ENSANCHADO: estaba escrito para el LLM, y la caída de Pinecone
    del 01/09 enseñó que aplica igual al vectorial — con el agravante de que sin
    él no hay ni chat ni análisis. Ver B.133.

    ══ 5 · MEMORIA CONVERSACIONAL DEL RAG — ENUNCIADO IMPRECISO ══
    La memoria conversacional EXISTE: `MAX_HISTORY_MESSAGES = 6` en `lib/rag.ts`,
    la reescritura de la consulta usa el historial (`rewriteQueryForSearch`) y
    el historial viaja al modelo en la llamada final.
    LO QUE NO SE REUTILIZA SON LOS DOCUMENTOS RECUPERADOS: cada turno vuelve a
    hacer el retrieval desde cero, aunque la pregunta siga siendo sobre los
    mismos documentos del turno anterior. Ese es el punto vivo, y es más
    estrecho que «no hay memoria conversacional».

    ══ EL RECUENTO, y la lección ══
      RESUELTO ENTERO ......... 1 (tokens OAuth)
      RESUELTO A MEDIAS ....... 1 (análisis síncrono: falta el rápido)
      MAL ENUNCIADO ........... 2 (observabilidad, memoria del RAG)
      VIVO TAL COMO ESTÁ ...... 1 (fallback entre proveedores)
    **Cuatro de cinco dirían algo falso si se citaran hoy en una consulta.**
    LA CAUSA ES ESTRUCTURAL Y NO DESCUIDO: la lista vive fuera del repositorio,
    así que no la toca ningún commit y nadie la verifica al usarla. Es
    exactamente la regla de verificación al usar (F-85 P3, protocolo) aplicada
    a un documento normativo que además **no está donde se verifica**.
    NO SE PROPONE DÓNDE DEBE VIVIR: es decisión del director.

  B.135 — [ABIERTO 01/09 al auditar la deuda · BLOQUEA A D2] DOS SISTEMAS
    PARALELOS REGISTRAN EL USO DE LLM, EN DOS TABLAS, Y NADIE SABE SI UNO
    SUSTITUYE AL OTRO.

    ══ LO QUE HAY ══
    · `logUsage` (`lib/usage-logger.ts`) → tabla **`usage_logs`**
      user_id, org_id, endpoint, model, input/output tokens, cache_creation y
      cache_read, latency_ms, success, credits_consumed, error_message,
      user_query.
    · `persistLLMUsage` / `recordUsage` (`lib/observability/record-usage.ts`)
      → tabla **`llm_usage`**
      org_id, user_id, operation, model, tokens (incluido cache_write), y
      **cost_usd calculado** con `llm-pricing.ts`.

    LOS DOS ESTÁN VIVOS Y CABLEADOS EN LOS MISMOS CUATRO ENDPOINTS —ask,
    analyze-v2, analyze-style, improve— y `recordUsage` se llama además desde
    tres puntos de `lib/llm/anthropic-client.ts`. O sea que una misma llamada al
    modelo puede quedar registrada DOS VECES, en dos tablas, con dos formas
    distintas de nombrar lo mismo (`endpoint` contra `operation`,
    `cache_creation_tokens` contra `cache_write_tokens`).

    ══ LO QUE NO SE SABE, y es lo que hay que decidir ══
    Si `llm_usage` nació para SUSTITUIR a `usage_logs` y la sustitución quedó a
    medias, o si conviven a propósito porque miden cosas distintas —uno el
    endpoint de cara al usuario, otro cada llamada al modelo—. Las dos lecturas
    son plausibles con el código delante y NO SE HA INVESTIGADO: se encontró
    auditando otra cosa.

    ══ POR QUÉ BLOQUEA ══
    D2 pide una vista de uso por usuario. **Construirla antes de decidir esto es
    elegir tabla a ciegas**, y la elección no es inocua: solo `llm_usage` tiene
    coste en dólares, y solo `usage_logs` tiene créditos consumidos y la
    consulta del usuario. Una vista sobre la tabla equivocada nace incompleta y
    con datos que parecen totales sin serlo.

    ══ Y UN RIESGO QUE NO SE HA MEDIDO ══
    Si las dos registran la misma llamada, **cualquier suma que cruce las dos
    tablas cuenta doble**. Hoy nadie las cruza, pero es exactamente el tipo de
    trampa que espera a la primera consulta de analítica que las junte sin
    saberlo.

    NO SE PROPONE CUÁL SE QUEDA: hace falta mirar cuándo nació cada una y qué
    consume cada tabla, y eso no se ha hecho.

  B.136 — [HUECO DOCUMENTAL CON CONSECUENCIA MEDIDA, abierto 01/09] EXISTE UN
    SUBSISTEMA ENTERO —EL AGENTE— QUE `CLAUDE.md` NO NOMBRA NI UNA VEZ, Y ESA
    OMISIÓN YA HA VIAJADO EN UNA CONSULTA.

    ══ LO MEDIDO ══
    `grep -ci "agente\|/api/agent" CLAUDE.md` → **0**.
    Y lo que existe en el repositorio:
      · `lib/agent/` — runner-conv.ts, system-prompt.ts, llm-call.ts,
        credit-calc.ts, persist-conv.ts, should-confirm.ts, types.ts
      · `lib/agent/tools/` — NUEVE herramientas: search-docs, read-doc,
        list-docs, usage-stats, escalate, finalize, ask-user, warn, index
      · `app/api/agent/conversations/` — con su endpoint de mensaje
      · `hasAgent` es CARACTERÍSTICA DE PLAN (`lib/stripe.ts:72,75,76`), al
        mismo nivel que hasDrive y hasAnalyticsPanel, y su endpoint la comprueba
        con `getOrgFeatures()` (message/route.ts:95)
      · TIENE RECONCILIACIÓN DE CRÉDITOS PROPIA, desde el worker
        (`lib/credits.ts:145`: «uso exclusivo: reconciliación final de tareas
        del agente»)
    Y `CLAUDE.md` enumera CUATRO endpoints con coste —ask, analyze-v2,
    analyze-style, improve— sin el agente; y lista las características de plan
    —Drive, Analytics, VariablePricing— sin `hasAgent`.

    ══ ⚠️ LA CONSECUENCIA, Y NO ES HIPOTÉTICA: YA OCURRIÓ ══
    La exploración previa al frente 2 enumeró «TODOS los puntos donde una
    llamada externa puede fallar» leyendo el repositorio con `CLAUDE.md` como
    mapa, y salieron CUATRO caminos: Anthropic, Pinecone embed, Pinecone datos,
    Drive. **Faltaba el del agente** — `lib/agent/llm-call.ts` llama al modelo
    por su propia vía.
    Esa enumeración viajó en F-94 y volvió contestada. **LA DECISIÓN DE LA
    REGLA 6 NO CAMBIA; SU ALCANCE SÍ**: cuando llegue el commit de los retries,
    el camino del agente entra en la cuenta, y hay que mirar además si su
    reconciliación de créditos desde el worker tiene los mismos huecos de
    memoria del fallo que B.132 encontró en el sync.

    ══ POR QUÉ PASÓ, que es lo que hay que retener ══
    `CLAUDE.md` es el documento que se carga como instrucciones del proyecto:
    lo que no está ahí no existe para quien empieza. No es que estuviera
    desactualizado en un detalle — es que un subsistema con su propia llamada al
    modelo, sus propios créditos y su propia característica de plan **no aparece
    en la arquitectura, ni en la tabla de costes, ni en la de planes, ni en la
    de ficheros clave**.
    ES LA TERCERA VEZ ESTA SEMANA que un documento normativo falla como premisa
    —el contrato de contadores, B.113, la lista de deuda (B.134)— pero las tres
    anteriores AFIRMABAN algo falso. Ésta CALLA algo cierto, que es más difícil
    de cazar: un `grep` que no devuelve nada parece una respuesta.

    NO SE ARREGLA AQUÍ. Qué debe decir CLAUDE.md del agente es decisión del
    director, y tocarlo sin él sería escribir la premisa de las sesiones
    siguientes por mi cuenta.

  B.137 — [ABIERTO 01/09 · LA BATERÍA PIERDE VIGILANCIA SIN DECIRLO · PREGUNTA
    SIN RESPUESTA] SIETE CASOS DESAPARECIERON Y LA SUITE SIGUIÓ EN VERDE

    ══ QUÉ PASÓ ══
    Al reescribir el guardián de `components/improvement/table-coverage.test.ts`
    partiéndolo en tres mitades, la edición reemplazó HASTA EL FINAL DEL FICHERO
    en vez de hasta el cierre del `describe`. Se llevó por delante siete casos
    que vivían debajo.
    LA SUITE SE QUEDÓ EN VERDE. 253 pasando, ni un rojo, ni un aviso. Lo único
    que lo delató fue que el recuento había BAJADO respecto a la corrida
    anterior — y lo único que hizo que alguien mirara el recuento fue la
    costumbre de anotarlo en cada informe. Restaurado desde HEAD reemplazando
    solo el `describe` de destino: 261.

    ══ POR QUÉ ES LO MÁS GRAVE DEL DÍA ══
    Es LA MISMA ENFERMEDAD que este frente lleva una semana persiguiendo en el
    código —una pieza deja de vigilar y nada lo dice— pero en el INSTRUMENTO que
    la vigila, que es el sitio donde no puede pasar.
    Y esta vez POR ACCIDENTE DE EDICIÓN, no por diseño. Un mecanismo retirado a
    propósito deja rastro en un commit y alguien puede leerlo; una edición que
    se come el final de un fichero no deja ninguno, y puede repetirse mañana
    exactamente igual sin que nadie se entere.

    ⚠️ ══ LA COINCIDENCIA QUE LO EMPEORA, y es la parte que hay que retener ══
    Pasó EN EL MISMO COMMIT en el que la mutación acababa de destapar que MEDIA
    FUNCIONALIDAD iba a entrar sin prueba (m1: el `Problem` dejaba de llevar la
    huella y la suite no se enteraba).
    Las dos cosas juntas se anulan: **si alguno de los siete casos perdidos
    hubiera sido de los que vigilan una mitad, esa mutación habría dado VERDE y
    la habríamos dado por buena.** La mutación no distingue entre «no hay caso
    que lo vigile» y «había uno y lo borré hace diez minutos». Lo que protege
    contra media funcionalidad sin prueba deja de proteger en cuanto la batería
    puede encoger en silencio, y el 01/09 las dos condiciones se dieron a la vez
    en el mismo fichero de trabajo.

    ══ LA PREGUNTA QUE HAY QUE CONTESTAR ══
    ¿HAY FORMA DE QUE LA SUITE AVISE CUANDO EL NÚMERO DE CASOS BAJA?
    Hoy el único guardián es que yo me fije en el recuento y lo compare con el
    de la corrida anterior de memoria. Eso no es un guardián: es una costumbre,
    y las costumbres no fallan en rojo.

    Lo que la pregunta tiene que resolver, y por eso NO se anota con solución:
    · un número que baja legítimamente (un test que se retira a propósito, un
      fichero que se divide) no puede ser un fallo, o el guardián se desactiva
      solo por ruido;
    · el umbral tiene que vivir en algún sitio que se actualice con el commit
      que sube el número, o caduca como caducó el protocolo de B.113;
    · y tiene que cazar el caso REAL, que no es «bajó de 261 a 253» sino «este
      fichero tenía 12 casos y ahora tiene 5», porque el total puede quedarse
      igual si otro commit añade siete en otra parte.

    NO SE DECIDE AQUÍ. Va con el frente de la regla 6 o donde el director lo
    ponga; lo que no puede es quedarse sin anotar, porque el accidente no dejó
    rastro en ningún sitio salvo este párrafo.

  B.138 — [ARREGLADO EL 01/09 EN EL MISMO DÍA QUE SE ENCONTRÓ · DAÑO
    IRREVERSIBLE, NUNCA OCURRIDO] UN 500 DE UN SEGUNDO BORRABA EL CORPUS DE
    DRIVE ENTERO DE LA ORGANIZACIÓN

    ══ LA CADENA, y cada eslabón era razonable por su cuenta ══
    1. `lib/drive/google.ts` — ante CUALQUIER respuesta no-2xx del listado:
       `console.error` y `return allFiles`, que estaba VACÍO. Sin lanzar y sin
       reintentar. En OneDrive lo mismo con otra forma: `graphGet` lanzaba, el
       bucle lo atrapaba y hacía `break` devolviendo lo acumulado.
    2. `app/api/drive/sync/route.ts` — ninguna guarda de listado vacío.
    3. Doce líneas más abajo: `docsToDelete` = todo `documents` con
       `source = provider.name` que no estuviera en el listado → TODO.
    4. `deleteDocument(..., reason: remote_deleted)`: vectores, fila y los
       análisis que colgaban. Y con el comentario «sin lápida», así que ni
       siquiera quedaba rastro de exclusión.

    Un 429 o un 500 de un segundo en la llamada a la carpeta raíz —el mismo
    tipo de incidente que Pinecone nos dio esta misma semana— y el corpus
    sincronizado de la organización desaparecía.

    ⚠️ ══ LO QUE LO HACE ENSEÑABLE, y es lo que hay que retener ══
    EL MISMO RAZONAMIENTO ESTABA RESUELTO BIEN A DOCE LÍNEAS. La lectura
    equivalente contra Supabase, en la misma función, aborta — y con el porqué
    escrito:
      «Critico: sin la lista de documentos existentes, el sync trataria todo el
       corpus como nuevo (reindexando y duplicando) y no detectaria borrados.
       Abortar es la unica opcion segura.»
    Mismo razonamiento, misma consecuencia, y en la lectura del proveedor
    externo se hacía lo contrario. NO FUE IGNORANCIA: alguien pensó el caso
    para una de las dos lecturas y nadie lo llevó a la otra. Es la especie que
    este frente persigue —dos sitios que deciden lo mismo y se separan— pero
    con daño irreversible en vez de un número mal contado.

    ══ LA CAUSA RAÍZ ERA EL TIPO — y es la parte que vale fuera de este caso ══
    `listFiles(): Promise<DriveFile[]>` no tenía sitio para «falló». Una lista
    vacía era la ÚNICA forma que las implementaciones tenían de volver de un
    error, y una lista vacía significa aguas abajo «el usuario vació la
    carpeta». Por eso el arreglo empieza en el tipo (`ResultadoDelListado`) y no
    en un `if`: con la firma vieja, el siguiente proveedor habría repetido el
    fallo sin enterarse.
    PROMOVIDA A REGLA en CLAUDE.md el 01/09: un tipo que no puede expresar el
    fallo obliga a inventarse un valor que lo signifique, y ese valor SIEMPRE
    significa ya otra cosa. Aquí la lista vacía significaba «carpeta vacía»; en
    otros sitios el cero significa «ninguno», la cadena vacía «sin nombre» y el
    `null` «todavía no». El fallo se disfraza del vecino y nadie lo ve venir.

    ══ POR QUÉ NO HABÍA EXPLOTADO, y no es un arreglo ══
    `grep -rn "api/drive"` sobre `app/**.tsx` y `components/` no devuelve NADA.
    El endpoint no tiene llamador en el repositorio: ni UI, ni herramienta del
    agente, ni cron (`vercel.json` no tiene `crons`). Solo se alcanza por HTTP
    directo, que es como se hicieron las sincronizaciones manuales de B.132.
    LA BOMBA ESTABA ARMADA Y NADIE TENÍA EL BOTÓN A MANO. Eso es suerte, y del
    tipo que se acaba el día que alguien pinte el botón.

    ══ EL PELIGRO DE VERDAD NO ERA «VACÍO»: ERA «INCOMPLETO» ══
    No hacía falta que fallara la primera llamada. En Google, el fallo de una
    SUBCARPETA devolvía una lista vacía en ese nivel y el padre seguía con el
    hueco dentro. En OneDrive, que PAGINA, un fallo en la página 3 de 5
    devolvía las dos primeras. La lista llegaba con contenido y le faltaban
    ficheros — y a los que faltaban se les borraba. Por eso el predicado no es
    «lista vacía» sino «lista no fiable», y por eso el fallo descarta la lista
    ENTERA.
    ⚠️⚠️ Y ESTO NO ESTABA EN EL DIAGNÓSTICO: UNA GUARDA DE «SI VIENE VACÍA,
    ABORTA» NO LO HABRÍA CAZADO. Es el arreglo que pedía el sentido común, es el
    que estuvo a punto de escribirse, y habría dejado vivo el caso peor —el que
    borra unos cuantos ficheros en vez de todos, que además es el que nadie
    notaría—. Se encontró abriendo las dos implementaciones para ver si OneDrive
    tenía «la misma forma»: no la tenía igual, la tenía peor.
    LA LECCIÓN DE MÉTODO: el diagnóstico de un fallo describe el CAMINO POR EL
    QUE SE VIO, no el conjunto de casos que lo producen. Comprobar el hermano
    —la otra implementación de la misma interfaz— es lo que reveló la variante.

    ══ QUÉ SE HIZO ══
    · `lib/drive/sync-guard.ts` — `ResultadoDelListado` y
      `decidirSincronizacion`, el único sitio donde se decide qué se borra. La
      ruta ya no cruza ids por su cuenta (`seenDriveIds` retirado: era la
      segunda implementación del mismo criterio).
    · Los dos proveedores convierten cualquier fallo en `ok: false`, en un solo
      punto cada uno.
    · La ruta aborta con 502 sin tocar nada, igual que hace con Supabase.
    · Siete casos deterministas. El listado LEGÍTIMAMENTE vacío SIGUE BORRANDO
      —vaciar la carpeta es cómo el usuario quita documentos— y eso queda
      declarado en un caso, no heredado por descuido.

    ══ LO QUE ESTO NO ARREGLA ══
    NO ES EL RETRY, y no lo sustituye. Con el proveedor caído cuarenta minutos
    el reintento se agotaría y —antes de hoy— el borrado habría ocurrido igual.
    Son las dos mitades de la regla 6 y ésta es la de «fallar bien». El retry de
    Drive sigue sin existir y va en su turno.

  B.139 — [ABIERTO 02/09 · NO TOCAR TODAVÍA · CAMINO DE DINERO] EL AGENTE COBRA
    Y DEVUELVE POR SU PROPIA VÍA, DISTINTA DE LA DE TODOS LOS DEMÁS

    Encontrado al hacer el censo de reembolsos de la regla 6, no buscándolo.

    ══ LAS DOS IMPLEMENTACIONES ══
    · TODOS LOS DEMÁS: `consumeCredits()` de `lib/credits.ts` para cobrar y
      `refundCredits()` para devolver.
    · EL AGENTE: `supabase.rpc('consume_credits')` DIRECTAMENTE
      (`app/api/agent/conversations/[id]/message/route.ts:162`) y `adjustCredits()`
      para devolver — en cinco sitios: cancelación, rechazo, parada, infraconsumo
      y fallo al insertar la fila.

    Es exactamente lo que CLAUDE.md prohíbe desde F-89 P2: dos caminos que
    deciden lo mismo —cuánto se cobra, cuánto se devuelve, dónde se apunta— y
    que el día que se separen nadie notará, porque los dos seguirán pareciendo
    correctos por su cuenta.

    ⚠️ ══ Y AQUÍ LA DUPLICACIÓN NO ESTÁ EN EL LADO MALO ══
    El agente es el que MEJOR lo hace de todo el sistema. Estima antes,
    reconcilia por tokens reales después y devuelve en cinco situaciones
    distintas. Los otros tres endpoints no devolvían NADA hasta el 01/09.
    O sea que unificar no es «llevar el agente al camino bueno»: es decidir
    cuál de los dos es el bueno, y probablemente sea el suyo.

    ══ POR QUÉ NO SE TOCA AHORA ══
    Es el camino de DINERO del subsistema mejor protegido. Un cambio ahí no se
    puede probar con la batería —`consume_credits` es una RPC y las rutas están
    fuera del alcance de Vitest— así que la verificación tendría que ser por
    lectura y por tanda, y merece su propia predicción y sus propias mitades.
    Decisión del director el 02/09: su commit, no ahora.

    ══ QUÉ HAY QUE DECIDIR CUANDO SE TOQUE ══
    1. Cuál de las dos vías queda: la del agente (estimar → reconciliar) es más
       fina, pero exige conocer los tokens reales, que los otros endpoints no
       tienen hasta después de responder.
    2. Si `adjustCredits` y `refundCredits` se funden. Hoy escriben en sitios
       distintos —conviene comprobar cuál toca `credits_extra` y cuál no— y esa
       diferencia puede ser deliberada o puede ser el primer síntoma de que ya
       se separaron.
    3. Qué pasa con el motivo: `adjustCredits` lleva `reason` y `refundCredits`
       no. Si la unificación pierde el motivo, se pierde la trazabilidad del
       único camino que hoy la tiene.

  F-81 [CONVERGENCIA — leer ANTES de planificar la fase 3 del diff, 27/08] No
    es un pendiente nuevo: es el sitio donde se juntan tres que ya existen, más
    una consecuencia. Se anota aquí porque el momento en que importan es el de
    planificar la fase 3, no el de descubrirlos a mitad del commit.
    Estado al escribirlo: fase 1 (fa22ca4e) y fase 2 (beaf075f) en main, 34
    casos deterministas, y SIN CONSUMIDORES — nada del pipeline las llama.

    1. B.110 DEJA DE SER APLAZABLE. KeyCounts y TableDiffCounts existen los
       dos, con dieciocho contadores fijos entre ambos (más dos banderas y un
       mapa por columna), y ninguno tiene dónde volcarse. La fase 3 no se puede
       escribir sin decidir eso, y decidirlo con el commit a medias es cómo se
       acaba con un contador por sitio — que es exactamente el estado del que
       B.110 viene. Ver B.110.

    2. B.111 EMPIEZA A PODER RESPONDERSE. Los cuatro contadores de ambigüedad
       del consenso (candidatas_discrepan, candidatas_parciales y los dos de
       valor repetido) solo dicen algo cuando esto corre contra documentos
       reales; con el corpus de pruebas son cero por construcción. Ver B.111.

    3. B.97 SE TOCA DE LADO, Y ESTA ES LA QUE MÁS CUIDADO PIDE. Si la fase 3
       emite igualTrasNormalizar como algo distinto de una discrepancia plena,
       está tomando por su cuenta la decisión que B.97 tiene abierta para R2.
       QUE SEA LA MISMA DECISIÓN, NO DOS — y si se toma en la fase 3, que
       CIERRE B.97 en el mismo movimiento, en vez de dejar dos criterios
       conviviendo en el mismo sistema. Ver B.97.

    4. Y LA CONSECUENCIA: la fase 3 es la que hace que el diff ACTÚE, y por
       tanto la PRIMERA que puede cumplir la condición 3 de la regla de entrada
       (protocolo, claude/Protocolo_Harness_Tasas.md). Hasta ahora el diff
       cumple mecanismo (condición 1, con los casos construidos) y no-regresión
       (condición 2) de forma TRIVIAL, porque no se ejecuta: no hay nada que
       pueda empeorar en algo que nadie llama. La fase 3 es donde las tres
       condiciones se vuelven exigibles de verdad.

  F-74 [ALCANCE DEL MVP, 27/08] Lo que entra y lo que NO entra antes de salir.
    DOCTRINA DE PRODUCTO DEL DIRECTOR, literal y por delante de todo lo demás:
      "el sistema no puede esconder hallazgos y confiar en que el usuario los
       encuentre repitiendo."
    Es el criterio que decide las cuatro exclusiones de abajo: ninguna puede
    justificarse con "el usuario siempre puede volver a lanzarlo".

    DENTRO DEL MVP, en orden:
      1. La ficha de AnalysisModal (B.96) — el modal de análisis sigue con dos
         <p> de citas crudas, la forma que F-70 ya retiró del modal de mejora.
      2. El !== crudo de applyDeterministicRules (B.97), EN OBSERVACIÓN: con la
         frase de F-70, un par visualmente idéntico queda en evidencia.
      3. El cobro sin devolución al lanzar excepción (B.99). Es el que peor
         queda con la doctrina de arriba: el cliente paga y no recibe nada.
      4. refund_credits como RPC atómica (B.100).

    FUERA DEL MVP, con su nombre para que nadie las dé por olvidadas:
      · PASADAS MÚLTIPLES (v2). Repetir el análisis del mismo par disparado por
        el contador de discrepantes-fuera. Fuera del MVP porque hoy no hay
        criterio medido de cuándo repetir; con uno malo, repetir es gastar
        créditos por si suena la flauta.
      · BOTÓN RÁPIDO/EXHAUSTIVO EN EL MODAL DE MEJORA. Hoy el "Reanalizar todo"
        de ese modal es siempre exhaustivo (useCrossDocAnalysis.ts:89), sin
        opción. La bandeja ya tiene los dos desde ff652755; el modal de mejora
        no.
      · onlyDocumentIds (F-61). La semántica que RESTRINGE el corpus, frente al
        batchDocumentIds de hoy que lo AMPLÍA. Con su propia batería: tocar el
        filtro de retrieval en medio de una auditoría serían dos variables a la
        vez.
      · FASE 2 DEL PROMPT. Todo lo que quedó del frente del juez y no era la
        cura de B.81.

    MEDICIÓN PENDIENTE QUE PROPONE FABLE — control comercial: RÁPIDO×N contra
    EXHAUSTIVO. Lanzar tres rápidos sobre el mismo par y comparar el resultado
    agregado con un exhaustivo. Si tres rápidos (15 créditos) consiguen casi lo
    mismo que un exhaustivo (30), eso FIJA EL PRECIO — y si lo consiguen del
    todo, cuestiona que el exhaustivo deba existir como producto aparte. Hoy no
    hay ningún dato que sostenga la diferencia de precio salvo el coste de
    Sonnet.
    OJO al diseñarla: desde 94ad06a0 los dos modos seleccionan igual, así que la
    comparación por fin mide alcance y garantía, no selección. Antes de F-73 no
    habría significado nada.

  F-20 [ESTADO, agosto 2026] Extracción estructurada. Paso 1 (extractSegments +
    EXTRACTOR_VERSION, commit 509e15d4) y paso 2 (persistencia en document_chunks,
    commit d00b085b) HECHOS. Paso 3 (job de reprocesado) RETIRADO — ver B.76. El
    siguiente paso del plan, si se retoma, es el paso 4. Registro SQL a posteriori
    del DDL de los pasos 1-2 (deuda documental, no ejecuta nada):
    supabase-f20-chunks-estructurados.sql (commit b84cd317). Fix de generación en
    mark-analyzed: commit b8d0df63 (ver B.73).

  C.8 — [NUEVO, diseño de Fable, S35] Ciclo de vida por conexión. Al cambiar o perder
    el proveedor de nube, los documentos del proveedor anterior NO se borran ni se
    siguen sirviendo: se INERTIZAN (perviven en BD pero fuera del chat y de los
    análisis) y REVIVEN al reconectar el mismo proveedor. Resuelve privacidad
    (ex-empleado que pierde acceso), fósiles y desconexión accidental. Idea del usuario.
    ORDEN DE FASE C (foto de fase F-17): F-9-rev CERRADO (barrido de resucitados
    ejecutado 15/08/2026, cero hallazgos — ver Bitácora "SESIÓN 38 (cierre)").
    Orden que queda: C.6 (fingerprints permanentes, prioridad subida por el
    portero) → C.5 (portero por hash, con dos adelantos ya anotados: la guarda de
    F-16 y que con staged compara contra staged.content_hash) → C.7 (aviso al
    borrar) → C.8 (ciclo de vida por conexión + formalizar CORPUS_ACTIVO) → C.4e
    (residual). F-17: lo peligroso de la Fase C queda detrás.

  [RESUELTO S35 como B.65 / D3-rev] ¿El análisis debe comparar contra documentos en estado 'pendiente'?
    Propuesta del usuario: NO contra pendientes sueltos (no validados), SÍ contra los
    del mismo lote de análisis (conecta con B.46, "analizar lote entre sí"), con una
    marca/estado que distinga "pendiente suelto" de "pendiente del lote actual".
    Técnicamente el retrieval puede filtrar por analysisStatus (está en metadata de
    Pinecone). Conecta con B.46, B.48 y los estados de C.8. — Implementado: compara
    solo contra 'analizado' en rápido y exhaustivo. La excepción del lote (B.46)
    queda para cuando exista el flujo de lotes.

  NOTAS PARA FASE D:
    - exclude_document_id en analysis_jobs es de facto el document_id del documento
      analizado. Fase D lo renombra o lo absorbe al migrar analysis_jobs (que ya
      necesita document_id para la cola por documento).
    - B.53 (document_id=null en resultados exhaustivos) sigue para Fase D.
    - Unificar el retrieval de app y worker en un módulo compartido (hoy el worker
      importa lib/analysis de la app; funciona, pero conviene formalizarlo). Fable
      lo situó en Fase D.

Forma de trabajo: diagnóstico con datos reales antes de arreglar; un
cambio lógico cada vez; build verde entre medias; SQL en Supabase antes
del push; plan completo antes de tocar varios archivos.
